{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "AcpConfig": {
      "additionalProperties": false,
      "properties": {
        "allowedAgents": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "backend": {
          "description": "Backend id registered by ACP runtime plugin (for example: acpx).",
          "type": "string"
        },
        "defaultAgent": {
          "type": "string"
        },
        "dispatch": {
          "$ref": "#/definitions/AcpDispatchConfig"
        },
        "enabled": {
          "description": "Global ACP runtime gate.",
          "type": "boolean"
        },
        "maxConcurrentSessions": {
          "type": "number"
        },
        "runtime": {
          "$ref": "#/definitions/AcpRuntimeConfig"
        },
        "stream": {
          "$ref": "#/definitions/AcpStreamConfig"
        }
      },
      "type": "object"
    },
    "AcpDispatchConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Master switch for ACP turn dispatch in the reply pipeline.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "AcpRuntimeConfig": {
      "additionalProperties": false,
      "properties": {
        "installCommand": {
          "description": "Optional operator install/setup command shown by `/acp install` and `/acp doctor`.",
          "type": "string"
        },
        "ttlMinutes": {
          "description": "Idle runtime TTL in minutes for ACP session workers.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "AcpStreamConfig": {
      "additionalProperties": false,
      "properties": {
        "coalesceIdleMs": {
          "description": "Coalescer idle flush window in milliseconds for ACP streamed text.",
          "type": "number"
        },
        "deliveryMode": {
          "description": "Live streams chunks or waits for terminal event before delivery.",
          "enum": [
            "live",
            "final_only"
          ],
          "type": "string"
        },
        "hiddenBoundarySeparator": {
          "description": "Separator inserted before visible text when hidden tool events occurred.",
          "enum": [
            "none",
            "space",
            "newline",
            "paragraph"
          ],
          "type": "string"
        },
        "maxChunkChars": {
          "description": "Maximum text size per streamed chunk.",
          "type": "number"
        },
        "maxOutputChars": {
          "description": "Maximum assistant output characters forwarded per turn.",
          "type": "number"
        },
        "maxSessionUpdateChars": {
          "description": "Maximum visible characters for projected session/update lines.",
          "type": "number"
        },
        "repeatSuppression": {
          "description": "Suppresses repeated ACP status/tool projection lines within a turn.",
          "type": "boolean"
        },
        "tagVisibility": {
          "$ref": "#/definitions/Partial%3CRecord%3CAcpSessionUpdateTag%2Cboolean%3E%3E",
          "description": "Per-sessionUpdate visibility overrides. Keys not listed here fall back to OpenClaw defaults."
        }
      },
      "type": "object"
    },
    "AgentAcpBinding": {
      "additionalProperties": false,
      "properties": {
        "acp": {
          "additionalProperties": false,
          "properties": {
            "backend": {
              "type": "string"
            },
            "cwd": {
              "type": "string"
            },
            "label": {
              "type": "string"
            },
            "mode": {
              "enum": [
                "persistent",
                "oneshot"
              ],
              "type": "string"
            }
          },
          "type": "object"
        },
        "agentId": {
          "type": "string"
        },
        "comment": {
          "type": "string"
        },
        "match": {
          "$ref": "#/definitions/AgentBindingMatch"
        },
        "type": {
          "const": "acp",
          "type": "string"
        }
      },
      "required": [
        "type",
        "agentId",
        "match"
      ],
      "type": "object"
    },
    "AgentBinding": {
      "anyOf": [
        {
          "$ref": "#/definitions/AgentRouteBinding"
        },
        {
          "$ref": "#/definitions/AgentAcpBinding"
        }
      ]
    },
    "AgentBindingMatch": {
      "additionalProperties": false,
      "properties": {
        "accountId": {
          "type": "string"
        },
        "channel": {
          "type": "string"
        },
        "guildId": {
          "type": "string"
        },
        "peer": {
          "additionalProperties": false,
          "properties": {
            "id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/ChatType"
            }
          },
          "required": [
            "kind",
            "id"
          ],
          "type": "object"
        },
        "roles": {
          "description": "Discord role IDs used for role-based routing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "teamId": {
          "type": "string"
        }
      },
      "required": [
        "channel"
      ],
      "type": "object"
    },
    "AgentCompactionConfig": {
      "additionalProperties": false,
      "properties": {
        "identifierInstructions": {
          "description": "Custom identifier-preservation instructions used when identifierPolicy is \"custom\".",
          "type": "string"
        },
        "identifierPolicy": {
          "$ref": "#/definitions/AgentCompactionIdentifierPolicy",
          "description": "Identifier-preservation instruction policy for compaction summaries."
        },
        "keepRecentTokens": {
          "description": "Pi keepRecentTokens budget used for cut-point selection.",
          "type": "number"
        },
        "maxHistoryShare": {
          "description": "Max share of context window for history during safeguard pruning (0.1–0.9, default 0.5).",
          "type": "number"
        },
        "memoryFlush": {
          "$ref": "#/definitions/AgentCompactionMemoryFlushConfig",
          "description": "Pre-compaction memory flush (agentic turn). Default: enabled."
        },
        "mode": {
          "$ref": "#/definitions/AgentCompactionMode",
          "description": "Compaction summarization mode."
        },
        "model": {
          "description": "Optional model override for compaction summarization (e.g. \"openrouter/anthropic/claude-sonnet-4-5\"). When set, compaction uses this model instead of the agent's primary model. Falls back to the primary model when unset.",
          "type": "string"
        },
        "postCompactionSections": {
          "description": "H2/H3 section names from AGENTS.md to inject after compaction. Defaults to [\"Session Startup\", \"Red Lines\"] when unset. Set to [] to disable post-compaction context injection entirely.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "qualityGuard": {
          "$ref": "#/definitions/AgentCompactionQualityGuardConfig",
          "description": "Optional quality-audit retries for safeguard compaction summaries."
        },
        "recentTurnsPreserve": {
          "description": "Preserve this many most-recent user/assistant turns verbatim in compaction summary context.",
          "type": "number"
        },
        "reserveTokens": {
          "description": "Pi reserve tokens target before floor enforcement.",
          "type": "number"
        },
        "reserveTokensFloor": {
          "description": "Minimum reserve tokens enforced for Pi compaction (0 disables the floor).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "AgentCompactionIdentifierPolicy": {
      "enum": [
        "strict",
        "off",
        "custom"
      ],
      "type": "string"
    },
    "AgentCompactionMemoryFlushConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Enable the pre-compaction memory flush (default: true).",
          "type": "boolean"
        },
        "forceFlushTranscriptBytes": {
          "description": "Force a memory flush when transcript size reaches this threshold (bytes, or byte-size string like \"2mb\"). Set to 0 to disable.",
          "type": [
            "number",
            "string"
          ]
        },
        "prompt": {
          "description": "User prompt used for the memory flush turn (NO_REPLY is enforced if missing).",
          "type": "string"
        },
        "softThresholdTokens": {
          "description": "Run the memory flush when context is within this many tokens of the compaction threshold.",
          "type": "number"
        },
        "systemPrompt": {
          "description": "System prompt appended for the memory flush turn.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "AgentCompactionMode": {
      "enum": [
        "default",
        "safeguard"
      ],
      "type": "string"
    },
    "AgentCompactionQualityGuardConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Enable compaction summary quality audits and regeneration retries. Default: false.",
          "type": "boolean"
        },
        "maxRetries": {
          "description": "Maximum regeneration retries after a failed quality audit. Default: 1 when enabled.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "AgentConfig": {
      "additionalProperties": false,
      "properties": {
        "agentDir": {
          "type": "string"
        },
        "default": {
          "type": "boolean"
        },
        "groupChat": {
          "$ref": "#/definitions/GroupChatConfig"
        },
        "heartbeat": {
          "additionalProperties": false,
          "description": "Optional per-agent heartbeat overrides.",
          "properties": {
            "accountId": {
              "description": "Optional account id for multi-account channels.",
              "type": "string"
            },
            "ackMaxChars": {
              "description": "Max chars allowed after HEARTBEAT_OK before delivery (default: 30).",
              "type": "number"
            },
            "activeHours": {
              "additionalProperties": false,
              "description": "Optional active-hours window (local time); heartbeats run only inside this window.",
              "properties": {
                "end": {
                  "description": "End time (24h, HH:MM). Exclusive. Use \"24:00\" for end-of-day.",
                  "type": "string"
                },
                "start": {
                  "description": "Start time (24h, HH:MM). Inclusive.",
                  "type": "string"
                },
                "timezone": {
                  "description": "Timezone for the window (\"user\", \"local\", or IANA TZ id). Default: \"user\".",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "directPolicy": {
              "description": "Direct/DM delivery policy. Default: \"allow\".",
              "enum": [
                "allow",
                "block"
              ],
              "type": "string"
            },
            "every": {
              "description": "Heartbeat interval (duration string, default unit: minutes; default: 30m).",
              "type": "string"
            },
            "includeReasoning": {
              "description": "When enabled, deliver the model's reasoning payload for heartbeat runs (when available) as a separate message prefixed with `Reasoning:` (same as `/reasoning on`).\n\nDefault: false (only the final heartbeat payload is delivered).",
              "type": "boolean"
            },
            "lightContext": {
              "description": "If true, run heartbeat turns with lightweight bootstrap context. Lightweight mode keeps only HEARTBEAT.md from workspace bootstrap files.",
              "type": "boolean"
            },
            "model": {
              "description": "Heartbeat model override (provider/model).",
              "type": "string"
            },
            "prompt": {
              "description": "Override the heartbeat prompt body (default: \"Read HEARTBEAT.md if it exists (workspace context). Follow it strictly. Do not infer or repeat old tasks from prior chats. If nothing needs attention, reply HEARTBEAT_OK.\").",
              "type": "string"
            },
            "session": {
              "description": "Session key for heartbeat runs (\"main\" or explicit session key).",
              "type": "string"
            },
            "suppressToolErrorWarnings": {
              "description": "Suppress tool error warning payloads during heartbeat runs.",
              "type": "boolean"
            },
            "target": {
              "anyOf": [
                {
                  "const": "last",
                  "type": "string"
                },
                {
                  "const": "none",
                  "type": "string"
                },
                {
                  "$ref": "#/definitions/ChannelId"
                }
              ],
              "description": "Delivery target (\"last\", \"none\", or a channel id)."
            },
            "to": {
              "description": "Optional delivery override (E.164 for WhatsApp, chat id for Telegram). Supports :topic:NNN suffix for Telegram topics.",
              "type": "string"
            }
          },
          "type": "object"
        },
        "humanDelay": {
          "$ref": "#/definitions/HumanDelayConfig",
          "description": "Human-like delay between block replies for this agent."
        },
        "id": {
          "type": "string"
        },
        "identity": {
          "$ref": "#/definitions/IdentityConfig"
        },
        "memorySearch": {
          "$ref": "#/definitions/MemorySearchConfig"
        },
        "model": {
          "$ref": "#/definitions/AgentModelConfig"
        },
        "name": {
          "type": "string"
        },
        "params": {
          "$ref": "#/definitions/Record%3Cstring%2Cunknown%3E",
          "description": "Optional per-agent stream params (e.g. cacheRetention, temperature)."
        },
        "runtime": {
          "$ref": "#/definitions/AgentRuntimeConfig",
          "description": "Optional runtime descriptor for this agent."
        },
        "sandbox": {
          "$ref": "#/definitions/AgentSandboxConfig",
          "description": "Optional per-agent sandbox overrides."
        },
        "skills": {
          "description": "Optional allowlist of skills for this agent (omit = all skills; empty = none).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "subagents": {
          "additionalProperties": false,
          "properties": {
            "allowAgents": {
              "description": "Allow spawning sub-agents under other agent ids. Use \"*\" to allow any.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "model": {
              "$ref": "#/definitions/AgentModelConfig",
              "description": "Per-agent default model for spawned sub-agents (string or {primary,fallbacks})."
            }
          },
          "type": "object"
        },
        "tools": {
          "$ref": "#/definitions/AgentToolsConfig"
        },
        "workspace": {
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "AgentContextPruningConfig": {
      "additionalProperties": false,
      "properties": {
        "hardClear": {
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "placeholder": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "hardClearRatio": {
          "type": "number"
        },
        "keepLastAssistants": {
          "type": "number"
        },
        "minPrunableToolChars": {
          "type": "number"
        },
        "mode": {
          "enum": [
            "off",
            "cache-ttl"
          ],
          "type": "string"
        },
        "softTrim": {
          "additionalProperties": false,
          "properties": {
            "headChars": {
              "type": "number"
            },
            "maxChars": {
              "type": "number"
            },
            "tailChars": {
              "type": "number"
            }
          },
          "type": "object"
        },
        "softTrimRatio": {
          "type": "number"
        },
        "tools": {
          "additionalProperties": false,
          "properties": {
            "allow": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "deny": {
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "ttl": {
          "description": "TTL to consider cache expired (duration string, default unit: minutes).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "AgentDefaultsConfig": {
      "additionalProperties": false,
      "properties": {
        "blockStreamingBreak": {
          "description": "Block streaming boundary:\n- \"text_end\": end of each assistant text content block (before tool calls)\n- \"message_end\": end of the whole assistant message (may include tool blocks)",
          "enum": [
            "text_end",
            "message_end"
          ],
          "type": "string"
        },
        "blockStreamingChunk": {
          "$ref": "#/definitions/BlockStreamingChunkConfig",
          "description": "Soft block chunking for streamed replies (min/max chars, prefer paragraph/newline)."
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Block reply coalescing (merge streamed chunks before send). idleMs: wait time before flushing when idle."
        },
        "blockStreamingDefault": {
          "description": "Default block streaming level when no override is present.",
          "enum": [
            "off",
            "on"
          ],
          "type": "string"
        },
        "bootstrapMaxChars": {
          "description": "Max chars for injected bootstrap files before truncation (default: 20000).",
          "type": "number"
        },
        "bootstrapPromptTruncationWarning": {
          "description": "Agent-visible bootstrap truncation warning mode:\n- off: do not inject warning text\n- once: inject once per unique truncation signature (default)\n- always: inject on every run with truncation",
          "enum": [
            "off",
            "once",
            "always"
          ],
          "type": "string"
        },
        "bootstrapTotalMaxChars": {
          "description": "Max total chars across all injected bootstrap files (default: 150000).",
          "type": "number"
        },
        "cliBackends": {
          "$ref": "#/definitions/Record%3Cstring%2CCliBackendConfig%3E",
          "description": "Optional CLI backends for text-only fallback (claude-cli, etc.)."
        },
        "compaction": {
          "$ref": "#/definitions/AgentCompactionConfig",
          "description": "Compaction tuning and pre-compaction memory flush behavior."
        },
        "contextPruning": {
          "$ref": "#/definitions/AgentContextPruningConfig",
          "description": "Opt-in: prune old tool results from the LLM context to reduce token usage."
        },
        "contextTokens": {
          "description": "Optional context window cap (used for runtime estimates + status %).",
          "type": "number"
        },
        "elevatedDefault": {
          "description": "Default elevated level when no /elevated directive is present.",
          "enum": [
            "off",
            "on",
            "ask",
            "full"
          ],
          "type": "string"
        },
        "embeddedPi": {
          "additionalProperties": false,
          "description": "Embedded Pi runner hardening and compatibility controls.",
          "properties": {
            "projectSettingsPolicy": {
              "description": "How embedded Pi should trust workspace-local `.pi/config/settings.json`.\n- sanitize (default): apply project settings except shellPath/shellCommandPrefix\n- ignore: ignore project settings entirely\n- trusted: trust project settings as-is",
              "enum": [
                "trusted",
                "sanitize",
                "ignore"
              ],
              "type": "string"
            }
          },
          "type": "object"
        },
        "envelopeElapsed": {
          "description": "Include elapsed time in message envelopes (\"on\" | \"off\", default: \"on\").",
          "enum": [
            "on",
            "off"
          ],
          "type": "string"
        },
        "envelopeTimestamp": {
          "description": "Include absolute timestamps in message envelopes (\"on\" | \"off\", default: \"on\").",
          "enum": [
            "on",
            "off"
          ],
          "type": "string"
        },
        "envelopeTimezone": {
          "description": "Envelope timestamp timezone: \"utc\" (default), \"local\", \"user\", or an IANA timezone string.",
          "type": "string"
        },
        "heartbeat": {
          "additionalProperties": false,
          "description": "Periodic background heartbeat runs.",
          "properties": {
            "accountId": {
              "description": "Optional account id for multi-account channels.",
              "type": "string"
            },
            "ackMaxChars": {
              "description": "Max chars allowed after HEARTBEAT_OK before delivery (default: 30).",
              "type": "number"
            },
            "activeHours": {
              "additionalProperties": false,
              "description": "Optional active-hours window (local time); heartbeats run only inside this window.",
              "properties": {
                "end": {
                  "description": "End time (24h, HH:MM). Exclusive. Use \"24:00\" for end-of-day.",
                  "type": "string"
                },
                "start": {
                  "description": "Start time (24h, HH:MM). Inclusive.",
                  "type": "string"
                },
                "timezone": {
                  "description": "Timezone for the window (\"user\", \"local\", or IANA TZ id). Default: \"user\".",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "directPolicy": {
              "description": "Direct/DM delivery policy. Default: \"allow\".",
              "enum": [
                "allow",
                "block"
              ],
              "type": "string"
            },
            "every": {
              "description": "Heartbeat interval (duration string, default unit: minutes; default: 30m).",
              "type": "string"
            },
            "includeReasoning": {
              "description": "When enabled, deliver the model's reasoning payload for heartbeat runs (when available) as a separate message prefixed with `Reasoning:` (same as `/reasoning on`).\n\nDefault: false (only the final heartbeat payload is delivered).",
              "type": "boolean"
            },
            "lightContext": {
              "description": "If true, run heartbeat turns with lightweight bootstrap context. Lightweight mode keeps only HEARTBEAT.md from workspace bootstrap files.",
              "type": "boolean"
            },
            "model": {
              "description": "Heartbeat model override (provider/model).",
              "type": "string"
            },
            "prompt": {
              "description": "Override the heartbeat prompt body (default: \"Read HEARTBEAT.md if it exists (workspace context). Follow it strictly. Do not infer or repeat old tasks from prior chats. If nothing needs attention, reply HEARTBEAT_OK.\").",
              "type": "string"
            },
            "session": {
              "description": "Session key for heartbeat runs (\"main\" or explicit session key).",
              "type": "string"
            },
            "suppressToolErrorWarnings": {
              "description": "Suppress tool error warning payloads during heartbeat runs.",
              "type": "boolean"
            },
            "target": {
              "anyOf": [
                {
                  "const": "last",
                  "type": "string"
                },
                {
                  "const": "none",
                  "type": "string"
                },
                {
                  "$ref": "#/definitions/ChannelId"
                }
              ],
              "description": "Delivery target (\"last\", \"none\", or a channel id)."
            },
            "to": {
              "description": "Optional delivery override (E.164 for WhatsApp, chat id for Telegram). Supports :topic:NNN suffix for Telegram topics.",
              "type": "string"
            }
          },
          "type": "object"
        },
        "humanDelay": {
          "$ref": "#/definitions/HumanDelayConfig",
          "description": "Human-like delay between block replies."
        },
        "imageMaxDimensionPx": {
          "description": "Max image side length (pixels) when sanitizing base64 image payloads in transcripts/tool results. Default: 1200.",
          "type": "number"
        },
        "imageModel": {
          "$ref": "#/definitions/AgentModelConfig",
          "description": "Optional image-capable model and fallbacks (provider/model). Accepts string or {primary,fallbacks}."
        },
        "maxConcurrent": {
          "description": "Max concurrent agent runs across all conversations. Default: 1 (sequential).",
          "type": "number"
        },
        "mediaMaxMb": {
          "description": "Max inbound media size in MB for agent-visible attachments (text note or future image attach).",
          "type": "number"
        },
        "memorySearch": {
          "$ref": "#/definitions/MemorySearchConfig",
          "description": "Vector memory search configuration (per-agent overrides supported)."
        },
        "model": {
          "$ref": "#/definitions/AgentModelConfig",
          "description": "Primary model and fallbacks (provider/model). Accepts string or {primary,fallbacks}."
        },
        "models": {
          "$ref": "#/definitions/Record%3Cstring%2CAgentModelEntryConfig%3E",
          "description": "Model catalog with optional aliases (full provider/model keys)."
        },
        "pdfMaxBytesMb": {
          "description": "Maximum PDF file size in megabytes (default: 10).",
          "type": "number"
        },
        "pdfMaxPages": {
          "description": "Maximum number of PDF pages to process (default: 20).",
          "type": "number"
        },
        "pdfModel": {
          "$ref": "#/definitions/AgentModelConfig",
          "description": "Optional PDF-capable model and fallbacks (provider/model). Accepts string or {primary,fallbacks}."
        },
        "repoRoot": {
          "description": "Optional repository root for system prompt runtime line (overrides auto-detect).",
          "type": "string"
        },
        "sandbox": {
          "$ref": "#/definitions/AgentSandboxConfig",
          "description": "Optional sandbox settings for non-main sessions."
        },
        "skipBootstrap": {
          "description": "Skip bootstrap (BOOTSTRAP.md creation, etc.) for pre-configured deployments.",
          "type": "boolean"
        },
        "subagents": {
          "additionalProperties": false,
          "description": "Sub-agent defaults (spawned via sessions_spawn).",
          "properties": {
            "announceTimeoutMs": {
              "description": "Gateway timeout in ms for sub-agent announce delivery calls (default: 60000).",
              "type": "number"
            },
            "archiveAfterMinutes": {
              "description": "Auto-archive sub-agent sessions after N minutes (default: 60).",
              "type": "number"
            },
            "maxChildrenPerAgent": {
              "description": "Maximum active children a single requester session may spawn. Default behavior: 5.",
              "type": "number"
            },
            "maxConcurrent": {
              "description": "Max concurrent sub-agent runs (global lane: \"subagent\"). Default: 1.",
              "type": "number"
            },
            "maxSpawnDepth": {
              "description": "Maximum depth allowed for sessions_spawn chains. Default behavior: 1 (no nested spawns).",
              "type": "number"
            },
            "model": {
              "$ref": "#/definitions/AgentModelConfig",
              "description": "Default model selection for spawned sub-agents (string or {primary,fallbacks})."
            },
            "runTimeoutSeconds": {
              "description": "Default run timeout in seconds for spawned sub-agents (0 = no timeout).",
              "type": "number"
            },
            "thinking": {
              "description": "Default thinking level for spawned sub-agents (e.g. \"off\", \"low\", \"medium\", \"high\").",
              "type": "string"
            }
          },
          "type": "object"
        },
        "thinkingDefault": {
          "description": "Default thinking level when no /think directive is present.",
          "enum": [
            "off",
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh",
            "adaptive"
          ],
          "type": "string"
        },
        "timeFormat": {
          "description": "Time format in system prompt: auto (OS preference), 12-hour, or 24-hour.",
          "enum": [
            "auto",
            "12",
            "24"
          ],
          "type": "string"
        },
        "timeoutSeconds": {
          "type": "number"
        },
        "typingIntervalSeconds": {
          "type": "number"
        },
        "typingMode": {
          "$ref": "#/definitions/TypingMode",
          "description": "Typing indicator start mode (never|instant|thinking|message)."
        },
        "userTimezone": {
          "description": "Optional IANA timezone for the user (used in system prompt; defaults to host timezone).",
          "type": "string"
        },
        "verboseDefault": {
          "description": "Default verbose level when no /verbose directive is present.",
          "enum": [
            "off",
            "on",
            "full"
          ],
          "type": "string"
        },
        "workspace": {
          "description": "Agent working directory (preferred). Used as the default cwd for agent runs.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "AgentElevatedAllowFromConfig": {
      "$ref": "#/definitions/Partial%3CRecord%3Cstring%2C(string%7Cnumber)%5B%5D%3E%3E"
    },
    "AgentModelConfig": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "additionalProperties": false,
          "properties": {
            "fallbacks": {
              "description": "Per-agent model fallbacks (provider/model).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "primary": {
              "description": "Primary model (provider/model).",
              "type": "string"
            }
          },
          "type": "object"
        }
      ]
    },
    "AgentModelEntryConfig": {
      "additionalProperties": false,
      "properties": {
        "alias": {
          "type": "string"
        },
        "params": {
          "$ref": "#/definitions/Record%3Cstring%2Cunknown%3E",
          "description": "Provider-specific API parameters (e.g., GLM-4.7 thinking mode)."
        },
        "streaming": {
          "description": "Enable streaming for this model (default: true, false for Ollama to avoid SDK issue #1205).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "AgentRouteBinding": {
      "additionalProperties": false,
      "properties": {
        "agentId": {
          "type": "string"
        },
        "comment": {
          "type": "string"
        },
        "match": {
          "$ref": "#/definitions/AgentBindingMatch"
        },
        "type": {
          "const": "route",
          "description": "Missing type is interpreted as route for backward compatibility.",
          "type": "string"
        }
      },
      "required": [
        "agentId",
        "match"
      ],
      "type": "object"
    },
    "AgentRuntimeAcpConfig": {
      "additionalProperties": false,
      "properties": {
        "agent": {
          "description": "ACP harness adapter id (for example codex, claude).",
          "type": "string"
        },
        "backend": {
          "description": "Optional ACP backend override for this agent runtime.",
          "type": "string"
        },
        "cwd": {
          "description": "Optional runtime working directory override.",
          "type": "string"
        },
        "mode": {
          "description": "Optional ACP session mode override.",
          "enum": [
            "persistent",
            "oneshot"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "AgentRuntimeConfig": {
      "anyOf": [
        {
          "additionalProperties": false,
          "properties": {
            "type": {
              "const": "embedded",
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "acp": {
              "$ref": "#/definitions/AgentRuntimeAcpConfig"
            },
            "type": {
              "const": "acp",
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "AgentSandboxConfig": {
      "additionalProperties": false,
      "properties": {
        "browser": {
          "$ref": "#/definitions/SandboxBrowserSettings",
          "description": "Optional sandboxed browser settings."
        },
        "docker": {
          "$ref": "#/definitions/SandboxDockerSettings",
          "description": "Docker-specific sandbox settings."
        },
        "mode": {
          "enum": [
            "off",
            "non-main",
            "all"
          ],
          "type": "string"
        },
        "perSession": {
          "description": "Legacy alias for scope (\"session\" when true, \"shared\" when false).",
          "type": "boolean"
        },
        "prune": {
          "$ref": "#/definitions/SandboxPruneSettings",
          "description": "Auto-prune sandbox settings."
        },
        "scope": {
          "description": "Container/workspace scope for sandbox isolation.",
          "enum": [
            "session",
            "agent",
            "shared"
          ],
          "type": "string"
        },
        "sessionToolsVisibility": {
          "description": "Session tools visibility for sandboxed sessions.\n- \"spawned\": only allow session tools to target sessions spawned from this session (default)\n- \"all\": allow session tools to target any session",
          "enum": [
            "spawned",
            "all"
          ],
          "type": "string"
        },
        "workspaceAccess": {
          "description": "Agent workspace access inside the sandbox.",
          "enum": [
            "none",
            "ro",
            "rw"
          ],
          "type": "string"
        },
        "workspaceRoot": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "AgentToolsConfig": {
      "additionalProperties": false,
      "properties": {
        "allow": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alsoAllow": {
          "description": "Additional allowlist entries merged into allow and/or profile allowlist.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "byProvider": {
          "$ref": "#/definitions/Record%3Cstring%2CToolPolicyConfig%3E",
          "description": "Optional tool policy overrides keyed by provider id or \"provider/model\"."
        },
        "deny": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "elevated": {
          "additionalProperties": false,
          "description": "Per-agent elevated exec gate (can only further restrict global tools.elevated).",
          "properties": {
            "allowFrom": {
              "$ref": "#/definitions/AgentElevatedAllowFromConfig",
              "description": "Approved senders for /elevated (per-provider allowlists)."
            },
            "enabled": {
              "description": "Enable or disable elevated mode for this agent (default: true).",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "exec": {
          "$ref": "#/definitions/ExecToolConfig",
          "description": "Exec tool defaults for this agent."
        },
        "fs": {
          "$ref": "#/definitions/FsToolsConfig",
          "description": "Filesystem tool path guards."
        },
        "loopDetection": {
          "$ref": "#/definitions/ToolLoopDetectionConfig",
          "description": "Runtime loop detection for repetitive/ stuck tool-call patterns."
        },
        "profile": {
          "$ref": "#/definitions/ToolProfileId",
          "description": "Base tool profile applied before allow/deny lists."
        },
        "sandbox": {
          "additionalProperties": false,
          "properties": {
            "tools": {
              "additionalProperties": false,
              "properties": {
                "allow": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "deny": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "AgentsConfig": {
      "additionalProperties": false,
      "properties": {
        "defaults": {
          "$ref": "#/definitions/AgentDefaultsConfig"
        },
        "list": {
          "items": {
            "$ref": "#/definitions/AgentConfig"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ApprovalsConfig": {
      "additionalProperties": false,
      "properties": {
        "exec": {
          "$ref": "#/definitions/ExecApprovalForwardingConfig"
        }
      },
      "type": "object"
    },
    "AudioConfig": {
      "additionalProperties": false,
      "properties": {
        "transcription": {
          "additionalProperties": false,
          "deprecated": "Use tools.media.audio.models instead.",
          "properties": {
            "command": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "timeoutSeconds": {
              "type": "number"
            }
          },
          "required": [
            "command"
          ],
          "type": "object"
        }
      },
      "type": "object"
    },
    "AuthConfig": {
      "additionalProperties": false,
      "properties": {
        "cooldowns": {
          "additionalProperties": false,
          "properties": {
            "billingBackoffHours": {
              "description": "Default billing backoff (hours). Default: 5.",
              "type": "number"
            },
            "billingBackoffHoursByProvider": {
              "$ref": "#/definitions/Record%3Cstring%2Cnumber%3E",
              "description": "Optional per-provider billing backoff (hours)."
            },
            "billingMaxHours": {
              "description": "Billing backoff cap (hours). Default: 24.",
              "type": "number"
            },
            "failureWindowHours": {
              "description": "Failure window for backoff counters (hours). If no failures occur within this window, counters reset. Default: 24.",
              "type": "number"
            }
          },
          "type": "object"
        },
        "order": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%5B%5D%3E"
        },
        "profiles": {
          "$ref": "#/definitions/Record%3Cstring%2CAuthProfileConfig%3E"
        }
      },
      "type": "object"
    },
    "AuthProfileConfig": {
      "additionalProperties": false,
      "properties": {
        "email": {
          "type": "string"
        },
        "mode": {
          "description": "Credential type expected in auth-profiles.json for this profile id.\n- api_key: static provider API key\n- oauth: refreshable OAuth credentials (access+refresh+expires)\n- token: static bearer-style token (optionally expiring; no refresh)",
          "enum": [
            "api_key",
            "oauth",
            "token"
          ],
          "type": "string"
        },
        "provider": {
          "type": "string"
        }
      },
      "required": [
        "provider",
        "mode"
      ],
      "type": "object"
    },
    "BedrockDiscoveryConfig": {
      "additionalProperties": false,
      "properties": {
        "defaultContextWindow": {
          "type": "number"
        },
        "defaultMaxTokens": {
          "type": "number"
        },
        "enabled": {
          "type": "boolean"
        },
        "providerFilter": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "refreshInterval": {
          "type": "number"
        },
        "region": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "BlockStreamingChunkConfig": {
      "additionalProperties": false,
      "properties": {
        "breakPreference": {
          "enum": [
            "paragraph",
            "newline",
            "sentence"
          ],
          "type": "string"
        },
        "maxChars": {
          "type": "number"
        },
        "minChars": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "BlockStreamingCoalesceConfig": {
      "additionalProperties": false,
      "properties": {
        "idleMs": {
          "type": "number"
        },
        "maxChars": {
          "type": "number"
        },
        "minChars": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "BroadcastConfig": {
      "additionalProperties": {
        "anyOf": [
          {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          {
            "$ref": "#/definitions/BroadcastStrategy"
          },
          {
            "not": {}
          }
        ],
        "description": "Map peer IDs to arrays of agent IDs that should ALL process messages.\n\nNote: the index signature includes `undefined` so `strategy?: ...` remains type-safe."
      },
      "properties": {
        "strategy": {
          "$ref": "#/definitions/BroadcastStrategy",
          "description": "Default processing strategy for broadcast peers."
        }
      },
      "type": "object"
    },
    "BroadcastStrategy": {
      "enum": [
        "parallel",
        "sequential"
      ],
      "type": "string"
    },
    "BrowserConfig": {
      "additionalProperties": false,
      "properties": {
        "attachOnly": {
          "description": "If true: never launch; only attach to an existing browser. Default: false",
          "type": "boolean"
        },
        "cdpPortRangeStart": {
          "description": "Starting local CDP port for auto-assigned browser profiles. Default derives from gateway port.",
          "type": "number"
        },
        "cdpUrl": {
          "description": "Base URL of the CDP endpoint (for remote browsers). Default: loopback CDP on the derived port.",
          "type": "string"
        },
        "color": {
          "description": "Accent color for the openclaw browser profile (hex). Default: #FF4500",
          "type": "string"
        },
        "defaultProfile": {
          "description": "Default profile to use when profile param is omitted. Default: \"chrome\"",
          "type": "string"
        },
        "enabled": {
          "type": "boolean"
        },
        "evaluateEnabled": {
          "description": "If false, disable browser act:evaluate (arbitrary JS). Default: true",
          "type": "boolean"
        },
        "executablePath": {
          "description": "Override the browser executable path (all platforms).",
          "type": "string"
        },
        "extraArgs": {
          "description": "Additional Chrome launch arguments. Useful for stealth flags, window size overrides, or custom user-agent strings. Example: [\"--window-size=1920,1080\", \"--disable-infobars\"]",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "headless": {
          "description": "Start Chrome headless (best-effort). Default: false",
          "type": "boolean"
        },
        "noSandbox": {
          "description": "Pass --no-sandbox to Chrome (Linux containers). Default: false",
          "type": "boolean"
        },
        "profiles": {
          "$ref": "#/definitions/Record%3Cstring%2CBrowserProfileConfig%3E",
          "description": "Named browser profiles with explicit CDP ports or URLs."
        },
        "relayBindHost": {
          "description": "Bind address for the Chrome extension relay server. Default: \"127.0.0.1\". Set to \"0.0.0.0\" for WSL2 or other environments where the relay must be reachable from a different network namespace.",
          "type": "string"
        },
        "remoteCdpHandshakeTimeoutMs": {
          "description": "Remote CDP WebSocket handshake timeout (ms). Default: max(remoteCdpTimeoutMs * 2, 2000).",
          "type": "number"
        },
        "remoteCdpTimeoutMs": {
          "description": "Remote CDP HTTP timeout (ms). Default: 1500.",
          "type": "number"
        },
        "snapshotDefaults": {
          "$ref": "#/definitions/BrowserSnapshotDefaults",
          "description": "Default snapshot options (applied by the browser tool/CLI when unset)."
        },
        "ssrfPolicy": {
          "$ref": "#/definitions/BrowserSsrFPolicyConfig",
          "description": "SSRF policy for browser navigation/open-tab operations."
        }
      },
      "type": "object"
    },
    "BrowserProfileConfig": {
      "additionalProperties": false,
      "properties": {
        "attachOnly": {
          "description": "If true, never launch a browser for this profile; only attach. Falls back to browser.attachOnly.",
          "type": "boolean"
        },
        "cdpPort": {
          "description": "CDP port for this profile. Allocated once at creation, persisted permanently.",
          "type": "number"
        },
        "cdpUrl": {
          "description": "CDP URL for this profile (use for remote Chrome).",
          "type": "string"
        },
        "color": {
          "description": "Profile color (hex). Auto-assigned at creation.",
          "type": "string"
        },
        "driver": {
          "description": "Profile driver (default: openclaw).",
          "enum": [
            "openclaw",
            "clawd",
            "extension"
          ],
          "type": "string"
        }
      },
      "required": [
        "color"
      ],
      "type": "object"
    },
    "BrowserSnapshotDefaults": {
      "additionalProperties": false,
      "properties": {
        "mode": {
          "const": "efficient",
          "description": "Default snapshot mode (applies when mode is not provided).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "BrowserSsrFPolicyConfig": {
      "additionalProperties": false,
      "properties": {
        "allowPrivateNetwork": {
          "description": "Legacy alias for private-network access. Prefer dangerouslyAllowPrivateNetwork.",
          "type": "boolean"
        },
        "allowedHostnames": {
          "description": "Explicitly allowed hostnames (exact-match), including blocked names like localhost. Example: [\"localhost\", \"metadata.internal\"]",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dangerouslyAllowPrivateNetwork": {
          "description": "If true, permit browser navigation to private/internal networks. Default: true",
          "type": "boolean"
        },
        "hostnameAllowlist": {
          "description": "Hostname allowlist patterns for browser navigation. Supports exact hosts and \"*.example.com\" wildcard subdomains.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "CanvasHostConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "liveReload": {
          "description": "Enable live-reload file watching + WS reloads (default: true).",
          "type": "boolean"
        },
        "port": {
          "description": "HTTP port to listen on (default: 18793).",
          "type": "number"
        },
        "root": {
          "description": "Directory to serve (default: ~/.openclaw/workspace/canvas).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ChannelDefaultsConfig": {
      "additionalProperties": false,
      "properties": {
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Default heartbeat visibility for all channels."
        }
      },
      "type": "object"
    },
    "ChannelHeartbeatVisibilityConfig": {
      "additionalProperties": false,
      "properties": {
        "showAlerts": {
          "description": "Show heartbeat alerts with actual content (default: true).",
          "type": "boolean"
        },
        "showOk": {
          "description": "Show HEARTBEAT_OK acknowledgments in chat (default: false).",
          "type": "boolean"
        },
        "useIndicator": {
          "description": "Emit indicator events for UI status display (default: true).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ChannelId": {
      "anyOf": [
        {
          "$ref": "#/definitions/ChatChannelId"
        },
        {
          "type": "string"
        }
      ]
    },
    "ChannelModelByChannelConfig": {
      "$ref": "#/definitions/Record%3Cstring%2CRecord%3Cstring%2Cstring%3E%3E"
    },
    "ChannelsConfig": {
      "properties": {
        "defaults": {
          "$ref": "#/definitions/ChannelDefaultsConfig"
        },
        "discord": {
          "$ref": "#/definitions/DiscordConfig"
        },
        "googlechat": {
          "$ref": "#/definitions/GoogleChatConfig"
        },
        "imessage": {
          "$ref": "#/definitions/IMessageConfig"
        },
        "irc": {
          "$ref": "#/definitions/IrcConfig"
        },
        "modelByChannel": {
          "$ref": "#/definitions/ChannelModelByChannelConfig",
          "description": "Map provider -> channel id -> model override."
        },
        "msteams": {
          "$ref": "#/definitions/MSTeamsConfig"
        },
        "signal": {
          "$ref": "#/definitions/SignalConfig"
        },
        "slack": {
          "$ref": "#/definitions/SlackConfig"
        },
        "telegram": {
          "$ref": "#/definitions/TelegramConfig"
        },
        "whatsapp": {
          "$ref": "#/definitions/WhatsAppConfig"
        },
        "feishu": {
          "description": "Feishu / Lark channel integration (requires openclaw feishu extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "appId": {
              "type": "string",
              "description": "Feishu app ID."
            },
            "appSecret": {
              "type": "string",
              "description": "Feishu app secret. Prefer env-var reference."
            },
            "encryptKey": {
              "type": "string",
              "description": "Feishu encrypt key for event webhook."
            },
            "verificationToken": {
              "type": "string"
            },
            "domain": {
              "type": "string",
              "enum": [
                "feishu",
                "lark"
              ],
              "default": "feishu",
              "description": "Domain: \"feishu\" (default) or \"lark\"."
            },
            "connectionMode": {
              "type": "string",
              "enum": [
                "websocket",
                "webhook"
              ],
              "default": "websocket",
              "description": "\"websocket\" (default): long-lived WebSocket connection managed by Feishu. \"webhook\": traditional HTTP POST to a public URL; requires verificationToken."
            },
            "webhookPath": {
              "type": "string",
              "default": "/feishu/events"
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "groupPolicy": {
              "$ref": "#/definitions/GroupPolicy"
            },
            "historyLimit": {
              "type": "integer",
              "minimum": 0
            },
            "configWrites": {
              "type": "boolean",
              "default": true
            },
            "accounts": {
              "type": "object",
              "description": "Multi-account configuration.",
              "additionalProperties": {
                "type": "object"
              }
            },
            "defaultAccount": {
              "type": "string"
            }
          }
        },
        "line": {
          "description": "LINE channel integration (requires openclaw line extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "channelAccessToken": {
              "type": "string",
              "description": "LINE channel access token."
            },
            "channelSecret": {
              "type": "string",
              "description": "LINE channel secret."
            },
            "webhookPath": {
              "type": "string",
              "default": "/line"
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "groupPolicy": {
              "$ref": "#/definitions/GroupPolicy"
            },
            "historyLimit": {
              "type": "integer",
              "minimum": 0
            },
            "mediaMaxMb": {
              "type": "number"
            }
          }
        },
        "matrix": {
          "description": "Matrix channel integration (requires openclaw matrix extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "homeserverUrl": {
              "type": "string",
              "description": "Matrix homeserver URL (e.g. https://matrix.org)."
            },
            "accessToken": {
              "type": "string",
              "description": "Matrix bot access token. Prefer env-var reference."
            },
            "userId": {
              "type": "string",
              "description": "Full Matrix user ID (e.g. @bot:matrix.org)."
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "groupPolicy": {
              "$ref": "#/definitions/GroupPolicy"
            },
            "historyLimit": {
              "type": "integer",
              "minimum": 0
            },
            "encryptionEnabled": {
              "type": "boolean",
              "default": false
            }
          }
        },
        "mattermost": {
          "description": "Mattermost channel integration (requires openclaw mattermost extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "botToken": {
              "type": "string",
              "description": "Mattermost bot access token. Prefer env-var reference."
            },
            "baseUrl": {
              "type": "string",
              "description": "Mattermost server base URL."
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "groupPolicy": {
              "$ref": "#/definitions/GroupPolicy"
            },
            "chatmode": {
              "type": "string",
              "enum": [
                "oncall",
                "onmessage",
                "onchar"
              ],
              "default": "oncall"
            },
            "historyLimit": {
              "type": "integer",
              "minimum": 0
            },
            "textChunkLimit": {
              "type": "integer"
            },
            "configWrites": {
              "type": "boolean",
              "default": true
            },
            "accounts": {
              "type": "object",
              "additionalProperties": {
                "type": "object"
              }
            },
            "defaultAccount": {
              "type": "string"
            }
          }
        },
        "nextcloud-talk": {
          "description": "Nextcloud Talk channel integration (requires openclaw nextcloud-talk extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "baseUrl": {
              "type": "string",
              "description": "Nextcloud instance URL."
            },
            "username": {
              "type": "string"
            },
            "password": {
              "type": "string",
              "description": "Prefer env-var reference."
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "groupPolicy": {
              "$ref": "#/definitions/GroupPolicy"
            },
            "pollIntervalMs": {
              "type": "integer",
              "default": 3000
            }
          }
        },
        "nostr": {
          "description": "Nostr channel integration (requires openclaw nostr extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "privateKey": {
              "type": "string",
              "description": "Nostr private key (nsec or hex). Prefer env-var reference."
            },
            "relays": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "WebSocket relay URLs."
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "historyLimit": {
              "type": "integer",
              "minimum": 0
            }
          }
        },
        "synologychat": {
          "description": "Synology Chat channel integration (requires openclaw synology-chat extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "incomingWebhook": {
              "type": "string",
              "description": "Synology Chat incoming webhook URL."
            },
            "token": {
              "type": "string",
              "description": "Synology Chat outgoing webhook token."
            },
            "webhookPath": {
              "type": "string",
              "default": "/synologychat"
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        },
        "tlon": {
          "description": "Tlon/Urbit channel integration (requires openclaw tlon extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "ship": {
              "type": "string",
              "description": "Urbit ship name (e.g. ~sampel-palnet)."
            },
            "url": {
              "type": "string",
              "description": "Urbit ship URL."
            },
            "code": {
              "type": "string",
              "description": "Urbit web login code. Prefer env-var reference."
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        },
        "twitch": {
          "description": "Twitch channel integration (requires openclaw twitch extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "accessToken": {
              "type": "string",
              "description": "Twitch OAuth access token. Prefer env-var reference."
            },
            "clientId": {
              "type": "string"
            },
            "channels": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Twitch channels to join."
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "commandPrefix": {
              "type": "string",
              "default": "!"
            }
          }
        },
        "webchat": {
          "description": "WebChat (embedded web widget) channel integration.",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "path": {
              "type": "string",
              "default": "/chat"
            },
            "allowOrigins": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "historyLimit": {
              "type": "integer",
              "minimum": 0
            }
          }
        },
        "zalo": {
          "description": "Zalo Official Account channel integration (requires openclaw zalo extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "accessToken": {
              "type": "string",
              "description": "Zalo OA access token. Prefer env-var reference."
            },
            "oaId": {
              "type": "string",
              "description": "Zalo Official Account ID."
            },
            "webhookPath": {
              "type": "string",
              "default": "/zalo"
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "historyLimit": {
              "type": "integer",
              "minimum": 0
            },
            "accounts": {
              "type": "object",
              "additionalProperties": {
                "type": "object"
              }
            },
            "defaultAccount": {
              "type": "string"
            }
          }
        },
        "zalouser": {
          "description": "Zalo Personal (user) channel integration (requires openclaw zalouser extension).",
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "phone": {
              "type": "string",
              "description": "Zalo personal phone number."
            },
            "dmPolicy": {
              "$ref": "#/definitions/DmPolicy"
            },
            "allowFrom": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      },
      "type": "object"
    },
    "ChatChannelId": {
      "enum": [
        "telegram",
        "whatsapp",
        "discord",
        "irc",
        "googlechat",
        "slack",
        "signal",
        "imessage",
        "line"
      ],
      "type": "string"
    },
    "ChatType": {
      "enum": [
        "direct",
        "group",
        "channel"
      ],
      "type": "string"
    },
    "CliBackendConfig": {
      "additionalProperties": false,
      "properties": {
        "args": {
          "description": "Base args applied to every invocation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "clearEnv": {
          "description": "Env vars to remove before launching this CLI.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "command": {
          "description": "CLI command to execute (absolute path or on PATH).",
          "type": "string"
        },
        "env": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E",
          "description": "Extra env vars injected for this CLI."
        },
        "imageArg": {
          "description": "Flag used to pass image paths.",
          "type": "string"
        },
        "imageMode": {
          "description": "How to pass multiple images.",
          "enum": [
            "repeat",
            "list"
          ],
          "type": "string"
        },
        "input": {
          "description": "Prompt input mode (default: arg).",
          "enum": [
            "arg",
            "stdin"
          ],
          "type": "string"
        },
        "maxPromptArgChars": {
          "description": "Max prompt length for arg mode (if exceeded, stdin is used).",
          "type": "number"
        },
        "modelAliases": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E",
          "description": "Model aliases mapping (config model id → CLI model id)."
        },
        "modelArg": {
          "description": "Flag used to pass model id (e.g. --model).",
          "type": "string"
        },
        "output": {
          "description": "Output parsing mode (default: json).",
          "enum": [
            "json",
            "text",
            "jsonl"
          ],
          "type": "string"
        },
        "reliability": {
          "additionalProperties": false,
          "description": "Runtime reliability tuning for this backend's process lifecycle.",
          "properties": {
            "watchdog": {
              "additionalProperties": false,
              "description": "No-output watchdog tuning (fresh vs resumed runs).",
              "properties": {
                "fresh": {
                  "additionalProperties": false,
                  "description": "Fresh/new sessions (non-resume).",
                  "properties": {
                    "maxMs": {
                      "description": "Upper bound for computed watchdog timeout.",
                      "type": "number"
                    },
                    "minMs": {
                      "description": "Lower bound for computed watchdog timeout.",
                      "type": "number"
                    },
                    "noOutputTimeoutMs": {
                      "description": "Fixed watchdog timeout in ms (overrides ratio when set).",
                      "type": "number"
                    },
                    "noOutputTimeoutRatio": {
                      "description": "Fraction of overall timeout used when fixed timeout is not set.",
                      "type": "number"
                    }
                  },
                  "type": "object"
                },
                "resume": {
                  "additionalProperties": false,
                  "description": "Resume sessions.",
                  "properties": {
                    "maxMs": {
                      "description": "Upper bound for computed watchdog timeout.",
                      "type": "number"
                    },
                    "minMs": {
                      "description": "Lower bound for computed watchdog timeout.",
                      "type": "number"
                    },
                    "noOutputTimeoutMs": {
                      "description": "Fixed watchdog timeout in ms (overrides ratio when set).",
                      "type": "number"
                    },
                    "noOutputTimeoutRatio": {
                      "description": "Fraction of overall timeout used when fixed timeout is not set.",
                      "type": "number"
                    }
                  },
                  "type": "object"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        },
        "resumeArgs": {
          "description": "Alternate args to use when resuming a session (use {sessionId} placeholder).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resumeOutput": {
          "description": "Output parsing mode when resuming a CLI session.",
          "enum": [
            "json",
            "text",
            "jsonl"
          ],
          "type": "string"
        },
        "serialize": {
          "description": "Serialize runs for this CLI.",
          "type": "boolean"
        },
        "sessionArg": {
          "description": "Flag used to pass session id (e.g. --session-id).",
          "type": "string"
        },
        "sessionArgs": {
          "description": "Extra args used when resuming a session (use {sessionId} placeholder).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "sessionIdFields": {
          "description": "JSON fields to read session id from (in order).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "sessionMode": {
          "description": "When to pass session ids.",
          "enum": [
            "always",
            "existing",
            "none"
          ],
          "type": "string"
        },
        "systemPromptArg": {
          "description": "Flag used to pass system prompt.",
          "type": "string"
        },
        "systemPromptMode": {
          "description": "System prompt behavior (append vs replace).",
          "enum": [
            "append",
            "replace"
          ],
          "type": "string"
        },
        "systemPromptWhen": {
          "description": "When to send system prompt.",
          "enum": [
            "first",
            "always",
            "never"
          ],
          "type": "string"
        }
      },
      "required": [
        "command"
      ],
      "type": "object"
    },
    "CliBannerTaglineMode": {
      "enum": [
        "random",
        "default",
        "off"
      ],
      "type": "string"
    },
    "CliConfig": {
      "additionalProperties": false,
      "properties": {
        "banner": {
          "additionalProperties": false,
          "properties": {
            "taglineMode": {
              "$ref": "#/definitions/CliBannerTaglineMode",
              "description": "Controls CLI banner tagline behavior.\n- \"random\": pick from tagline pool (default)\n- \"default\": always use DEFAULT_TAGLINE\n- \"off\": hide tagline text"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "CommandAllowFrom": {
      "$ref": "#/definitions/Record%3Cstring%2C(string%7Cnumber)%5B%5D%3E",
      "description": "Per-provider allowlist for command authorization. Keys are channel IDs (e.g., \"discord\", \"whatsapp\") or \"*\" for global default. Values are arrays of sender IDs allowed to use commands on that channel."
    },
    "CommandOwnerDisplay": {
      "enum": [
        "raw",
        "hash"
      ],
      "type": "string"
    },
    "CommandsConfig": {
      "additionalProperties": false,
      "properties": {
        "allowFrom": {
          "$ref": "#/definitions/CommandAllowFrom",
          "description": "Per-provider allowlist restricting who can use slash commands. If set, overrides the channel's allowFrom for command authorization. Use \"*\" key for global default, provider-specific keys override the global. Example: { \"*\": [\"user1\"], discord: [\"user:123\"] }"
        },
        "bash": {
          "description": "Allow bash chat command (`!`; `/bash` alias) (default: false).",
          "type": "boolean"
        },
        "bashForegroundMs": {
          "description": "How long bash waits before backgrounding (default: 2000; 0 backgrounds immediately).",
          "type": "number"
        },
        "config": {
          "description": "Allow /config command (default: false).",
          "type": "boolean"
        },
        "debug": {
          "description": "Allow /debug command (default: false).",
          "type": "boolean"
        },
        "native": {
          "$ref": "#/definitions/NativeCommandsSetting",
          "description": "Enable native command registration when supported (default: \"auto\")."
        },
        "nativeSkills": {
          "$ref": "#/definitions/NativeCommandsSetting",
          "description": "Enable native skill command registration when supported (default: \"auto\")."
        },
        "ownerAllowFrom": {
          "description": "Explicit owner allowlist for owner-only tools/commands (channel-native IDs).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "ownerDisplay": {
          "$ref": "#/definitions/CommandOwnerDisplay",
          "description": "How owner IDs are rendered in system prompts."
        },
        "ownerDisplaySecret": {
          "description": "Secret used to key owner ID hashes when ownerDisplay is \"hash\".",
          "type": "string"
        },
        "restart": {
          "description": "Allow restart commands/tools (default: true).",
          "type": "boolean"
        },
        "text": {
          "description": "Enable text command parsing (default: true).",
          "type": "boolean"
        },
        "useAccessGroups": {
          "description": "Enforce access-group allowlists/policies for commands (default: true).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "CronConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "failureAlert": {
          "$ref": "#/definitions/CronFailureAlertConfig"
        },
        "failureDestination": {
          "$ref": "#/definitions/CronFailureDestinationConfig",
          "description": "Default destination for failure notifications across all cron jobs."
        },
        "maxConcurrentRuns": {
          "type": "number"
        },
        "retry": {
          "$ref": "#/definitions/CronRetryConfig",
          "description": "Override default retry policy for one-shot jobs on transient errors."
        },
        "runLog": {
          "additionalProperties": false,
          "description": "Run-log pruning controls for `cron/runs/<jobId>.jsonl`. Defaults: `maxBytes=2_000_000`, `keepLines=2000`.",
          "properties": {
            "keepLines": {
              "type": "number"
            },
            "maxBytes": {
              "type": [
                "number",
                "string"
              ]
            }
          },
          "type": "object"
        },
        "sessionRetention": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "const": false,
              "type": "boolean"
            }
          ],
          "description": "How long to retain completed cron run sessions before automatic pruning. Accepts a duration string (e.g. \"24h\", \"7d\", \"1h30m\") or `false` to disable pruning. Default: \"24h\"."
        },
        "store": {
          "type": "string"
        },
        "webhook": {
          "description": "Deprecated legacy fallback webhook URL used only for stored jobs with notify=true. Prefer per-job delivery.mode=\"webhook\" with delivery.to.",
          "type": "string"
        },
        "webhookToken": {
          "$ref": "#/definitions/SecretInput",
          "description": "Bearer token for cron webhook POST delivery."
        }
      },
      "type": "object"
    },
    "CronFailureAlertConfig": {
      "additionalProperties": false,
      "properties": {
        "accountId": {
          "type": "string"
        },
        "after": {
          "type": "number"
        },
        "cooldownMs": {
          "type": "number"
        },
        "enabled": {
          "type": "boolean"
        },
        "mode": {
          "enum": [
            "announce",
            "webhook"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "CronFailureDestinationConfig": {
      "additionalProperties": false,
      "properties": {
        "accountId": {
          "type": "string"
        },
        "channel": {
          "type": "string"
        },
        "mode": {
          "enum": [
            "announce",
            "webhook"
          ],
          "type": "string"
        },
        "to": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "CronRetryConfig": {
      "additionalProperties": false,
      "properties": {
        "backoffMs": {
          "description": "Backoff delays in ms for each retry attempt (default: [30000, 60000, 300000]).",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "maxAttempts": {
          "description": "Max retries for transient errors before permanent disable (default: 3).",
          "type": "number"
        },
        "retryOn": {
          "description": "Error types to retry; omit to retry all transient types.",
          "items": {
            "$ref": "#/definitions/CronRetryOn"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "CronRetryOn": {
      "description": "Error types that can trigger retries for one-shot jobs.",
      "enum": [
        "rate_limit",
        "overloaded",
        "network",
        "timeout",
        "server_error"
      ],
      "type": "string"
    },
    "DiagnosticsCacheTraceConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "filePath": {
          "type": "string"
        },
        "includeMessages": {
          "type": "boolean"
        },
        "includePrompt": {
          "type": "boolean"
        },
        "includeSystem": {
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DiagnosticsConfig": {
      "additionalProperties": false,
      "properties": {
        "cacheTrace": {
          "$ref": "#/definitions/DiagnosticsCacheTraceConfig"
        },
        "enabled": {
          "type": "boolean"
        },
        "flags": {
          "description": "Optional ad-hoc diagnostics flags (e.g. \"telegram.http\").",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "otel": {
          "$ref": "#/definitions/DiagnosticsOtelConfig"
        },
        "stuckSessionWarnMs": {
          "description": "Threshold in ms before a processing session logs \"stuck session\" diagnostics.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "DiagnosticsOtelConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "endpoint": {
          "type": "string"
        },
        "flushIntervalMs": {
          "description": "Metric export interval (ms).",
          "type": "number"
        },
        "headers": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E"
        },
        "logs": {
          "type": "boolean"
        },
        "metrics": {
          "type": "boolean"
        },
        "protocol": {
          "enum": [
            "http/protobuf",
            "grpc"
          ],
          "type": "string"
        },
        "sampleRate": {
          "description": "Trace sample rate (0.0 - 1.0).",
          "type": "number"
        },
        "serviceName": {
          "type": "string"
        },
        "traces": {
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DiscordAccountConfig": {
      "additionalProperties": false,
      "properties": {
        "ackReaction": {
          "description": "Per-channel ack reaction override. Discord supports both unicode emoji and custom emoji names.",
          "type": "string"
        },
        "ackReactionScope": {
          "description": "When to send ack reactions for this Discord account. Overrides messages.ackReactionScope.",
          "enum": [
            "group-mentions",
            "group-all",
            "direct",
            "all",
            "off",
            "none"
          ],
          "type": "string"
        },
        "actions": {
          "$ref": "#/definitions/DiscordActionConfig",
          "description": "Per-action tool gating (default: true for all)."
        },
        "activity": {
          "description": "Bot activity status text (e.g. \"Watching X\").",
          "type": "string"
        },
        "activityType": {
          "description": "Activity type (0=Game, 1=Streaming, 2=Listening, 3=Watching, 4=Custom, 5=Competing). Defaults to 4 (Custom) when activity is set.",
          "enum": [
            0,
            1,
            2,
            3,
            4,
            5
          ],
          "type": "number"
        },
        "activityUrl": {
          "description": "Streaming URL (Twitch/YouTube). Required when activityType=1.",
          "type": "string"
        },
        "agentComponents": {
          "$ref": "#/definitions/DiscordAgentComponentsConfig",
          "description": "Agent-controlled interactive components (buttons, select menus)."
        },
        "allowBots": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "mentions",
              "type": "string"
            }
          ],
          "description": "Allow bot-authored messages to trigger replies (default: false). Set \"mentions\" to gate on mentions."
        },
        "allowFrom": {
          "description": "Alias for dm.allowFrom (prefer this so it inherits cleanly via base->account shallow merge). Legacy key: channels.discord.dm.allowFrom.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "autoPresence": {
          "$ref": "#/definitions/DiscordAutoPresenceConfig",
          "description": "Automatic runtime/quota presence signaling (status text + status mapping)."
        },
        "blockStreaming": {
          "description": "Disable block streaming for this account.",
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "commands": {
          "$ref": "#/definitions/ProviderCommandsConfig",
          "description": "Override native command registration for Discord (bool or \"auto\")."
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "dangerouslyAllowNameMatching": {
          "description": "Break-glass override: allow mutable identity matching (names/tags/slugs) in allowlists. Default behavior is ID-only matching.",
          "type": "boolean"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dm": {
          "$ref": "#/definitions/DiscordDmConfig"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Alias for dm.policy (prefer this so it inherits cleanly via base->account shallow merge). Legacy key: channels.discord.dm.policy."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "draftChunk": {
          "$ref": "#/definitions/BlockStreamingChunkConfig",
          "description": "Chunking config for Discord stream previews in `streaming: \"block\"`."
        },
        "enabled": {
          "description": "If false, do not start this Discord account. Default: true.",
          "type": "boolean"
        },
        "eventQueue": {
          "additionalProperties": false,
          "description": "Carbon EventQueue configuration. Controls how Discord gateway events are processed. `listenerTimeout` only covers gateway listener work such as normalization and enqueue. It does not control the lifetime of queued inbound agent turns.",
          "properties": {
            "listenerTimeout": {
              "description": "Max time (ms) a single listener can run before being killed. Default: 120000.",
              "type": "number"
            },
            "maxConcurrency": {
              "description": "Max concurrent event processing operations. Default: 50.",
              "type": "number"
            },
            "maxQueueSize": {
              "description": "Max events queued before backpressure is applied. Default: 10000.",
              "type": "number"
            }
          },
          "type": "object"
        },
        "execApprovals": {
          "$ref": "#/definitions/DiscordExecApprovalConfig",
          "description": "Exec approval forwarding configuration."
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how guild channel messages are handled:\n- \"open\": guild channels bypass allowlists; mention-gating applies\n- \"disabled\": block all guild channel messages\n- \"allowlist\": only allow channels present in discord.guilds.*.channels"
        },
        "guilds": {
          "$ref": "#/definitions/Record%3Cstring%2CDiscordGuildEntry%3E",
          "description": "New per-guild config keyed by guild id or slug."
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "type": "number"
        },
        "inboundWorker": {
          "additionalProperties": false,
          "description": "In-process worker settings for queued inbound Discord runs. This is separate from Carbon's eventQueue listener budget.",
          "properties": {
            "runTimeoutMs": {
              "description": "Max time (ms) a queued inbound run may execute before OpenClaw aborts it. Defaults to 1800000 (30 minutes). Set 0 to disable the worker-owned timeout.",
              "type": "number"
            }
          },
          "type": "object"
        },
        "intents": {
          "$ref": "#/definitions/DiscordIntentsConfig",
          "description": "Privileged Gateway Intents (must also be enabled in Discord Developer Portal)."
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "maxLinesPerMessage": {
          "description": "Soft max line count per Discord message. Discord clients can clip/collapse very tall messages; splitting by lines keeps replies readable in-channel. Default: 17.",
          "type": "number"
        },
        "mediaMaxMb": {
          "type": "number"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "pluralkit": {
          "$ref": "#/definitions/DiscordPluralKitConfig",
          "description": "PluralKit identity resolution for proxied messages."
        },
        "proxy": {
          "description": "HTTP(S) proxy URL for Discord gateway WebSocket connections.",
          "type": "string"
        },
        "replyToMode": {
          "$ref": "#/definitions/ReplyToMode",
          "description": "Control reply threading when reply tags are present (off|first|all)."
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "retry": {
          "$ref": "#/definitions/OutboundRetryConfig",
          "description": "Retry policy for outbound Discord API calls."
        },
        "slashCommand": {
          "$ref": "#/definitions/DiscordSlashCommandConfig",
          "description": "Slash command configuration."
        },
        "status": {
          "description": "Bot status (online|dnd|idle|invisible). Defaults to online when presence is configured.",
          "enum": [
            "online",
            "dnd",
            "idle",
            "invisible"
          ],
          "type": "string"
        },
        "streamMode": {
          "deprecated": "Legacy key; migrated automatically to `streaming`.",
          "enum": [
            "partial",
            "block",
            "off"
          ],
          "type": "string"
        },
        "streaming": {
          "anyOf": [
            {
              "$ref": "#/definitions/DiscordStreamMode"
            },
            {
              "type": "boolean"
            }
          ],
          "description": "Live stream preview mode:\n- \"off\": disable preview updates\n- \"partial\": edit a single preview message\n- \"block\": stream in chunked preview updates\n- \"progress\": alias that maps to \"partial\" on Discord\n\nLegacy boolean values are still accepted and auto-migrated."
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 2000.",
          "type": "number"
        },
        "threadBindings": {
          "$ref": "#/definitions/DiscordThreadBindingsConfig",
          "description": "Thread binding lifecycle settings (focus/subagent thread sessions)."
        },
        "token": {
          "$ref": "#/definitions/SecretInput"
        },
        "ui": {
          "$ref": "#/definitions/DiscordUiConfig",
          "description": "Discord UI customization (components, modals, etc.)."
        },
        "voice": {
          "$ref": "#/definitions/DiscordVoiceConfig",
          "description": "Voice channel conversation settings."
        }
      },
      "type": "object"
    },
    "DiscordActionConfig": {
      "additionalProperties": false,
      "properties": {
        "channelInfo": {
          "type": "boolean"
        },
        "channels": {
          "type": "boolean"
        },
        "emojiUploads": {
          "type": "boolean"
        },
        "events": {
          "type": "boolean"
        },
        "memberInfo": {
          "type": "boolean"
        },
        "messages": {
          "type": "boolean"
        },
        "moderation": {
          "type": "boolean"
        },
        "permissions": {
          "type": "boolean"
        },
        "pins": {
          "type": "boolean"
        },
        "polls": {
          "type": "boolean"
        },
        "presence": {
          "description": "Enable bot presence/activity changes (default: false).",
          "type": "boolean"
        },
        "reactions": {
          "type": "boolean"
        },
        "roleInfo": {
          "type": "boolean"
        },
        "roles": {
          "type": "boolean"
        },
        "search": {
          "type": "boolean"
        },
        "stickerUploads": {
          "type": "boolean"
        },
        "stickers": {
          "type": "boolean"
        },
        "threads": {
          "type": "boolean"
        },
        "voiceStatus": {
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DiscordAgentComponentsConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Enable agent-controlled interactive components (buttons, select menus). Default: true.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DiscordAutoPresenceConfig": {
      "additionalProperties": false,
      "properties": {
        "degradedText": {
          "description": "Optional custom status text while runtime/quota state is degraded or unknown.",
          "type": "string"
        },
        "enabled": {
          "description": "Enable automatic runtime/quota-based Discord presence updates. Default: false.",
          "type": "boolean"
        },
        "exhaustedText": {
          "description": "Optional custom status text while runtime detects quota/token exhaustion.",
          "type": "string"
        },
        "healthyText": {
          "description": "Optional custom status text while runtime is healthy; supports plain text.",
          "type": "string"
        },
        "intervalMs": {
          "description": "Poll interval for evaluating runtime availability state (ms). Default: 30000.",
          "type": "number"
        },
        "minUpdateIntervalMs": {
          "description": "Minimum spacing between actual gateway presence updates (ms). Default: 15000.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "DiscordConfig": {
      "additionalProperties": false,
      "properties": {
        "accounts": {
          "$ref": "#/definitions/Record%3Cstring%2CDiscordAccountConfig%3E",
          "description": "Optional per-account Discord configuration (multi-account)."
        },
        "ackReaction": {
          "description": "Per-channel ack reaction override. Discord supports both unicode emoji and custom emoji names.",
          "type": "string"
        },
        "ackReactionScope": {
          "description": "When to send ack reactions for this Discord account. Overrides messages.ackReactionScope.",
          "enum": [
            "group-mentions",
            "group-all",
            "direct",
            "all",
            "off",
            "none"
          ],
          "type": "string"
        },
        "actions": {
          "$ref": "#/definitions/DiscordActionConfig",
          "description": "Per-action tool gating (default: true for all)."
        },
        "activity": {
          "description": "Bot activity status text (e.g. \"Watching X\").",
          "type": "string"
        },
        "activityType": {
          "description": "Activity type (0=Game, 1=Streaming, 2=Listening, 3=Watching, 4=Custom, 5=Competing). Defaults to 4 (Custom) when activity is set.",
          "enum": [
            0,
            1,
            2,
            3,
            4,
            5
          ],
          "type": "number"
        },
        "activityUrl": {
          "description": "Streaming URL (Twitch/YouTube). Required when activityType=1.",
          "type": "string"
        },
        "agentComponents": {
          "$ref": "#/definitions/DiscordAgentComponentsConfig",
          "description": "Agent-controlled interactive components (buttons, select menus)."
        },
        "allowBots": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "mentions",
              "type": "string"
            }
          ],
          "description": "Allow bot-authored messages to trigger replies (default: false). Set \"mentions\" to gate on mentions."
        },
        "allowFrom": {
          "description": "Alias for dm.allowFrom (prefer this so it inherits cleanly via base->account shallow merge). Legacy key: channels.discord.dm.allowFrom.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "autoPresence": {
          "$ref": "#/definitions/DiscordAutoPresenceConfig",
          "description": "Automatic runtime/quota presence signaling (status text + status mapping)."
        },
        "blockStreaming": {
          "description": "Disable block streaming for this account.",
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "commands": {
          "$ref": "#/definitions/ProviderCommandsConfig",
          "description": "Override native command registration for Discord (bool or \"auto\")."
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "dangerouslyAllowNameMatching": {
          "description": "Break-glass override: allow mutable identity matching (names/tags/slugs) in allowlists. Default behavior is ID-only matching.",
          "type": "boolean"
        },
        "defaultAccount": {
          "description": "Optional default account id when multiple accounts are configured.",
          "type": "string"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dm": {
          "$ref": "#/definitions/DiscordDmConfig"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Alias for dm.policy (prefer this so it inherits cleanly via base->account shallow merge). Legacy key: channels.discord.dm.policy."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "draftChunk": {
          "$ref": "#/definitions/BlockStreamingChunkConfig",
          "description": "Chunking config for Discord stream previews in `streaming: \"block\"`."
        },
        "enabled": {
          "description": "If false, do not start this Discord account. Default: true.",
          "type": "boolean"
        },
        "eventQueue": {
          "additionalProperties": false,
          "description": "Carbon EventQueue configuration. Controls how Discord gateway events are processed. `listenerTimeout` only covers gateway listener work such as normalization and enqueue. It does not control the lifetime of queued inbound agent turns.",
          "properties": {
            "listenerTimeout": {
              "description": "Max time (ms) a single listener can run before being killed. Default: 120000.",
              "type": "number"
            },
            "maxConcurrency": {
              "description": "Max concurrent event processing operations. Default: 50.",
              "type": "number"
            },
            "maxQueueSize": {
              "description": "Max events queued before backpressure is applied. Default: 10000.",
              "type": "number"
            }
          },
          "type": "object"
        },
        "execApprovals": {
          "$ref": "#/definitions/DiscordExecApprovalConfig",
          "description": "Exec approval forwarding configuration."
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how guild channel messages are handled:\n- \"open\": guild channels bypass allowlists; mention-gating applies\n- \"disabled\": block all guild channel messages\n- \"allowlist\": only allow channels present in discord.guilds.*.channels"
        },
        "guilds": {
          "$ref": "#/definitions/Record%3Cstring%2CDiscordGuildEntry%3E",
          "description": "New per-guild config keyed by guild id or slug."
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "type": "number"
        },
        "inboundWorker": {
          "additionalProperties": false,
          "description": "In-process worker settings for queued inbound Discord runs. This is separate from Carbon's eventQueue listener budget.",
          "properties": {
            "runTimeoutMs": {
              "description": "Max time (ms) a queued inbound run may execute before OpenClaw aborts it. Defaults to 1800000 (30 minutes). Set 0 to disable the worker-owned timeout.",
              "type": "number"
            }
          },
          "type": "object"
        },
        "intents": {
          "$ref": "#/definitions/DiscordIntentsConfig",
          "description": "Privileged Gateway Intents (must also be enabled in Discord Developer Portal)."
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "maxLinesPerMessage": {
          "description": "Soft max line count per Discord message. Discord clients can clip/collapse very tall messages; splitting by lines keeps replies readable in-channel. Default: 17.",
          "type": "number"
        },
        "mediaMaxMb": {
          "type": "number"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "pluralkit": {
          "$ref": "#/definitions/DiscordPluralKitConfig",
          "description": "PluralKit identity resolution for proxied messages."
        },
        "proxy": {
          "description": "HTTP(S) proxy URL for Discord gateway WebSocket connections.",
          "type": "string"
        },
        "replyToMode": {
          "$ref": "#/definitions/ReplyToMode",
          "description": "Control reply threading when reply tags are present (off|first|all)."
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "retry": {
          "$ref": "#/definitions/OutboundRetryConfig",
          "description": "Retry policy for outbound Discord API calls."
        },
        "slashCommand": {
          "$ref": "#/definitions/DiscordSlashCommandConfig",
          "description": "Slash command configuration."
        },
        "status": {
          "description": "Bot status (online|dnd|idle|invisible). Defaults to online when presence is configured.",
          "enum": [
            "online",
            "dnd",
            "idle",
            "invisible"
          ],
          "type": "string"
        },
        "streamMode": {
          "deprecated": "Legacy key; migrated automatically to `streaming`.",
          "enum": [
            "partial",
            "block",
            "off"
          ],
          "type": "string"
        },
        "streaming": {
          "anyOf": [
            {
              "$ref": "#/definitions/DiscordStreamMode"
            },
            {
              "type": "boolean"
            }
          ],
          "description": "Live stream preview mode:\n- \"off\": disable preview updates\n- \"partial\": edit a single preview message\n- \"block\": stream in chunked preview updates\n- \"progress\": alias that maps to \"partial\" on Discord\n\nLegacy boolean values are still accepted and auto-migrated."
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 2000.",
          "type": "number"
        },
        "threadBindings": {
          "$ref": "#/definitions/DiscordThreadBindingsConfig",
          "description": "Thread binding lifecycle settings (focus/subagent thread sessions)."
        },
        "token": {
          "$ref": "#/definitions/SecretInput"
        },
        "ui": {
          "$ref": "#/definitions/DiscordUiConfig",
          "description": "Discord UI customization (components, modals, etc.)."
        },
        "voice": {
          "$ref": "#/definitions/DiscordVoiceConfig",
          "description": "Voice channel conversation settings."
        }
      },
      "type": "object"
    },
    "DiscordDmConfig": {
      "additionalProperties": false,
      "properties": {
        "allowFrom": {
          "description": "Allowlist for DM senders (ids or names).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "description": "If false, ignore all incoming Discord DMs. Default: true.",
          "type": "boolean"
        },
        "groupChannels": {
          "description": "Optional allowlist for group DM channels (ids or slugs).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "groupEnabled": {
          "description": "If true, allow group DMs (default: false).",
          "type": "boolean"
        },
        "policy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        }
      },
      "type": "object"
    },
    "DiscordExecApprovalConfig": {
      "additionalProperties": false,
      "properties": {
        "agentFilter": {
          "description": "Only forward approvals for these agent IDs. Omit = all agents.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "approvers": {
          "description": "Discord user IDs to receive approval prompts. Required if enabled.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cleanupAfterResolve": {
          "description": "Delete approval DMs after approval, denial, or timeout. Default: false.",
          "type": "boolean"
        },
        "enabled": {
          "description": "Enable exec approval forwarding to Discord DMs. Default: false.",
          "type": "boolean"
        },
        "sessionFilter": {
          "description": "Only forward approvals matching these session key patterns (substring or regex).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "target": {
          "description": "Where to send approval prompts. \"dm\" sends to approver DMs (default), \"channel\" sends to the originating Discord channel, \"both\" sends to both. When target is \"channel\" or \"both\", buttons are only usable by configured approvers; other users receive an ephemeral denial.",
          "enum": [
            "dm",
            "channel",
            "both"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "DiscordGuildChannelConfig": {
      "additionalProperties": false,
      "properties": {
        "allow": {
          "type": "boolean"
        },
        "enabled": {
          "description": "If false, disable the bot for this channel.",
          "type": "boolean"
        },
        "ignoreOtherMentions": {
          "description": "If true, drop messages that mention another user/role but not this one (not",
          "type": "boolean"
        },
        "includeThreadStarter": {
          "description": "If false, omit thread starter context for this channel (default: true).",
          "type": "boolean"
        },
        "requireMention": {
          "type": "boolean"
        },
        "roles": {
          "description": "Optional allowlist for channel senders by role ID.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "skills": {
          "description": "If specified, only load these skills for this channel. Omit = all skills; empty = no skills.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "systemPrompt": {
          "description": "Optional system prompt snippet for this channel.",
          "type": "string"
        },
        "tools": {
          "$ref": "#/definitions/GroupToolPolicyConfig",
          "description": "Optional tool policy overrides for this channel."
        },
        "toolsBySender": {
          "$ref": "#/definitions/GroupToolPolicyBySenderConfig"
        },
        "users": {
          "description": "Optional allowlist for channel senders (ids or names).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "DiscordGuildEntry": {
      "additionalProperties": false,
      "properties": {
        "channels": {
          "$ref": "#/definitions/Record%3Cstring%2CDiscordGuildChannelConfig%3E"
        },
        "ignoreOtherMentions": {
          "description": "If true, drop messages that mention another user/role but not this one (not",
          "type": "boolean"
        },
        "reactionNotifications": {
          "$ref": "#/definitions/DiscordReactionNotificationMode",
          "description": "Reaction notification mode (off|own|all|allowlist). Default: own."
        },
        "requireMention": {
          "type": "boolean"
        },
        "roles": {
          "description": "Optional allowlist for guild senders by role ID.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "slug": {
          "type": "string"
        },
        "tools": {
          "$ref": "#/definitions/GroupToolPolicyConfig",
          "description": "Optional tool policy overrides for this guild (used when channel override is missing)."
        },
        "toolsBySender": {
          "$ref": "#/definitions/GroupToolPolicyBySenderConfig"
        },
        "users": {
          "description": "Optional allowlist for guild senders (ids or names).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "DiscordIntentsConfig": {
      "additionalProperties": false,
      "properties": {
        "guildMembers": {
          "description": "Enable Guild Members privileged intent (requires Portal opt-in). Default: false.",
          "type": "boolean"
        },
        "presence": {
          "description": "Enable Guild Presences privileged intent (requires Portal opt-in). Default: false.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DiscordPluralKitConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "token": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "DiscordReactionNotificationMode": {
      "enum": [
        "off",
        "own",
        "all",
        "allowlist"
      ],
      "type": "string"
    },
    "DiscordSlashCommandConfig": {
      "additionalProperties": false,
      "properties": {
        "ephemeral": {
          "description": "Reply ephemerally (default: true).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DiscordStreamMode": {
      "enum": [
        "off",
        "partial",
        "block",
        "progress"
      ],
      "type": "string"
    },
    "DiscordThreadBindingsConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Enable Discord thread binding features (/focus, thread-bound delivery, and thread-bound subagent session flows). Overrides session.threadBindings.enabled when set.",
          "type": "boolean"
        },
        "idleHours": {
          "description": "Inactivity window for thread-bound sessions in hours. Session auto-unfocuses after this amount of idle time. Set to 0 to disable. Default: 24.",
          "type": "number"
        },
        "maxAgeHours": {
          "description": "Optional hard max age for thread-bound sessions in hours. Session auto-unfocuses once this age is reached even if active. Set to 0 to disable. Default: 0.",
          "type": "number"
        },
        "spawnAcpSessions": {
          "description": "Allow `/acp spawn` to auto-create + bind Discord threads for ACP sessions. Default: false (opt-in).",
          "type": "boolean"
        },
        "spawnSubagentSessions": {
          "description": "Allow `sessions_spawn({ thread: true })` to auto-create + bind Discord threads for subagent sessions. Default: false (opt-in).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DiscordUiComponentsConfig": {
      "additionalProperties": false,
      "properties": {
        "accentColor": {
          "description": "Accent color used by Discord component containers (hex).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "DiscordUiConfig": {
      "additionalProperties": false,
      "properties": {
        "components": {
          "$ref": "#/definitions/DiscordUiComponentsConfig"
        }
      },
      "type": "object"
    },
    "DiscordVoiceAutoJoinConfig": {
      "additionalProperties": false,
      "properties": {
        "channelId": {
          "description": "Voice channel ID to join.",
          "type": "string"
        },
        "guildId": {
          "description": "Guild ID that owns the voice channel.",
          "type": "string"
        }
      },
      "required": [
        "guildId",
        "channelId"
      ],
      "type": "object"
    },
    "DiscordVoiceConfig": {
      "additionalProperties": false,
      "properties": {
        "autoJoin": {
          "description": "Voice channels to auto-join on startup.",
          "items": {
            "$ref": "#/definitions/DiscordVoiceAutoJoinConfig"
          },
          "type": "array"
        },
        "daveEncryption": {
          "description": "Enable/disable DAVE end-to-end encryption (default: true; Discord may require this).",
          "type": "boolean"
        },
        "decryptionFailureTolerance": {
          "description": "Consecutive decrypt failures before DAVE session reinitialization (default: 24).",
          "type": "number"
        },
        "enabled": {
          "description": "Enable Discord voice channel conversations (default: true).",
          "type": "boolean"
        },
        "tts": {
          "$ref": "#/definitions/TtsConfig",
          "description": "Optional TTS overrides for Discord voice output."
        }
      },
      "type": "object"
    },
    "DiscoveryConfig": {
      "additionalProperties": false,
      "properties": {
        "mdns": {
          "$ref": "#/definitions/MdnsDiscoveryConfig"
        },
        "wideArea": {
          "$ref": "#/definitions/WideAreaDiscoveryConfig"
        }
      },
      "type": "object"
    },
    "DmConfig": {
      "additionalProperties": false,
      "properties": {
        "historyLimit": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "DmPolicy": {
      "enum": [
        "pairing",
        "allowlist",
        "open",
        "disabled"
      ],
      "type": "string"
    },
    "DmScope": {
      "enum": [
        "main",
        "per-peer",
        "per-channel-peer",
        "per-account-channel-peer"
      ],
      "type": "string"
    },
    "EnvSecretProviderConfig": {
      "additionalProperties": false,
      "properties": {
        "allowlist": {
          "description": "Optional env var allowlist (exact names).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "source": {
          "const": "env",
          "type": "string"
        }
      },
      "required": [
        "source"
      ],
      "type": "object"
    },
    "ExecApprovalForwardTarget": {
      "additionalProperties": false,
      "properties": {
        "accountId": {
          "description": "Optional account id for multi-account channels.",
          "type": "string"
        },
        "channel": {
          "description": "Channel id (e.g. \"discord\", \"slack\", or plugin channel id).",
          "type": "string"
        },
        "threadId": {
          "description": "Optional thread id to reply inside a thread.",
          "type": [
            "string",
            "number"
          ]
        },
        "to": {
          "description": "Destination id (channel id, user id, etc. depending on channel).",
          "type": "string"
        }
      },
      "required": [
        "channel",
        "to"
      ],
      "type": "object"
    },
    "ExecApprovalForwardingConfig": {
      "additionalProperties": false,
      "properties": {
        "agentFilter": {
          "description": "Only forward approvals for these agent IDs. Omit = all agents.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "description": "Enable forwarding exec approvals to chat channels. Default: false.",
          "type": "boolean"
        },
        "mode": {
          "$ref": "#/definitions/ExecApprovalForwardingMode",
          "description": "Delivery mode (session=origin chat, targets=config targets, both=both). Default: session."
        },
        "sessionFilter": {
          "description": "Only forward approvals matching these session key patterns (substring or regex).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "targets": {
          "description": "Explicit delivery targets (used when mode includes targets).",
          "items": {
            "$ref": "#/definitions/ExecApprovalForwardTarget"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ExecApprovalForwardingMode": {
      "enum": [
        "session",
        "targets",
        "both"
      ],
      "type": "string"
    },
    "ExecSecretProviderConfig": {
      "additionalProperties": false,
      "properties": {
        "allowInsecurePath": {
          "type": "boolean"
        },
        "allowSymlinkCommand": {
          "type": "boolean"
        },
        "args": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "command": {
          "type": "string"
        },
        "env": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E"
        },
        "jsonOnly": {
          "type": "boolean"
        },
        "maxOutputBytes": {
          "type": "number"
        },
        "noOutputTimeoutMs": {
          "type": "number"
        },
        "passEnv": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "source": {
          "const": "exec",
          "type": "string"
        },
        "timeoutMs": {
          "type": "number"
        },
        "trustedDirs": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "source",
        "command"
      ],
      "type": "object"
    },
    "ExecToolConfig": {
      "additionalProperties": false,
      "properties": {
        "applyPatch": {
          "additionalProperties": false,
          "description": "apply_patch subtool configuration (experimental).",
          "properties": {
            "allowModels": {
              "description": "Optional allowlist of model ids that can use apply_patch. Accepts either raw ids (e.g. \"gpt-5.2\") or full ids (e.g. \"openai/gpt-5.2\").",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "enabled": {
              "description": "Enable apply_patch for OpenAI models (default: false).",
              "type": "boolean"
            },
            "workspaceOnly": {
              "description": "Restrict apply_patch paths to the workspace directory. Default: true (safer; does not affect read/write/edit).",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "approvalRunningNoticeMs": {
          "description": "Emit a running notice (ms) when approval-backed exec runs long (default: 10000, 0 = off).",
          "type": "number"
        },
        "ask": {
          "description": "Exec ask mode (default: on-miss).",
          "enum": [
            "off",
            "on-miss",
            "always"
          ],
          "type": "string"
        },
        "backgroundMs": {
          "description": "Default time (ms) before an exec command auto-backgrounds.",
          "type": "number"
        },
        "cleanupMs": {
          "description": "How long to keep finished sessions in memory (ms).",
          "type": "number"
        },
        "host": {
          "description": "Exec host routing (default: sandbox).",
          "enum": [
            "sandbox",
            "gateway",
            "node"
          ],
          "type": "string"
        },
        "node": {
          "description": "Default node binding for exec.host=node (node id/name).",
          "type": "string"
        },
        "notifyOnExit": {
          "description": "Emit a system event and heartbeat when a backgrounded exec exits.",
          "type": "boolean"
        },
        "notifyOnExitEmptySuccess": {
          "description": "Also emit success exit notifications when a backgrounded exec has no output. Default false to reduce context noise.",
          "type": "boolean"
        },
        "pathPrepend": {
          "description": "Directories to prepend to PATH when running exec (gateway/sandbox).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "safeBinProfiles": {
          "$ref": "#/definitions/Record%3Cstring%2CSafeBinProfileFixture%3E",
          "description": "Optional custom safe-bin profiles for entries in tools.exec.safeBins."
        },
        "safeBinTrustedDirs": {
          "description": "Extra explicit directories trusted for safeBins path checks (never derived from PATH).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "safeBins": {
          "description": "Safe stdin-only binaries that can run without allowlist entries.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "security": {
          "description": "Exec security mode (default: deny).",
          "enum": [
            "deny",
            "allowlist",
            "full"
          ],
          "type": "string"
        },
        "timeoutSec": {
          "description": "Default timeout (seconds) before auto-killing exec commands.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "FileSecretProviderConfig": {
      "additionalProperties": false,
      "properties": {
        "maxBytes": {
          "type": "number"
        },
        "mode": {
          "$ref": "#/definitions/FileSecretProviderMode"
        },
        "path": {
          "type": "string"
        },
        "source": {
          "const": "file",
          "type": "string"
        },
        "timeoutMs": {
          "type": "number"
        }
      },
      "required": [
        "source",
        "path"
      ],
      "type": "object"
    },
    "FileSecretProviderMode": {
      "enum": [
        "singleValue",
        "json"
      ],
      "type": "string"
    },
    "FsToolsConfig": {
      "additionalProperties": false,
      "properties": {
        "workspaceOnly": {
          "description": "Restrict filesystem tools (read/write/edit/apply_patch) to the agent workspace directory. Default: false (unrestricted, matches legacy behavior).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "GatewayAuthConfig": {
      "additionalProperties": false,
      "properties": {
        "allowTailscale": {
          "description": "Allow Tailscale identity headers when serve mode is enabled.",
          "type": "boolean"
        },
        "mode": {
          "$ref": "#/definitions/GatewayAuthMode",
          "description": "Authentication mode for Gateway connections. Defaults to token when unset."
        },
        "password": {
          "$ref": "#/definitions/SecretInput",
          "description": "Shared password for password mode (consider env instead)."
        },
        "rateLimit": {
          "$ref": "#/definitions/GatewayAuthRateLimitConfig",
          "description": "Rate-limit configuration for failed authentication attempts."
        },
        "token": {
          "$ref": "#/definitions/SecretInput",
          "description": "Shared token for token mode (plaintext or SecretRef)."
        },
        "trustedProxy": {
          "$ref": "#/definitions/GatewayTrustedProxyConfig",
          "description": "Configuration for trusted-proxy auth mode. Required when mode is \"trusted-proxy\"."
        }
      },
      "type": "object"
    },
    "GatewayAuthMode": {
      "enum": [
        "none",
        "token",
        "password",
        "trusted-proxy"
      ],
      "type": "string"
    },
    "GatewayAuthRateLimitConfig": {
      "additionalProperties": false,
      "properties": {
        "exemptLoopback": {
          "default": true,
          "description": "Exempt localhost/loopback addresses from auth rate limiting.",
          "type": "boolean"
        },
        "lockoutMs": {
          "default": "300000 (5 min)",
          "description": "Lockout duration in milliseconds after the limit is exceeded.",
          "type": "number"
        },
        "maxAttempts": {
          "default": 10,
          "description": "Maximum failed attempts per IP before blocking.",
          "type": "number"
        },
        "windowMs": {
          "default": "60000 (1 min)",
          "description": "Sliding window duration in milliseconds.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "GatewayBindMode": {
      "enum": [
        "auto",
        "lan",
        "loopback",
        "custom",
        "tailnet"
      ],
      "type": "string"
    },
    "GatewayConfig": {
      "additionalProperties": false,
      "properties": {
        "allowRealIpFallback": {
          "description": "Allow `x-real-ip` as a fallback only when `x-forwarded-for` is missing. Default: false (safer fail-closed behavior).",
          "type": "boolean"
        },
        "auth": {
          "$ref": "#/definitions/GatewayAuthConfig"
        },
        "bind": {
          "$ref": "#/definitions/GatewayBindMode",
          "description": "Bind address policy for the Gateway WebSocket + Control UI HTTP server.\n- auto: Loopback (127.0.0.1) if available, else 0.0.0.0 (fallback to all interfaces)\n- lan: 0.0.0.0 (all interfaces, no fallback)\n- loopback: 127.0.0.1 (local-only)\n- tailnet: Tailnet IPv4 if available (100.64.0.0/10), else loopback\n- custom: User-specified IP, fallback to 0.0.0.0 if unavailable (requires customBindHost) Default: loopback (127.0.0.1)."
        },
        "channelHealthCheckMinutes": {
          "description": "Channel health monitor interval in minutes. Periodically checks channel health and restarts unhealthy channels. Set to 0 to disable. Default: 5.",
          "type": "number"
        },
        "controlUi": {
          "$ref": "#/definitions/GatewayControlUiConfig"
        },
        "customBindHost": {
          "description": "Custom IP address for bind=\"custom\" mode. Fallback: 0.0.0.0.",
          "type": "string"
        },
        "http": {
          "$ref": "#/definitions/GatewayHttpConfig"
        },
        "mode": {
          "description": "Explicit gateway mode. When set to \"remote\", local gateway start is disabled. When set to \"local\", the CLI may start the gateway locally.",
          "enum": [
            "local",
            "remote"
          ],
          "type": "string"
        },
        "nodes": {
          "$ref": "#/definitions/GatewayNodesConfig"
        },
        "port": {
          "description": "Single multiplexed port for Gateway WS + HTTP (default: 18789).",
          "type": "number"
        },
        "reload": {
          "$ref": "#/definitions/GatewayReloadConfig"
        },
        "remote": {
          "$ref": "#/definitions/GatewayRemoteConfig"
        },
        "tailscale": {
          "$ref": "#/definitions/GatewayTailscaleConfig"
        },
        "tls": {
          "$ref": "#/definitions/GatewayTlsConfig"
        },
        "tools": {
          "$ref": "#/definitions/GatewayToolsConfig",
          "description": "Tool access restrictions for HTTP /tools/invoke endpoint."
        },
        "trustedProxies": {
          "description": "IPs of trusted reverse proxies (e.g. Traefik, nginx). When a connection arrives from one of these IPs, the Gateway trusts `x-forwarded-for` to determine the client IP for local pairing and HTTP checks.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GatewayControlUiConfig": {
      "additionalProperties": false,
      "properties": {
        "allowInsecureAuth": {
          "description": "Insecure-auth toggle. Control UI still requires secure context + device identity unless dangerouslyDisableDeviceAuth is enabled.",
          "type": "boolean"
        },
        "allowedOrigins": {
          "description": "Allowed browser origins for Control UI/WebChat websocket connections.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "basePath": {
          "description": "Optional base path prefix for the Control UI (e.g. \"/openclaw\").",
          "type": "string"
        },
        "dangerouslyAllowHostHeaderOriginFallback": {
          "description": "DANGEROUS: Keep Host-header origin fallback behavior. Supported long-term for deployments that intentionally rely on this policy.",
          "type": "boolean"
        },
        "dangerouslyDisableDeviceAuth": {
          "description": "DANGEROUS: Disable device identity checks for the Control UI (default: false).",
          "type": "boolean"
        },
        "enabled": {
          "description": "If false, the Gateway will not serve the Control UI (default /).",
          "type": "boolean"
        },
        "root": {
          "description": "Optional filesystem root for Control UI assets (defaults to dist/control-ui).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "GatewayHttpChatCompletionsConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "If false, the Gateway will not serve `POST /v1/chat/completions`. Default: false when absent.",
          "type": "boolean"
        },
        "images": {
          "$ref": "#/definitions/GatewayHttpChatCompletionsImagesConfig",
          "description": "Image input controls for `image_url` parts."
        },
        "maxBodyBytes": {
          "description": "Max request body size in bytes for `/v1/chat/completions`. Default: 20MB.",
          "type": "number"
        },
        "maxImageParts": {
          "description": "Max number of `image_url` parts processed from the latest user message. Default: 8.",
          "type": "number"
        },
        "maxTotalImageBytes": {
          "description": "Max cumulative decoded image bytes for all `image_url` parts in one request. Default: 20MB.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "GatewayHttpChatCompletionsImagesConfig": {
      "additionalProperties": false,
      "properties": {
        "allowUrl": {
          "description": "Allow URL fetches for `image_url` parts. Default: false.",
          "type": "boolean"
        },
        "allowedMimes": {
          "description": "Allowed MIME types (case-insensitive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxBytes": {
          "description": "Max bytes per image. Default: 10MB.",
          "type": "number"
        },
        "maxRedirects": {
          "description": "Max redirects when fetching a URL. Default: 3.",
          "type": "number"
        },
        "timeoutMs": {
          "description": "Fetch timeout in ms. Default: 10s.",
          "type": "number"
        },
        "urlAllowlist": {
          "description": "Optional hostname allowlist for URL fetches. Supports exact hosts and `*.example.com` wildcards.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GatewayHttpConfig": {
      "additionalProperties": false,
      "properties": {
        "endpoints": {
          "$ref": "#/definitions/GatewayHttpEndpointsConfig"
        },
        "securityHeaders": {
          "$ref": "#/definitions/GatewayHttpSecurityHeadersConfig"
        }
      },
      "type": "object"
    },
    "GatewayHttpEndpointsConfig": {
      "additionalProperties": false,
      "properties": {
        "chatCompletions": {
          "$ref": "#/definitions/GatewayHttpChatCompletionsConfig"
        },
        "responses": {
          "$ref": "#/definitions/GatewayHttpResponsesConfig"
        }
      },
      "type": "object"
    },
    "GatewayHttpResponsesConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "If false, the Gateway will not serve `POST /v1/responses` (OpenResponses API). Default: false when absent.",
          "type": "boolean"
        },
        "files": {
          "$ref": "#/definitions/GatewayHttpResponsesFilesConfig",
          "description": "File inputs (input_file)."
        },
        "images": {
          "$ref": "#/definitions/GatewayHttpResponsesImagesConfig",
          "description": "Image inputs (input_image)."
        },
        "maxBodyBytes": {
          "description": "Max request body size in bytes for `/v1/responses`. Default: 20MB.",
          "type": "number"
        },
        "maxUrlParts": {
          "description": "Max number of URL-based `input_file` + `input_image` parts per request. Default: 8.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "GatewayHttpResponsesFilesConfig": {
      "additionalProperties": false,
      "properties": {
        "allowUrl": {
          "description": "Allow URL fetches for input_file. Default: true.",
          "type": "boolean"
        },
        "allowedMimes": {
          "description": "Allowed MIME types (case-insensitive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxBytes": {
          "description": "Max bytes per file. Default: 5MB.",
          "type": "number"
        },
        "maxChars": {
          "description": "Max decoded characters per file. Default: 200k.",
          "type": "number"
        },
        "maxRedirects": {
          "description": "Max redirects when fetching a URL. Default: 3.",
          "type": "number"
        },
        "pdf": {
          "$ref": "#/definitions/GatewayHttpResponsesPdfConfig",
          "description": "PDF handling (application/pdf)."
        },
        "timeoutMs": {
          "description": "Fetch timeout in ms. Default: 10s.",
          "type": "number"
        },
        "urlAllowlist": {
          "description": "Optional hostname allowlist for URL fetches. Supports exact hosts and `*.example.com` wildcards.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GatewayHttpResponsesImagesConfig": {
      "additionalProperties": false,
      "properties": {
        "allowUrl": {
          "description": "Allow URL fetches for input_image. Default: true.",
          "type": "boolean"
        },
        "allowedMimes": {
          "description": "Allowed MIME types (case-insensitive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxBytes": {
          "description": "Max bytes per image. Default: 10MB.",
          "type": "number"
        },
        "maxRedirects": {
          "description": "Max redirects when fetching a URL. Default: 3.",
          "type": "number"
        },
        "timeoutMs": {
          "description": "Fetch timeout in ms. Default: 10s.",
          "type": "number"
        },
        "urlAllowlist": {
          "description": "Optional hostname allowlist for URL fetches. Supports exact hosts and `*.example.com` wildcards.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GatewayHttpResponsesPdfConfig": {
      "additionalProperties": false,
      "properties": {
        "maxPages": {
          "description": "Max pages to parse/render. Default: 4.",
          "type": "number"
        },
        "maxPixels": {
          "description": "Max pixels per rendered page. Default: 4M.",
          "type": "number"
        },
        "minTextChars": {
          "description": "Minimum extracted text length to skip rasterization. Default: 200 chars.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "GatewayHttpSecurityHeadersConfig": {
      "additionalProperties": false,
      "properties": {
        "strictTransportSecurity": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "const": false,
              "type": "boolean"
            }
          ],
          "description": "Value for the Strict-Transport-Security response header. Set to false to disable explicitly.\n\nExample: \"max-age=31536000; includeSubDomains\""
        }
      },
      "type": "object"
    },
    "GatewayNodesConfig": {
      "additionalProperties": false,
      "properties": {
        "allowCommands": {
          "description": "Additional node.invoke commands to allow on the gateway.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "browser": {
          "additionalProperties": false,
          "description": "Browser routing policy for node-hosted browser proxies.",
          "properties": {
            "mode": {
              "description": "Routing mode (default: auto).",
              "enum": [
                "auto",
                "manual",
                "off"
              ],
              "type": "string"
            },
            "node": {
              "description": "Pin to a specific node id/name (optional).",
              "type": "string"
            }
          },
          "type": "object"
        },
        "denyCommands": {
          "description": "Commands to deny even if they appear in the defaults or node claims.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GatewayReloadConfig": {
      "additionalProperties": false,
      "properties": {
        "debounceMs": {
          "description": "Debounce window for config reloads (ms). Default: 300.",
          "type": "number"
        },
        "mode": {
          "$ref": "#/definitions/GatewayReloadMode",
          "description": "Reload strategy for config changes (default: hybrid)."
        }
      },
      "type": "object"
    },
    "GatewayReloadMode": {
      "enum": [
        "off",
        "restart",
        "hot",
        "hybrid"
      ],
      "type": "string"
    },
    "GatewayRemoteConfig": {
      "additionalProperties": false,
      "properties": {
        "password": {
          "$ref": "#/definitions/SecretInput",
          "description": "Password for remote auth (when the gateway requires password auth)."
        },
        "sshIdentity": {
          "description": "SSH identity file path for tunneling remote Gateway.",
          "type": "string"
        },
        "sshTarget": {
          "description": "SSH target for tunneling remote Gateway (user@host).",
          "type": "string"
        },
        "tlsFingerprint": {
          "description": "Expected TLS certificate fingerprint (sha256) for remote gateways.",
          "type": "string"
        },
        "token": {
          "$ref": "#/definitions/SecretInput",
          "description": "Token for remote auth (when the gateway requires token auth)."
        },
        "transport": {
          "description": "Transport for macOS remote connections (ssh tunnel or direct WS).",
          "enum": [
            "ssh",
            "direct"
          ],
          "type": "string"
        },
        "url": {
          "description": "Remote Gateway WebSocket URL (ws:// or wss://).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "GatewayTailscaleConfig": {
      "additionalProperties": false,
      "properties": {
        "mode": {
          "$ref": "#/definitions/GatewayTailscaleMode",
          "description": "Tailscale exposure mode for the Gateway control UI."
        },
        "resetOnExit": {
          "description": "Reset serve/funnel configuration on shutdown.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "GatewayTailscaleMode": {
      "enum": [
        "off",
        "serve",
        "funnel"
      ],
      "type": "string"
    },
    "GatewayTlsConfig": {
      "additionalProperties": false,
      "properties": {
        "autoGenerate": {
          "description": "Auto-generate a self-signed cert if cert/key are missing (default: true).",
          "type": "boolean"
        },
        "caPath": {
          "description": "Optional PEM CA bundle for TLS clients (mTLS or custom roots).",
          "type": "string"
        },
        "certPath": {
          "description": "PEM certificate path for the gateway server.",
          "type": "string"
        },
        "enabled": {
          "description": "Enable TLS for the gateway server.",
          "type": "boolean"
        },
        "keyPath": {
          "description": "PEM private key path for the gateway server.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "GatewayToolsConfig": {
      "additionalProperties": false,
      "properties": {
        "allow": {
          "description": "Tools to explicitly allow (removes from default deny list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "deny": {
          "description": "Tools to deny via gateway HTTP /tools/invoke (extends defaults).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GatewayTrustedProxyConfig": {
      "additionalProperties": false,
      "description": "Configuration for trusted reverse proxy authentication. Used when Clawdbot runs behind an identity-aware proxy (Pomerium, Caddy + OAuth, etc.) that handles authentication and passes user identity via headers.",
      "properties": {
        "allowUsers": {
          "description": "Optional allowlist of user identities that can access the gateway. If empty or omitted, all authenticated users from the proxy are allowed. Example: [\"nick@example.com\", \"admin@company.org\"]",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "requiredHeaders": {
          "description": "Additional headers that MUST be present for the request to be trusted. Use this to verify the request actually came through the proxy. Example: [\"x-forwarded-proto\", \"x-forwarded-host\"]",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "userHeader": {
          "description": "Header name containing the authenticated user identity (required). Common values: \"x-forwarded-user\", \"x-remote-user\", \"x-pomerium-claim-email\"",
          "type": "string"
        }
      },
      "required": [
        "userHeader"
      ],
      "type": "object"
    },
    "GoogleChatAccountConfig": {
      "additionalProperties": false,
      "properties": {
        "actions": {
          "$ref": "#/definitions/GoogleChatActionConfig",
          "description": "Per-action tool gating (default: true for all)."
        },
        "allowBots": {
          "description": "Allow bot-authored messages to trigger replies (default: false).",
          "type": "boolean"
        },
        "audience": {
          "description": "Audience value (app URL or project number).",
          "type": "string"
        },
        "audienceType": {
          "description": "Webhook audience type (app-url or project-number).",
          "enum": [
            "app-url",
            "project-number"
          ],
          "type": "string"
        },
        "blockStreaming": {
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "botUser": {
          "description": "Optional bot user resource name (users/...).",
          "type": "string"
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "dangerouslyAllowNameMatching": {
          "description": "Break-glass override: allow mutable principal matching (raw email entries) in allowlists. Default behavior is ID-only matching.",
          "type": "boolean"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dm": {
          "$ref": "#/definitions/GoogleChatDmConfig"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user id."
        },
        "enabled": {
          "description": "If false, do not start this Google Chat account. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for space senders (user ids or emails).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how space messages are handled:\n- \"open\": spaces bypass allowlists; mention-gating applies\n- \"disabled\": block all space messages\n- \"allowlist\": only allow spaces present in channels.googlechat.groups"
        },
        "groups": {
          "$ref": "#/definitions/Record%3Cstring%2CGoogleChatGroupConfig%3E",
          "description": "Per-space configuration keyed by space id or name."
        },
        "historyLimit": {
          "description": "Max space messages to keep as history context (0 disables).",
          "type": "number"
        },
        "mediaMaxMb": {
          "type": "number"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "replyToMode": {
          "$ref": "#/definitions/ReplyToMode",
          "description": "Control reply threading when reply tags are present (off|first|all)."
        },
        "requireMention": {
          "description": "Default mention requirement for space messages (default: true).",
          "type": "boolean"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "serviceAccount": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/Record%3Cstring%2Cunknown%3E"
            },
            {
              "$ref": "#/definitions/SecretRef"
            }
          ],
          "description": "Service account JSON (inline string, object, or secret reference)."
        },
        "serviceAccountFile": {
          "description": "Service account JSON file path.",
          "type": "string"
        },
        "serviceAccountRef": {
          "$ref": "#/definitions/SecretRef",
          "description": "Explicit secret reference for service account JSON."
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 4000.",
          "type": "number"
        },
        "typingIndicator": {
          "description": "Typing indicator mode (default: \"message\").\n- \"none\": No indicator\n- \"message\": Send \"_<name> is typing..._\" then edit with response\n- \"reaction\": React with 👀 to user message, remove on reply   NOTE: Reaction mode requires user OAuth (not supported with service account auth).   If configured, falls back to message mode with a warning.",
          "enum": [
            "none",
            "message",
            "reaction"
          ],
          "type": "string"
        },
        "webhookPath": {
          "description": "Google Chat webhook path (default: /googlechat).",
          "type": "string"
        },
        "webhookUrl": {
          "description": "Google Chat webhook URL (used to derive the path).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "GoogleChatActionConfig": {
      "additionalProperties": false,
      "properties": {
        "reactions": {
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "GoogleChatConfig": {
      "additionalProperties": false,
      "properties": {
        "accounts": {
          "$ref": "#/definitions/Record%3Cstring%2CGoogleChatAccountConfig%3E",
          "description": "Optional per-account Google Chat configuration (multi-account)."
        },
        "actions": {
          "$ref": "#/definitions/GoogleChatActionConfig",
          "description": "Per-action tool gating (default: true for all)."
        },
        "allowBots": {
          "description": "Allow bot-authored messages to trigger replies (default: false).",
          "type": "boolean"
        },
        "audience": {
          "description": "Audience value (app URL or project number).",
          "type": "string"
        },
        "audienceType": {
          "description": "Webhook audience type (app-url or project-number).",
          "enum": [
            "app-url",
            "project-number"
          ],
          "type": "string"
        },
        "blockStreaming": {
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "botUser": {
          "description": "Optional bot user resource name (users/...).",
          "type": "string"
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "dangerouslyAllowNameMatching": {
          "description": "Break-glass override: allow mutable principal matching (raw email entries) in allowlists. Default behavior is ID-only matching.",
          "type": "boolean"
        },
        "defaultAccount": {
          "description": "Optional default account id when multiple accounts are configured.",
          "type": "string"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dm": {
          "$ref": "#/definitions/GoogleChatDmConfig"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user id."
        },
        "enabled": {
          "description": "If false, do not start this Google Chat account. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for space senders (user ids or emails).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how space messages are handled:\n- \"open\": spaces bypass allowlists; mention-gating applies\n- \"disabled\": block all space messages\n- \"allowlist\": only allow spaces present in channels.googlechat.groups"
        },
        "groups": {
          "$ref": "#/definitions/Record%3Cstring%2CGoogleChatGroupConfig%3E",
          "description": "Per-space configuration keyed by space id or name."
        },
        "historyLimit": {
          "description": "Max space messages to keep as history context (0 disables).",
          "type": "number"
        },
        "mediaMaxMb": {
          "type": "number"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "replyToMode": {
          "$ref": "#/definitions/ReplyToMode",
          "description": "Control reply threading when reply tags are present (off|first|all)."
        },
        "requireMention": {
          "description": "Default mention requirement for space messages (default: true).",
          "type": "boolean"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "serviceAccount": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/Record%3Cstring%2Cunknown%3E"
            },
            {
              "$ref": "#/definitions/SecretRef"
            }
          ],
          "description": "Service account JSON (inline string, object, or secret reference)."
        },
        "serviceAccountFile": {
          "description": "Service account JSON file path.",
          "type": "string"
        },
        "serviceAccountRef": {
          "$ref": "#/definitions/SecretRef",
          "description": "Explicit secret reference for service account JSON."
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 4000.",
          "type": "number"
        },
        "typingIndicator": {
          "description": "Typing indicator mode (default: \"message\").\n- \"none\": No indicator\n- \"message\": Send \"_<name> is typing..._\" then edit with response\n- \"reaction\": React with 👀 to user message, remove on reply   NOTE: Reaction mode requires user OAuth (not supported with service account auth).   If configured, falls back to message mode with a warning.",
          "enum": [
            "none",
            "message",
            "reaction"
          ],
          "type": "string"
        },
        "webhookPath": {
          "description": "Google Chat webhook path (default: /googlechat).",
          "type": "string"
        },
        "webhookUrl": {
          "description": "Google Chat webhook URL (used to derive the path).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "GoogleChatDmConfig": {
      "additionalProperties": false,
      "properties": {
        "allowFrom": {
          "description": "Allowlist for DM senders (user ids or emails).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "enabled": {
          "description": "If false, ignore all incoming Google Chat DMs. Default: true.",
          "type": "boolean"
        },
        "policy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        }
      },
      "type": "object"
    },
    "GoogleChatGroupConfig": {
      "additionalProperties": false,
      "properties": {
        "allow": {
          "description": "Legacy allow toggle; prefer enabled.",
          "type": "boolean"
        },
        "enabled": {
          "description": "If false, disable the bot in this space. (Alias for allow: false.)",
          "type": "boolean"
        },
        "requireMention": {
          "description": "Require mentioning the bot to trigger replies.",
          "type": "boolean"
        },
        "systemPrompt": {
          "description": "Optional system prompt for this space.",
          "type": "string"
        },
        "users": {
          "description": "Allowlist of users that can invoke the bot in this space.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GroupChatConfig": {
      "additionalProperties": false,
      "properties": {
        "historyLimit": {
          "type": "number"
        },
        "mentionPatterns": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GroupPolicy": {
      "enum": [
        "open",
        "disabled",
        "allowlist"
      ],
      "type": "string"
    },
    "GroupToolPolicyBySenderConfig": {
      "$ref": "#/definitions/Record%3Cstring%2CGroupToolPolicyConfig%3E",
      "description": "Per-sender overrides.\n\nPrefer explicit key prefixes:\n- id:<senderId>\n- e164:<phone>\n- username:<handle>\n- name:<display-name>\n- * (wildcard)\n\nLegacy unprefixed keys are supported for backward compatibility and are matched as senderId only."
    },
    "GroupToolPolicyConfig": {
      "additionalProperties": false,
      "properties": {
        "allow": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alsoAllow": {
          "description": "Additional allowlist entries merged into allow.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "deny": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "HookConfig": {
      "additionalProperties": {},
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "env": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E"
        }
      },
      "type": "object"
    },
    "HookInstallRecord": {
      "additionalProperties": false,
      "properties": {
        "hooks": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "installPath": {
          "type": "string"
        },
        "installedAt": {
          "type": "string"
        },
        "integrity": {
          "type": "string"
        },
        "resolvedAt": {
          "type": "string"
        },
        "resolvedName": {
          "type": "string"
        },
        "resolvedSpec": {
          "type": "string"
        },
        "resolvedVersion": {
          "type": "string"
        },
        "shasum": {
          "type": "string"
        },
        "source": {
          "enum": [
            "npm",
            "archive",
            "path"
          ],
          "type": "string"
        },
        "sourcePath": {
          "type": "string"
        },
        "spec": {
          "type": "string"
        },
        "version": {
          "type": "string"
        }
      },
      "required": [
        "source"
      ],
      "type": "object"
    },
    "HookMappingConfig": {
      "additionalProperties": false,
      "properties": {
        "action": {
          "enum": [
            "wake",
            "agent"
          ],
          "type": "string"
        },
        "agentId": {
          "description": "Route this hook to a specific agent (unknown ids fall back to the default agent).",
          "type": "string"
        },
        "allowUnsafeExternalContent": {
          "description": "DANGEROUS: Disable external content safety wrapping for this hook.",
          "type": "boolean"
        },
        "channel": {
          "enum": [
            "last",
            "whatsapp",
            "telegram",
            "discord",
            "irc",
            "googlechat",
            "slack",
            "signal",
            "imessage",
            "msteams"
          ],
          "type": "string"
        },
        "deliver": {
          "type": "boolean"
        },
        "id": {
          "type": "string"
        },
        "match": {
          "$ref": "#/definitions/HookMappingMatch"
        },
        "messageTemplate": {
          "type": "string"
        },
        "model": {
          "description": "Override model for this hook (provider/model or alias).",
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "sessionKey": {
          "type": "string"
        },
        "textTemplate": {
          "type": "string"
        },
        "thinking": {
          "type": "string"
        },
        "timeoutSeconds": {
          "type": "number"
        },
        "to": {
          "type": "string"
        },
        "transform": {
          "$ref": "#/definitions/HookMappingTransform"
        },
        "wakeMode": {
          "enum": [
            "now",
            "next-heartbeat"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "HookMappingMatch": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "type": "string"
        },
        "source": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "HookMappingTransform": {
      "additionalProperties": false,
      "properties": {
        "export": {
          "type": "string"
        },
        "module": {
          "type": "string"
        }
      },
      "required": [
        "module"
      ],
      "type": "object"
    },
    "HooksConfig": {
      "additionalProperties": false,
      "properties": {
        "allowRequestSessionKey": {
          "description": "Allow `sessionKey` from external `/hooks/agent` request payloads. Default: false.",
          "type": "boolean"
        },
        "allowedAgentIds": {
          "description": "Restrict explicit hook `agentId` routing to these agent ids. Omit or include `*` to allow any agent. Set `[]` to deny all explicit `agentId` routing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "allowedSessionKeyPrefixes": {
          "description": "Optional allowlist for explicit session keys (request + mapping). Example: [\"hook:\"]. Empty/omitted means no prefix restriction.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "defaultSessionKey": {
          "description": "Default session key used for hook agent runs when no request/mapping session key is used. If omitted, OpenClaw generates `hook:<uuid>` per request.",
          "type": "string"
        },
        "enabled": {
          "type": "boolean"
        },
        "gmail": {
          "$ref": "#/definitions/HooksGmailConfig"
        },
        "internal": {
          "$ref": "#/definitions/InternalHooksConfig",
          "description": "Internal agent event hooks"
        },
        "mappings": {
          "items": {
            "$ref": "#/definitions/HookMappingConfig"
          },
          "type": "array"
        },
        "maxBodyBytes": {
          "type": "number"
        },
        "path": {
          "type": "string"
        },
        "presets": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "token": {
          "type": "string"
        },
        "transformsDir": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "HooksGmailConfig": {
      "additionalProperties": false,
      "properties": {
        "account": {
          "type": "string"
        },
        "allowUnsafeExternalContent": {
          "description": "DANGEROUS: Disable external content safety wrapping for Gmail hooks.",
          "type": "boolean"
        },
        "hookUrl": {
          "type": "string"
        },
        "includeBody": {
          "type": "boolean"
        },
        "label": {
          "type": "string"
        },
        "maxBytes": {
          "type": "number"
        },
        "model": {
          "description": "Optional model override for Gmail hook processing (provider/model or alias).",
          "type": "string"
        },
        "pushToken": {
          "type": "string"
        },
        "renewEveryMinutes": {
          "type": "number"
        },
        "serve": {
          "additionalProperties": false,
          "properties": {
            "bind": {
              "type": "string"
            },
            "path": {
              "type": "string"
            },
            "port": {
              "type": "number"
            }
          },
          "type": "object"
        },
        "subscription": {
          "type": "string"
        },
        "tailscale": {
          "additionalProperties": false,
          "properties": {
            "mode": {
              "$ref": "#/definitions/HooksGmailTailscaleMode"
            },
            "path": {
              "type": "string"
            },
            "target": {
              "description": "Optional tailscale serve/funnel target (port, host:port, or full URL).",
              "type": "string"
            }
          },
          "type": "object"
        },
        "thinking": {
          "description": "Optional thinking level override for Gmail hook processing.",
          "enum": [
            "off",
            "minimal",
            "low",
            "medium",
            "high"
          ],
          "type": "string"
        },
        "topic": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "HooksGmailTailscaleMode": {
      "enum": [
        "off",
        "serve",
        "funnel"
      ],
      "type": "string"
    },
    "HumanDelayConfig": {
      "additionalProperties": false,
      "properties": {
        "maxMs": {
          "description": "Maximum delay in milliseconds (default: 2500).",
          "type": "number"
        },
        "minMs": {
          "description": "Minimum delay in milliseconds (default: 800).",
          "type": "number"
        },
        "mode": {
          "description": "Delay style for block replies (off|natural|custom).",
          "enum": [
            "off",
            "natural",
            "custom"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "IMessageAccountConfig": {
      "additionalProperties": false,
      "properties": {
        "allowFrom": {
          "description": "Optional allowlist for inbound handles or chat_id targets.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "attachmentRoots": {
          "description": "Allowed local iMessage attachment roots (supports single-segment `*` wildcards).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blockStreaming": {
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "cliPath": {
          "description": "imsg CLI binary path (default: imsg).",
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "dbPath": {
          "description": "Optional Messages db path override.",
          "type": "string"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "enabled": {
          "description": "If false, do not start this iMessage account. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for group senders or chat_id targets.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how group messages are handled:\n- \"open\": groups bypass allowFrom; mention-gating applies\n- \"disabled\": block all group messages entirely\n- \"allowlist\": only allow group messages from senders in groupAllowFrom/allowFrom"
        },
        "groups": {
          "$ref": "#/definitions/Record%3Cstring%2Cstructure-d.ts-3185-3321-d.ts-3170-3322-d.ts-3157-3323-d.ts-367-3534-d.ts-331-3535-d.ts-0-3827%3E"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max group messages to keep as history context (0 disables).",
          "type": "number"
        },
        "includeAttachments": {
          "description": "Include attachments + reactions in watch payloads.",
          "type": "boolean"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "description": "Max outbound media size in MB.",
          "type": "number"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "probeTimeoutMs": {
          "description": "Timeout for probe/RPC operations in milliseconds (default: 10000).",
          "type": "number"
        },
        "region": {
          "description": "Optional default region (used when sending SMS).",
          "type": "string"
        },
        "remoteAttachmentRoots": {
          "description": "Allowed remote iMessage attachment roots for SCP fetches (supports `*`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "remoteHost": {
          "description": "Remote SSH host token for SCP attachment fetches (`host` or `user@host`).",
          "type": "string"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "service": {
          "description": "Optional default send service (imessage|sms|auto).",
          "enum": [
            "imessage",
            "sms",
            "auto"
          ],
          "type": "string"
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 4000.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "IMessageConfig": {
      "additionalProperties": false,
      "properties": {
        "accounts": {
          "$ref": "#/definitions/Record%3Cstring%2CIMessageAccountConfig%3E",
          "description": "Optional per-account iMessage configuration (multi-account)."
        },
        "allowFrom": {
          "description": "Optional allowlist for inbound handles or chat_id targets.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "attachmentRoots": {
          "description": "Allowed local iMessage attachment roots (supports single-segment `*` wildcards).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blockStreaming": {
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "cliPath": {
          "description": "imsg CLI binary path (default: imsg).",
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "dbPath": {
          "description": "Optional Messages db path override.",
          "type": "string"
        },
        "defaultAccount": {
          "description": "Optional default account id when multiple accounts are configured.",
          "type": "string"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "enabled": {
          "description": "If false, do not start this iMessage account. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for group senders or chat_id targets.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how group messages are handled:\n- \"open\": groups bypass allowFrom; mention-gating applies\n- \"disabled\": block all group messages entirely\n- \"allowlist\": only allow group messages from senders in groupAllowFrom/allowFrom"
        },
        "groups": {
          "$ref": "#/definitions/Record%3Cstring%2Cstructure-d.ts-3185-3321-d.ts-3170-3322-d.ts-3157-3323-d.ts-367-3534-d.ts-331-3535-d.ts-0-3827%3E"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max group messages to keep as history context (0 disables).",
          "type": "number"
        },
        "includeAttachments": {
          "description": "Include attachments + reactions in watch payloads.",
          "type": "boolean"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "description": "Max outbound media size in MB.",
          "type": "number"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "probeTimeoutMs": {
          "description": "Timeout for probe/RPC operations in milliseconds (default: 10000).",
          "type": "number"
        },
        "region": {
          "description": "Optional default region (used when sending SMS).",
          "type": "string"
        },
        "remoteAttachmentRoots": {
          "description": "Allowed remote iMessage attachment roots for SCP fetches (supports `*`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "remoteHost": {
          "description": "Remote SSH host token for SCP attachment fetches (`host` or `user@host`).",
          "type": "string"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "service": {
          "description": "Optional default send service (imessage|sms|auto).",
          "enum": [
            "imessage",
            "sms",
            "auto"
          ],
          "type": "string"
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 4000.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "IdentityConfig": {
      "additionalProperties": false,
      "properties": {
        "avatar": {
          "description": "Avatar image: workspace-relative path, http(s) URL, or data URI.",
          "type": "string"
        },
        "emoji": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "theme": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "InboundDebounceByProvider": {
      "$ref": "#/definitions/Record%3Cstring%2Cnumber%3E"
    },
    "InboundDebounceConfig": {
      "additionalProperties": false,
      "properties": {
        "byChannel": {
          "$ref": "#/definitions/InboundDebounceByProvider"
        },
        "debounceMs": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "InstallRecordBase": {
      "additionalProperties": false,
      "properties": {
        "installPath": {
          "type": "string"
        },
        "installedAt": {
          "type": "string"
        },
        "integrity": {
          "type": "string"
        },
        "resolvedAt": {
          "type": "string"
        },
        "resolvedName": {
          "type": "string"
        },
        "resolvedSpec": {
          "type": "string"
        },
        "resolvedVersion": {
          "type": "string"
        },
        "shasum": {
          "type": "string"
        },
        "source": {
          "enum": [
            "npm",
            "archive",
            "path"
          ],
          "type": "string"
        },
        "sourcePath": {
          "type": "string"
        },
        "spec": {
          "type": "string"
        },
        "version": {
          "type": "string"
        }
      },
      "required": [
        "source"
      ],
      "type": "object"
    },
    "InternalHookHandlerConfig": {
      "additionalProperties": false,
      "properties": {
        "event": {
          "description": "Event key to listen for (e.g., 'command:new', 'message:received', 'message:transcribed', 'session:start')",
          "type": "string"
        },
        "export": {
          "description": "Export name from module (default: 'default')",
          "type": "string"
        },
        "module": {
          "description": "Path to handler module (workspace-relative)",
          "type": "string"
        }
      },
      "required": [
        "event",
        "module"
      ],
      "type": "object"
    },
    "InternalHooksConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Enable hooks system",
          "type": "boolean"
        },
        "entries": {
          "$ref": "#/definitions/Record%3Cstring%2CHookConfig%3E",
          "description": "Per-hook configuration overrides"
        },
        "handlers": {
          "description": "Legacy: List of internal hook handlers to register (still supported)",
          "items": {
            "$ref": "#/definitions/InternalHookHandlerConfig"
          },
          "type": "array"
        },
        "installs": {
          "$ref": "#/definitions/Record%3Cstring%2CHookInstallRecord%3E",
          "description": "Install records for hook packs or hooks"
        },
        "load": {
          "additionalProperties": false,
          "description": "Load configuration",
          "properties": {
            "extraDirs": {
              "description": "Additional hook directories to scan",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "IrcAccountConfig": {
      "additionalProperties": false,
      "properties": {
        "allowFrom": {
          "description": "Optional allowlist for inbound DM senders.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "blockStreaming": {
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "channels": {
          "description": "Auto-join channel list at connect (example: [\"#openclaw\"]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by sender ID."
        },
        "enabled": {
          "description": "If false, do not start this account. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for group/channel senders.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Group/channel message handling policy."
        },
        "groups": {
          "$ref": "#/definitions/Record%3Cstring%2Cstructure--1718-1983--1703-1984--1690-1985--244-2090--213-2090--182-2091--0-2363%3E"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max group/channel messages to keep as history context (0 disables).",
          "type": "number"
        },
        "host": {
          "description": "IRC server hostname (example: irc.libera.chat).",
          "type": "string"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "description": "Max outbound media size in MB.",
          "type": "number"
        },
        "mentionPatterns": {
          "description": "Optional mention patterns specific to IRC channel messages.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "nick": {
          "description": "IRC nickname to identify this bot.",
          "type": "string"
        },
        "nickserv": {
          "additionalProperties": false,
          "description": "Optional NickServ identify/register settings.",
          "properties": {
            "enabled": {
              "description": "Enable NickServ identify/register after connect (default: enabled when password is set).",
              "type": "boolean"
            },
            "password": {
              "description": "NickServ password (sensitive).",
              "type": "string"
            },
            "passwordFile": {
              "description": "Optional file path containing NickServ password.",
              "type": "string"
            },
            "register": {
              "description": "If true, send NickServ REGISTER on connect.",
              "type": "boolean"
            },
            "registerEmail": {
              "description": "Email used with NickServ REGISTER.",
              "type": "string"
            },
            "service": {
              "description": "NickServ service nick (default: NickServ).",
              "type": "string"
            }
          },
          "type": "object"
        },
        "password": {
          "description": "Optional IRC server password (sensitive).",
          "type": "string"
        },
        "passwordFile": {
          "description": "Optional file path containing IRC server password.",
          "type": "string"
        },
        "port": {
          "description": "IRC server port (default: 6697 with TLS, otherwise 6667).",
          "type": "number"
        },
        "realname": {
          "description": "IRC USER field realname (default: OpenClaw).",
          "type": "string"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars).",
          "type": "number"
        },
        "tls": {
          "description": "Use TLS for IRC connection (default: true).",
          "type": "boolean"
        },
        "username": {
          "description": "IRC USER field username (defaults to nick).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "IrcConfig": {
      "additionalProperties": false,
      "properties": {
        "accounts": {
          "$ref": "#/definitions/Record%3Cstring%2CIrcAccountConfig%3E",
          "description": "Optional per-account IRC configuration (multi-account)."
        },
        "allowFrom": {
          "description": "Optional allowlist for inbound DM senders.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "blockStreaming": {
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "channels": {
          "description": "Auto-join channel list at connect (example: [\"#openclaw\"]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "defaultAccount": {
          "description": "Optional default account id when multiple accounts are configured.",
          "type": "string"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by sender ID."
        },
        "enabled": {
          "description": "If false, do not start this account. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for group/channel senders.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Group/channel message handling policy."
        },
        "groups": {
          "$ref": "#/definitions/Record%3Cstring%2Cstructure--1718-1983--1703-1984--1690-1985--244-2090--213-2090--182-2091--0-2363%3E"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max group/channel messages to keep as history context (0 disables).",
          "type": "number"
        },
        "host": {
          "description": "IRC server hostname (example: irc.libera.chat).",
          "type": "string"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "description": "Max outbound media size in MB.",
          "type": "number"
        },
        "mentionPatterns": {
          "description": "Optional mention patterns specific to IRC channel messages.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "nick": {
          "description": "IRC nickname to identify this bot.",
          "type": "string"
        },
        "nickserv": {
          "additionalProperties": false,
          "description": "Optional NickServ identify/register settings.",
          "properties": {
            "enabled": {
              "description": "Enable NickServ identify/register after connect (default: enabled when password is set).",
              "type": "boolean"
            },
            "password": {
              "description": "NickServ password (sensitive).",
              "type": "string"
            },
            "passwordFile": {
              "description": "Optional file path containing NickServ password.",
              "type": "string"
            },
            "register": {
              "description": "If true, send NickServ REGISTER on connect.",
              "type": "boolean"
            },
            "registerEmail": {
              "description": "Email used with NickServ REGISTER.",
              "type": "string"
            },
            "service": {
              "description": "NickServ service nick (default: NickServ).",
              "type": "string"
            }
          },
          "type": "object"
        },
        "password": {
          "description": "Optional IRC server password (sensitive).",
          "type": "string"
        },
        "passwordFile": {
          "description": "Optional file path containing IRC server password.",
          "type": "string"
        },
        "port": {
          "description": "IRC server port (default: 6697 with TLS, otherwise 6667).",
          "type": "number"
        },
        "realname": {
          "description": "IRC USER field realname (default: OpenClaw).",
          "type": "string"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars).",
          "type": "number"
        },
        "tls": {
          "description": "Use TLS for IRC connection (default: true).",
          "type": "boolean"
        },
        "username": {
          "description": "IRC USER field username (defaults to nick).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "LinkModelConfig": {
      "additionalProperties": false,
      "properties": {
        "args": {
          "description": "CLI args (template-enabled).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "command": {
          "description": "CLI binary (required when type=cli).",
          "type": "string"
        },
        "timeoutSeconds": {
          "description": "Optional timeout override (seconds) for this model entry.",
          "type": "number"
        },
        "type": {
          "const": "cli",
          "description": "Use a CLI command for link processing.",
          "type": "string"
        }
      },
      "required": [
        "command"
      ],
      "type": "object"
    },
    "LinkToolsConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Enable link understanding when models are configured.",
          "type": "boolean"
        },
        "maxLinks": {
          "description": "Max number of links to process per message.",
          "type": "number"
        },
        "models": {
          "description": "Ordered model list (fallbacks in order).",
          "items": {
            "$ref": "#/definitions/LinkModelConfig"
          },
          "type": "array"
        },
        "scope": {
          "$ref": "#/definitions/MediaUnderstandingScopeConfig",
          "description": "Optional scope gating for understanding."
        },
        "timeoutSeconds": {
          "description": "Default timeout (seconds).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "LoggingConfig": {
      "additionalProperties": false,
      "properties": {
        "consoleLevel": {
          "enum": [
            "silent",
            "fatal",
            "error",
            "warn",
            "info",
            "debug",
            "trace"
          ],
          "type": "string"
        },
        "consoleStyle": {
          "enum": [
            "pretty",
            "compact",
            "json"
          ],
          "type": "string"
        },
        "file": {
          "type": "string"
        },
        "level": {
          "enum": [
            "silent",
            "fatal",
            "error",
            "warn",
            "info",
            "debug",
            "trace"
          ],
          "type": "string"
        },
        "maxFileBytes": {
          "description": "Maximum size of a single log file in bytes before writes are suppressed. Default: 500 MB.",
          "type": "number"
        },
        "redactPatterns": {
          "description": "Regex patterns used to redact sensitive tokens (defaults apply when unset).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "redactSensitive": {
          "description": "Redact sensitive tokens in tool summaries. Default: \"tools\".",
          "enum": [
            "off",
            "tools"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "MSTeamsChannelConfig": {
      "additionalProperties": false,
      "description": "Channel-level config for MS Teams.",
      "properties": {
        "replyStyle": {
          "$ref": "#/definitions/MSTeamsReplyStyle",
          "description": "Reply style: \"thread\" replies to the message, \"top-level\" posts a new message."
        },
        "requireMention": {
          "description": "Require",
          "type": "boolean"
        },
        "tools": {
          "$ref": "#/definitions/GroupToolPolicyConfig",
          "description": "Optional tool policy overrides for this channel."
        },
        "toolsBySender": {
          "$ref": "#/definitions/GroupToolPolicyBySenderConfig"
        }
      },
      "type": "object"
    },
    "MSTeamsConfig": {
      "additionalProperties": false,
      "properties": {
        "allowFrom": {
          "description": "Allowlist for DM senders (AAD object IDs or UPNs).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "appId": {
          "description": "Azure Bot App ID (from Azure Bot registration).",
          "type": "string"
        },
        "appPassword": {
          "$ref": "#/definitions/SecretInput",
          "description": "Azure Bot App Password / Client Secret."
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "dangerouslyAllowNameMatching": {
          "description": "Break-glass override: allow mutable identity matching (display names/UPNs) in allowlists. Default behavior is ID-only matching.",
          "type": "boolean"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "enabled": {
          "description": "If false, do not start the MS Teams provider. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for group/channel senders (AAD object IDs or UPNs).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how group/channel messages are handled:\n- \"open\": groups bypass allowFrom; mention-gating applies\n- \"disabled\": block all group messages\n- \"allowlist\": only allow group messages from senders in groupAllowFrom/allowFrom"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max group/channel messages to keep as history context (0 disables).",
          "type": "number"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaAllowHosts": {
          "description": "Allowed host suffixes for inbound attachment downloads. Use [\"*\"] to allow any host (not recommended).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mediaAuthAllowHosts": {
          "description": "Allowed host suffixes for attaching Authorization headers to inbound media retries. Use specific hosts only; avoid multi-tenant suffixes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mediaMaxMb": {
          "description": "Max media size in MB (default: 100MB for OneDrive upload support).",
          "type": "number"
        },
        "replyStyle": {
          "$ref": "#/definitions/MSTeamsReplyStyle",
          "description": "Default reply style: \"thread\" replies to the message, \"top-level\" posts a new message."
        },
        "requireMention": {
          "description": "Default: require",
          "type": "boolean"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "sharePointSiteId": {
          "description": "SharePoint site ID for file uploads in group chats/channels (e.g., \"contoso.sharepoint.com,guid1,guid2\").",
          "type": "string"
        },
        "teams": {
          "$ref": "#/definitions/Record%3Cstring%2CMSTeamsTeamConfig%3E",
          "description": "Per-team config. Key is team ID (from the /team/ URL path segment)."
        },
        "tenantId": {
          "description": "Azure AD Tenant ID (for single-tenant bots).",
          "type": "string"
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 4000.",
          "type": "number"
        },
        "webhook": {
          "$ref": "#/definitions/MSTeamsWebhookConfig",
          "description": "Webhook server configuration."
        }
      },
      "type": "object"
    },
    "MSTeamsReplyStyle": {
      "description": "Reply style for MS Teams messages.",
      "enum": [
        "thread",
        "top-level"
      ],
      "type": "string"
    },
    "MSTeamsTeamConfig": {
      "additionalProperties": false,
      "description": "Team-level config for MS Teams.",
      "properties": {
        "channels": {
          "$ref": "#/definitions/Record%3Cstring%2CMSTeamsChannelConfig%3E",
          "description": "Per-channel overrides. Key is conversation ID (e.g., \"19:...@thread.tacv2\")."
        },
        "replyStyle": {
          "$ref": "#/definitions/MSTeamsReplyStyle",
          "description": "Default reply style for channels in this team."
        },
        "requireMention": {
          "description": "Default requireMention for channels in this team.",
          "type": "boolean"
        },
        "tools": {
          "$ref": "#/definitions/GroupToolPolicyConfig",
          "description": "Default tool policy for channels in this team."
        },
        "toolsBySender": {
          "$ref": "#/definitions/GroupToolPolicyBySenderConfig"
        }
      },
      "type": "object"
    },
    "MSTeamsWebhookConfig": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "description": "Path for the messages endpoint. Default: /api/messages.",
          "type": "string"
        },
        "port": {
          "description": "Port for the webhook server. Default: 3978.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "MarkdownConfig": {
      "additionalProperties": false,
      "properties": {
        "tables": {
          "$ref": "#/definitions/MarkdownTableMode",
          "description": "Table rendering mode (off|bullets|code)."
        }
      },
      "type": "object"
    },
    "MarkdownTableMode": {
      "enum": [
        "off",
        "bullets",
        "code"
      ],
      "type": "string"
    },
    "MdnsDiscoveryConfig": {
      "additionalProperties": false,
      "properties": {
        "mode": {
          "$ref": "#/definitions/MdnsDiscoveryMode",
          "description": "mDNS/Bonjour discovery broadcast mode (default: minimal).\n- off: disable mDNS entirely\n- minimal: omit cliPath/sshPort from TXT records\n- full: include cliPath/sshPort in TXT records"
        }
      },
      "type": "object"
    },
    "MdnsDiscoveryMode": {
      "enum": [
        "off",
        "minimal",
        "full"
      ],
      "type": "string"
    },
    "MediaToolsConfig": {
      "additionalProperties": false,
      "properties": {
        "audio": {
          "$ref": "#/definitions/MediaUnderstandingConfig"
        },
        "concurrency": {
          "description": "Max concurrent media understanding runs.",
          "type": "number"
        },
        "image": {
          "$ref": "#/definitions/MediaUnderstandingConfig"
        },
        "models": {
          "description": "Shared model list applied across image/audio/video.",
          "items": {
            "$ref": "#/definitions/MediaUnderstandingModelConfig"
          },
          "type": "array"
        },
        "video": {
          "$ref": "#/definitions/MediaUnderstandingConfig"
        }
      },
      "type": "object"
    },
    "MediaUnderstandingAttachmentsConfig": {
      "additionalProperties": false,
      "properties": {
        "maxAttachments": {
          "description": "Max number of attachments to process (default: 1).",
          "type": "number"
        },
        "mode": {
          "description": "Select the first matching attachment or process multiple.",
          "enum": [
            "first",
            "all"
          ],
          "type": "string"
        },
        "prefer": {
          "description": "Attachment ordering preference.",
          "enum": [
            "first",
            "last",
            "path",
            "url"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "MediaUnderstandingCapability": {
      "enum": [
        "image",
        "audio",
        "video"
      ],
      "type": "string"
    },
    "MediaUnderstandingConfig": {
      "additionalProperties": false,
      "properties": {
        "attachments": {
          "$ref": "#/definitions/MediaUnderstandingAttachmentsConfig",
          "description": "Attachment selection policy."
        },
        "baseUrl": {
          "description": "Optional base URL override for provider requests.",
          "type": "string"
        },
        "deepgram": {
          "additionalProperties": false,
          "deprecated": "Use providerOptions.deepgram instead.",
          "properties": {
            "detectLanguage": {
              "type": "boolean"
            },
            "punctuate": {
              "type": "boolean"
            },
            "smartFormat": {
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "echoFormat": {
          "description": "Format string for the echoed transcript. Use `{transcript}` as placeholder. Default: '📝 \"{transcript}\"'",
          "type": "string"
        },
        "echoTranscript": {
          "description": "Echo the audio transcript back to the originating chat before agent processing. Lets users verify what was heard. Default: false.",
          "type": "boolean"
        },
        "enabled": {
          "description": "Enable media understanding when models are configured.",
          "type": "boolean"
        },
        "headers": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E",
          "description": "Optional headers merged into provider requests."
        },
        "language": {
          "description": "Default language hint (audio).",
          "type": "string"
        },
        "maxBytes": {
          "description": "Default max bytes to send.",
          "type": "number"
        },
        "maxChars": {
          "description": "Default max output characters.",
          "type": "number"
        },
        "models": {
          "description": "Ordered model list (fallbacks in order).",
          "items": {
            "$ref": "#/definitions/MediaUnderstandingModelConfig"
          },
          "type": "array"
        },
        "prompt": {
          "description": "Default prompt.",
          "type": "string"
        },
        "providerOptions": {
          "$ref": "#/definitions/Record%3Cstring%2CRecord%3Cstring%2C(string%7Cnumber%7Cboolean)%3E%3E",
          "description": "Optional provider-specific query params (merged into requests)."
        },
        "scope": {
          "$ref": "#/definitions/MediaUnderstandingScopeConfig",
          "description": "Optional scope gating for understanding."
        },
        "timeoutSeconds": {
          "description": "Default timeout (seconds).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "MediaUnderstandingModelConfig": {
      "additionalProperties": false,
      "properties": {
        "args": {
          "description": "CLI args (template-enabled).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "baseUrl": {
          "description": "Optional base URL override for provider requests.",
          "type": "string"
        },
        "capabilities": {
          "description": "Optional capability tags for shared model lists.",
          "items": {
            "$ref": "#/definitions/MediaUnderstandingCapability"
          },
          "type": "array"
        },
        "command": {
          "description": "CLI binary (required when type=cli).",
          "type": "string"
        },
        "deepgram": {
          "additionalProperties": false,
          "deprecated": "Use providerOptions.deepgram instead.",
          "properties": {
            "detectLanguage": {
              "type": "boolean"
            },
            "punctuate": {
              "type": "boolean"
            },
            "smartFormat": {
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "headers": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E",
          "description": "Optional headers merged into provider requests."
        },
        "language": {
          "description": "Optional language hint for audio transcription.",
          "type": "string"
        },
        "maxBytes": {
          "description": "Optional max bytes for this model entry.",
          "type": "number"
        },
        "maxChars": {
          "description": "Optional max output characters for this model entry.",
          "type": "number"
        },
        "model": {
          "description": "Model id for provider-based understanding.",
          "type": "string"
        },
        "preferredProfile": {
          "description": "Preferred profile id if multiple are available.",
          "type": "string"
        },
        "profile": {
          "description": "Auth profile id to use for this provider.",
          "type": "string"
        },
        "prompt": {
          "description": "Optional prompt override for this model entry.",
          "type": "string"
        },
        "provider": {
          "description": "provider API id (e.g. openai, google).",
          "type": "string"
        },
        "providerOptions": {
          "$ref": "#/definitions/Record%3Cstring%2CRecord%3Cstring%2C(string%7Cnumber%7Cboolean)%3E%3E",
          "description": "Optional provider-specific query params (merged into requests)."
        },
        "timeoutSeconds": {
          "description": "Optional timeout override (seconds) for this model entry.",
          "type": "number"
        },
        "type": {
          "description": "Use a CLI command instead of provider API.",
          "enum": [
            "provider",
            "cli"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "MediaUnderstandingScopeConfig": {
      "additionalProperties": false,
      "properties": {
        "default": {
          "$ref": "#/definitions/SessionSendPolicyAction"
        },
        "rules": {
          "items": {
            "$ref": "#/definitions/MediaUnderstandingScopeRule"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "MediaUnderstandingScopeMatch": {
      "additionalProperties": false,
      "properties": {
        "channel": {
          "type": "string"
        },
        "chatType": {
          "$ref": "#/definitions/ChatType"
        },
        "keyPrefix": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "MediaUnderstandingScopeRule": {
      "additionalProperties": false,
      "properties": {
        "action": {
          "$ref": "#/definitions/SessionSendPolicyAction"
        },
        "match": {
          "$ref": "#/definitions/MediaUnderstandingScopeMatch"
        }
      },
      "required": [
        "action"
      ],
      "type": "object"
    },
    "MemoryBackend": {
      "enum": [
        "builtin",
        "qmd"
      ],
      "type": "string"
    },
    "MemoryCitationsMode": {
      "enum": [
        "auto",
        "on",
        "off"
      ],
      "type": "string"
    },
    "MemoryConfig": {
      "additionalProperties": false,
      "properties": {
        "backend": {
          "$ref": "#/definitions/MemoryBackend"
        },
        "citations": {
          "$ref": "#/definitions/MemoryCitationsMode"
        },
        "qmd": {
          "$ref": "#/definitions/MemoryQmdConfig"
        }
      },
      "type": "object"
    },
    "MemoryQmdConfig": {
      "additionalProperties": false,
      "properties": {
        "command": {
          "type": "string"
        },
        "includeDefaultMemory": {
          "type": "boolean"
        },
        "limits": {
          "$ref": "#/definitions/MemoryQmdLimitsConfig"
        },
        "mcporter": {
          "$ref": "#/definitions/MemoryQmdMcporterConfig"
        },
        "paths": {
          "items": {
            "$ref": "#/definitions/MemoryQmdIndexPath"
          },
          "type": "array"
        },
        "scope": {
          "$ref": "#/definitions/SessionSendPolicyConfig"
        },
        "searchMode": {
          "$ref": "#/definitions/MemoryQmdSearchMode"
        },
        "sessions": {
          "$ref": "#/definitions/MemoryQmdSessionConfig"
        },
        "update": {
          "$ref": "#/definitions/MemoryQmdUpdateConfig"
        }
      },
      "type": "object"
    },
    "MemoryQmdIndexPath": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string"
        },
        "path": {
          "type": "string"
        },
        "pattern": {
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "MemoryQmdLimitsConfig": {
      "additionalProperties": false,
      "properties": {
        "maxInjectedChars": {
          "type": "number"
        },
        "maxResults": {
          "type": "number"
        },
        "maxSnippetChars": {
          "type": "number"
        },
        "timeoutMs": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "MemoryQmdMcporterConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Route QMD searches through mcporter (MCP runtime) instead of spawning `qmd` per query. Requires:\n- `mcporter` installed and on PATH\n- A configured mcporter server that runs `qmd mcp` with `lifecycle: keep-alive`",
          "type": "boolean"
        },
        "serverName": {
          "description": "mcporter server name (defaults to \"qmd\")",
          "type": "string"
        },
        "startDaemon": {
          "description": "Start the mcporter daemon automatically (defaults to true when enabled).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "MemoryQmdSearchMode": {
      "enum": [
        "query",
        "search",
        "vsearch"
      ],
      "type": "string"
    },
    "MemoryQmdSessionConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "exportDir": {
          "type": "string"
        },
        "retentionDays": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "MemoryQmdUpdateConfig": {
      "additionalProperties": false,
      "properties": {
        "commandTimeoutMs": {
          "type": "number"
        },
        "debounceMs": {
          "type": "number"
        },
        "embedInterval": {
          "type": "string"
        },
        "embedTimeoutMs": {
          "type": "number"
        },
        "interval": {
          "type": "string"
        },
        "onBoot": {
          "type": "boolean"
        },
        "updateTimeoutMs": {
          "type": "number"
        },
        "waitForBootSync": {
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "MemorySearchConfig": {
      "additionalProperties": false,
      "properties": {
        "cache": {
          "additionalProperties": false,
          "description": "Index cache behavior.",
          "properties": {
            "enabled": {
              "description": "Cache chunk embeddings in SQLite (default: true).",
              "type": "boolean"
            },
            "maxEntries": {
              "description": "Optional cap on cached embeddings (best-effort).",
              "type": "number"
            }
          },
          "type": "object"
        },
        "chunking": {
          "additionalProperties": false,
          "description": "Chunking configuration.",
          "properties": {
            "overlap": {
              "type": "number"
            },
            "tokens": {
              "type": "number"
            }
          },
          "type": "object"
        },
        "enabled": {
          "description": "Enable vector memory search (default: true).",
          "type": "boolean"
        },
        "experimental": {
          "additionalProperties": false,
          "description": "Experimental memory search settings.",
          "properties": {
            "sessionMemory": {
              "description": "Enable session transcript indexing (experimental, default: false).",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "extraPaths": {
          "description": "Extra paths to include in memory search (directories or .md files).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fallback": {
          "description": "Fallback behavior when embeddings fail.",
          "enum": [
            "openai",
            "gemini",
            "local",
            "voyage",
            "mistral",
            "ollama",
            "none"
          ],
          "type": "string"
        },
        "local": {
          "additionalProperties": false,
          "description": "Local embedding settings (node-llama-cpp).",
          "properties": {
            "modelCacheDir": {
              "description": "Optional cache directory for local models.",
              "type": "string"
            },
            "modelPath": {
              "description": "GGUF model path or hf: URI.",
              "type": "string"
            }
          },
          "type": "object"
        },
        "model": {
          "description": "Embedding model id (remote) or alias (local).",
          "type": "string"
        },
        "provider": {
          "description": "Embedding provider mode.",
          "enum": [
            "openai",
            "gemini",
            "local",
            "voyage",
            "mistral",
            "ollama"
          ],
          "type": "string"
        },
        "query": {
          "additionalProperties": false,
          "description": "Query behavior.",
          "properties": {
            "hybrid": {
              "additionalProperties": false,
              "properties": {
                "candidateMultiplier": {
                  "description": "Multiplier for candidate pool size (default: 4).",
                  "type": "number"
                },
                "enabled": {
                  "description": "Enable hybrid BM25 + vector search (default: true).",
                  "type": "boolean"
                },
                "mmr": {
                  "additionalProperties": false,
                  "description": "Optional MMR re-ranking for result diversity.",
                  "properties": {
                    "enabled": {
                      "description": "Enable MMR re-ranking (default: false).",
                      "type": "boolean"
                    },
                    "lambda": {
                      "description": "Lambda: 0 = max diversity, 1 = max relevance (default: 0.7).",
                      "type": "number"
                    }
                  },
                  "type": "object"
                },
                "temporalDecay": {
                  "additionalProperties": false,
                  "description": "Optional temporal decay to boost recency in hybrid scoring.",
                  "properties": {
                    "enabled": {
                      "description": "Enable temporal decay (default: false).",
                      "type": "boolean"
                    },
                    "halfLifeDays": {
                      "description": "Half-life in days for exponential decay (default: 30).",
                      "type": "number"
                    }
                  },
                  "type": "object"
                },
                "textWeight": {
                  "description": "Weight for BM25 text relevance when merging results (0-1).",
                  "type": "number"
                },
                "vectorWeight": {
                  "description": "Weight for vector similarity when merging results (0-1).",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "maxResults": {
              "type": "number"
            },
            "minScore": {
              "type": "number"
            }
          },
          "type": "object"
        },
        "remote": {
          "additionalProperties": false,
          "properties": {
            "apiKey": {
              "$ref": "#/definitions/SecretInput"
            },
            "baseUrl": {
              "type": "string"
            },
            "batch": {
              "additionalProperties": false,
              "properties": {
                "concurrency": {
                  "description": "Max concurrent batch jobs (default: 2).",
                  "type": "number"
                },
                "enabled": {
                  "description": "Enable batch API for embedding indexing (OpenAI/Gemini; default: true).",
                  "type": "boolean"
                },
                "pollIntervalMs": {
                  "description": "Poll interval in ms (default: 5000).",
                  "type": "number"
                },
                "timeoutMinutes": {
                  "description": "Timeout in minutes (default: 60).",
                  "type": "number"
                },
                "wait": {
                  "description": "Wait for batch completion (default: true).",
                  "type": "boolean"
                }
              },
              "type": "object"
            },
            "headers": {
              "$ref": "#/definitions/Record%3Cstring%2Cstring%3E"
            }
          },
          "type": "object"
        },
        "sources": {
          "description": "Sources to index and search (default: [\"memory\"]).",
          "items": {
            "enum": [
              "memory",
              "sessions"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "store": {
          "additionalProperties": false,
          "description": "Index storage configuration.",
          "properties": {
            "cache": {
              "additionalProperties": false,
              "properties": {
                "enabled": {
                  "description": "Enable embedding cache (default: true).",
                  "type": "boolean"
                },
                "maxEntries": {
                  "description": "Optional max cache entries per provider/model.",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "driver": {
              "const": "sqlite",
              "type": "string"
            },
            "path": {
              "type": "string"
            },
            "vector": {
              "additionalProperties": false,
              "properties": {
                "enabled": {
                  "description": "Enable sqlite-vec extension for vector search (default: true).",
                  "type": "boolean"
                },
                "extensionPath": {
                  "description": "Optional override path to sqlite-vec extension (.dylib/.so/.dll).",
                  "type": "string"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        },
        "sync": {
          "additionalProperties": false,
          "description": "Sync behavior.",
          "properties": {
            "intervalMinutes": {
              "type": "number"
            },
            "onSearch": {
              "type": "boolean"
            },
            "onSessionStart": {
              "type": "boolean"
            },
            "sessions": {
              "additionalProperties": false,
              "properties": {
                "deltaBytes": {
                  "description": "Minimum appended bytes before session transcripts are reindexed.",
                  "type": "number"
                },
                "deltaMessages": {
                  "description": "Minimum appended JSONL lines before session transcripts are reindexed.",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "watch": {
              "type": "boolean"
            },
            "watchDebounceMs": {
              "type": "number"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "MessagesConfig": {
      "additionalProperties": false,
      "properties": {
        "ackReaction": {
          "description": "Emoji reaction used to acknowledge inbound messages (empty disables).",
          "type": "string"
        },
        "ackReactionScope": {
          "description": "When to send ack reactions. Default: \"group-mentions\".",
          "enum": [
            "group-mentions",
            "group-all",
            "direct",
            "all",
            "off",
            "none"
          ],
          "type": "string"
        },
        "groupChat": {
          "$ref": "#/definitions/GroupChatConfig"
        },
        "inbound": {
          "$ref": "#/definitions/InboundDebounceConfig",
          "description": "Debounce rapid inbound messages per sender (global + per-channel overrides)."
        },
        "messagePrefix": {
          "deprecated": "Use `whatsapp.messagePrefix` (WhatsApp-only inbound prefix).",
          "type": "string"
        },
        "queue": {
          "$ref": "#/definitions/QueueConfig"
        },
        "removeAckAfterReply": {
          "description": "Remove ack reaction after reply is sent (default: false).",
          "type": "boolean"
        },
        "responsePrefix": {
          "description": "Prefix auto-added to all outbound replies.\n\n- string: explicit prefix (may include template variables)\n- special value: `\"auto\"` derives `[{agents.list[].identity.name}]` for the routed agent (when set)\n\nSupported template variables (case-insensitive):\n- `{model}` - short model name (e.g., `claude-opus-4-6`, `gpt-4o`)\n- `{modelFull}` - full model identifier (e.g., `anthropic/claude-opus-4-6`)\n- `{provider}` - provider name (e.g., `anthropic`, `openai`)\n- `{thinkingLevel}` or `{think}` - current thinking level (`high`, `low`, `off`)\n- `{identity.name}` or `{identityName}` - agent identity name\n\nExample: `\"[{model} | think:{thinkingLevel}]\"` → `\"[claude-opus-4-6 | think:high]\"`\n\nUnresolved variables remain as literal text (e.g., `{model}` if context unavailable).\n\nDefault: none",
          "type": "string"
        },
        "statusReactions": {
          "$ref": "#/definitions/StatusReactionsConfig",
          "description": "Lifecycle status reactions configuration."
        },
        "suppressToolErrors": {
          "description": "When true, suppress ⚠️ tool-error warnings from being shown to the user. Default: false.",
          "type": "boolean"
        },
        "tts": {
          "$ref": "#/definitions/TtsConfig",
          "description": "Text-to-speech settings for outbound replies."
        }
      },
      "type": "object"
    },
    "ModelApi": {
      "enum": [
        "openai-completions",
        "openai-responses",
        "openai-codex-responses",
        "anthropic-messages",
        "google-generative-ai",
        "github-copilot",
        "bedrock-converse-stream",
        "ollama"
      ],
      "type": "string"
    },
    "ModelCompatConfig": {
      "additionalProperties": false,
      "properties": {
        "maxTokensField": {
          "enum": [
            "max_completion_tokens",
            "max_tokens"
          ],
          "type": "string"
        },
        "requiresAssistantAfterToolResult": {
          "type": "boolean"
        },
        "requiresMistralToolIds": {
          "type": "boolean"
        },
        "requiresOpenAiAnthropicToolPayload": {
          "type": "boolean"
        },
        "requiresThinkingAsText": {
          "type": "boolean"
        },
        "requiresToolResultName": {
          "type": "boolean"
        },
        "supportsDeveloperRole": {
          "type": "boolean"
        },
        "supportsReasoningEffort": {
          "type": "boolean"
        },
        "supportsStore": {
          "type": "boolean"
        },
        "supportsStrictMode": {
          "type": "boolean"
        },
        "supportsTools": {
          "type": "boolean"
        },
        "supportsUsageInStreaming": {
          "type": "boolean"
        },
        "thinkingFormat": {
          "enum": [
            "openai",
            "zai",
            "qwen"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "ModelDefinitionConfig": {
      "additionalProperties": false,
      "properties": {
        "api": {
          "$ref": "#/definitions/ModelApi"
        },
        "compat": {
          "$ref": "#/definitions/ModelCompatConfig"
        },
        "contextWindow": {
          "type": "number"
        },
        "cost": {
          "additionalProperties": false,
          "properties": {
            "cacheRead": {
              "type": "number"
            },
            "cacheWrite": {
              "type": "number"
            },
            "input": {
              "type": "number"
            },
            "output": {
              "type": "number"
            }
          },
          "required": [
            "input",
            "output",
            "cacheRead",
            "cacheWrite"
          ],
          "type": "object"
        },
        "headers": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E"
        },
        "id": {
          "type": "string"
        },
        "input": {
          "items": {
            "enum": [
              "text",
              "image"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "maxTokens": {
          "type": "number"
        },
        "name": {
          "type": "string"
        },
        "reasoning": {
          "type": "boolean"
        }
      },
      "required": [
        "id",
        "name",
        "reasoning",
        "input",
        "cost",
        "contextWindow",
        "maxTokens"
      ],
      "type": "object"
    },
    "ModelProviderAuthMode": {
      "enum": [
        "api-key",
        "aws-sdk",
        "oauth",
        "token"
      ],
      "type": "string"
    },
    "ModelProviderConfig": {
      "additionalProperties": false,
      "properties": {
        "api": {
          "$ref": "#/definitions/ModelApi"
        },
        "apiKey": {
          "$ref": "#/definitions/SecretInput"
        },
        "auth": {
          "$ref": "#/definitions/ModelProviderAuthMode"
        },
        "authHeader": {
          "type": "boolean"
        },
        "baseUrl": {
          "type": "string"
        },
        "headers": {
          "$ref": "#/definitions/Record%3Cstring%2CSecretInput%3E"
        },
        "injectNumCtxForOpenAICompat": {
          "type": "boolean"
        },
        "models": {
          "items": {
            "$ref": "#/definitions/ModelDefinitionConfig"
          },
          "type": "array"
        }
      },
      "required": [
        "baseUrl",
        "models"
      ],
      "type": "object"
    },
    "ModelsConfig": {
      "additionalProperties": false,
      "properties": {
        "bedrockDiscovery": {
          "$ref": "#/definitions/BedrockDiscoveryConfig"
        },
        "mode": {
          "enum": [
            "merge",
            "replace"
          ],
          "type": "string"
        },
        "providers": {
          "$ref": "#/definitions/Record%3Cstring%2CModelProviderConfig%3E"
        }
      },
      "type": "object"
    },
    "NativeCommandsSetting": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "const": "auto",
          "type": "string"
        }
      ]
    },
    "NodeHostBrowserProxyConfig": {
      "additionalProperties": false,
      "properties": {
        "allowProfiles": {
          "description": "Optional allowlist of profile names exposed via the proxy.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "description": "Enable the browser proxy on the node host (default: true).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "NodeHostConfig": {
      "additionalProperties": false,
      "properties": {
        "browserProxy": {
          "$ref": "#/definitions/NodeHostBrowserProxyConfig",
          "description": "Browser proxy settings for node hosts."
        }
      },
      "type": "object"
    },
    "OutboundRetryConfig": {
      "additionalProperties": false,
      "properties": {
        "attempts": {
          "description": "Max retry attempts for outbound requests (default: 3).",
          "type": "number"
        },
        "jitter": {
          "description": "Jitter factor (0-1) applied to delays (default: 0.1).",
          "type": "number"
        },
        "maxDelayMs": {
          "description": "Maximum retry delay cap in ms (default: 30000).",
          "type": "number"
        },
        "minDelayMs": {
          "description": "Minimum retry delay in ms (default: 300-500ms depending on provider).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "Partial<Record<(\"direct\"|\"group\"|\"channel\"),ReplyToMode>>": {
      "additionalProperties": false,
      "properties": {
        "channel": {
          "$ref": "#/definitions/ReplyToMode"
        },
        "direct": {
          "$ref": "#/definitions/ReplyToMode"
        },
        "group": {
          "$ref": "#/definitions/ReplyToMode"
        }
      },
      "type": "object"
    },
    "Partial<Record<AcpSessionUpdateTag,boolean>>": {
      "additionalProperties": {
        "type": "boolean"
      },
      "properties": {
        "agent_message_chunk": {
          "type": "boolean"
        },
        "agent_thought_chunk": {
          "type": "boolean"
        },
        "available_commands_update": {
          "type": "boolean"
        },
        "config_option_update": {
          "type": "boolean"
        },
        "current_mode_update": {
          "type": "boolean"
        },
        "plan": {
          "type": "boolean"
        },
        "session_info_update": {
          "type": "boolean"
        },
        "tool_call": {
          "type": "boolean"
        },
        "tool_call_update": {
          "type": "boolean"
        },
        "usage_update": {
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "Partial<Record<string,(string|number)[]>>": {
      "additionalProperties": {
        "items": {
          "type": [
            "string",
            "number"
          ]
        },
        "type": "array"
      },
      "type": "object"
    },
    "PluginEntryConfig": {
      "additionalProperties": false,
      "properties": {
        "config": {
          "$ref": "#/definitions/Record%3Cstring%2Cunknown%3E"
        },
        "enabled": {
          "type": "boolean"
        },
        "hooks": {
          "additionalProperties": false,
          "properties": {
            "allowPromptInjection": {
              "description": "Controls prompt mutation via before_prompt_build and prompt fields from legacy before_agent_start.",
              "type": "boolean"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "PluginInstallRecord": {
      "$ref": "#/definitions/InstallRecordBase"
    },
    "PluginSlotsConfig": {
      "additionalProperties": false,
      "properties": {
        "contextEngine": {
          "description": "Select which plugin owns the context-engine slot.",
          "type": "string"
        },
        "memory": {
          "description": "Select which plugin owns the memory slot (\"none\" disables memory plugins).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "PluginsConfig": {
      "additionalProperties": false,
      "properties": {
        "allow": {
          "description": "Optional plugin allowlist (plugin ids).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "deny": {
          "description": "Optional plugin denylist (plugin ids).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "description": "Enable or disable plugin loading.",
          "type": "boolean"
        },
        "entries": {
          "$ref": "#/definitions/Record%3Cstring%2CPluginEntryConfig%3E"
        },
        "installs": {
          "$ref": "#/definitions/Record%3Cstring%2CPluginInstallRecord%3E"
        },
        "load": {
          "$ref": "#/definitions/PluginsLoadConfig"
        },
        "slots": {
          "$ref": "#/definitions/PluginSlotsConfig"
        }
      },
      "type": "object"
    },
    "PluginsLoadConfig": {
      "additionalProperties": false,
      "properties": {
        "paths": {
          "description": "Additional plugin/extension paths to load.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ProviderCommandsConfig": {
      "additionalProperties": false,
      "properties": {
        "native": {
          "$ref": "#/definitions/NativeCommandsSetting",
          "description": "Override native command registration for this provider (bool or \"auto\")."
        },
        "nativeSkills": {
          "$ref": "#/definitions/NativeCommandsSetting",
          "description": "Override native skill command registration for this provider (bool or \"auto\")."
        }
      },
      "type": "object"
    },
    "QueueConfig": {
      "additionalProperties": false,
      "properties": {
        "byChannel": {
          "$ref": "#/definitions/QueueModeByProvider"
        },
        "cap": {
          "type": "number"
        },
        "debounceMs": {
          "type": "number"
        },
        "debounceMsByChannel": {
          "$ref": "#/definitions/InboundDebounceByProvider",
          "description": "Per-channel debounce overrides (ms)."
        },
        "drop": {
          "$ref": "#/definitions/QueueDropPolicy"
        },
        "mode": {
          "$ref": "#/definitions/QueueMode"
        }
      },
      "type": "object"
    },
    "QueueDropPolicy": {
      "enum": [
        "old",
        "new",
        "summarize"
      ],
      "type": "string"
    },
    "QueueMode": {
      "enum": [
        "steer",
        "followup",
        "collect",
        "steer-backlog",
        "steer+backlog",
        "queue",
        "interrupt"
      ],
      "type": "string"
    },
    "QueueModeByProvider": {
      "additionalProperties": false,
      "properties": {
        "discord": {
          "$ref": "#/definitions/QueueMode"
        },
        "googlechat": {
          "$ref": "#/definitions/QueueMode"
        },
        "imessage": {
          "$ref": "#/definitions/QueueMode"
        },
        "irc": {
          "$ref": "#/definitions/QueueMode"
        },
        "msteams": {
          "$ref": "#/definitions/QueueMode"
        },
        "signal": {
          "$ref": "#/definitions/QueueMode"
        },
        "slack": {
          "$ref": "#/definitions/QueueMode"
        },
        "telegram": {
          "$ref": "#/definitions/QueueMode"
        },
        "webchat": {
          "$ref": "#/definitions/QueueMode"
        },
        "whatsapp": {
          "$ref": "#/definitions/QueueMode"
        }
      },
      "type": "object"
    },
    "Record<string,(string|number)[]>": {
      "additionalProperties": {
        "items": {
          "type": [
            "string",
            "number"
          ]
        },
        "type": "array"
      },
      "type": "object"
    },
    "Record<string,(string|number|boolean)>": {
      "additionalProperties": {
        "type": [
          "string",
          "number",
          "boolean"
        ]
      },
      "type": "object"
    },
    "Record<string,(string|number|structure-.ts-1372-1426-.ts-1354-1426-.ts-1339-1427-.ts-1194-1428-.ts-35-2491-.ts-0-2492-.ts-0-3771)>": {
      "additionalProperties": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "number"
          },
          {
            "additionalProperties": false,
            "properties": {
              "hard": {
                "type": "number"
              },
              "soft": {
                "type": "number"
              }
            },
            "type": "object"
          }
        ]
      },
      "type": "object"
    },
    "Record<string,AgentModelEntryConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/AgentModelEntryConfig"
      },
      "type": "object"
    },
    "Record<string,AuthProfileConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/AuthProfileConfig"
      },
      "type": "object"
    },
    "Record<string,BrowserProfileConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/BrowserProfileConfig"
      },
      "type": "object"
    },
    "Record<string,CliBackendConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/CliBackendConfig"
      },
      "type": "object"
    },
    "Record<string,DiscordAccountConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/DiscordAccountConfig"
      },
      "type": "object"
    },
    "Record<string,DiscordGuildChannelConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/DiscordGuildChannelConfig"
      },
      "type": "object"
    },
    "Record<string,DiscordGuildEntry>": {
      "additionalProperties": {
        "$ref": "#/definitions/DiscordGuildEntry"
      },
      "type": "object"
    },
    "Record<string,DmConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/DmConfig"
      },
      "type": "object"
    },
    "Record<string,GoogleChatAccountConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/GoogleChatAccountConfig"
      },
      "type": "object"
    },
    "Record<string,GoogleChatGroupConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/GoogleChatGroupConfig"
      },
      "type": "object"
    },
    "Record<string,GroupToolPolicyConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/GroupToolPolicyConfig"
      },
      "type": "object"
    },
    "Record<string,HookConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/HookConfig"
      },
      "type": "object"
    },
    "Record<string,HookInstallRecord>": {
      "additionalProperties": {
        "$ref": "#/definitions/HookInstallRecord"
      },
      "type": "object"
    },
    "Record<string,IMessageAccountConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/IMessageAccountConfig"
      },
      "type": "object"
    },
    "Record<string,IrcAccountConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/IrcAccountConfig"
      },
      "type": "object"
    },
    "Record<string,MSTeamsChannelConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/MSTeamsChannelConfig"
      },
      "type": "object"
    },
    "Record<string,MSTeamsTeamConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/MSTeamsTeamConfig"
      },
      "type": "object"
    },
    "Record<string,ModelProviderConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/ModelProviderConfig"
      },
      "type": "object"
    },
    "Record<string,PluginEntryConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/PluginEntryConfig"
      },
      "type": "object"
    },
    "Record<string,PluginInstallRecord>": {
      "additionalProperties": {
        "$ref": "#/definitions/PluginInstallRecord"
      },
      "type": "object"
    },
    "Record<string,Record<string,(string|number|boolean)>>": {
      "additionalProperties": {
        "$ref": "#/definitions/Record%3Cstring%2C(string%7Cnumber%7Cboolean)%3E"
      },
      "type": "object"
    },
    "Record<string,Record<string,string>>": {
      "additionalProperties": {
        "$ref": "#/definitions/Record%3Cstring%2Cstring%3E"
      },
      "type": "object"
    },
    "Record<string,SafeBinProfileFixture>": {
      "additionalProperties": {
        "$ref": "#/definitions/SafeBinProfileFixture"
      },
      "type": "object"
    },
    "Record<string,SecretInput>": {
      "additionalProperties": {
        "$ref": "#/definitions/SecretInput"
      },
      "type": "object"
    },
    "Record<string,SecretProviderConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/SecretProviderConfig"
      },
      "type": "object"
    },
    "Record<string,SessionResetConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/SessionResetConfig"
      },
      "type": "object"
    },
    "Record<string,SignalAccountConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/SignalAccountConfig"
      },
      "type": "object"
    },
    "Record<string,SkillConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/SkillConfig"
      },
      "type": "object"
    },
    "Record<string,SlackAccountConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/SlackAccountConfig"
      },
      "type": "object"
    },
    "Record<string,SlackChannelConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/SlackChannelConfig"
      },
      "type": "object"
    },
    "Record<string,TalkProviderConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/TalkProviderConfig"
      },
      "type": "object"
    },
    "Record<string,TelegramAccountConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/TelegramAccountConfig"
      },
      "type": "object"
    },
    "Record<string,TelegramDirectConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/TelegramDirectConfig"
      },
      "type": "object"
    },
    "Record<string,TelegramGroupConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/TelegramGroupConfig"
      },
      "type": "object"
    },
    "Record<string,TelegramTopicConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/TelegramTopicConfig"
      },
      "type": "object"
    },
    "Record<string,ToolPolicyConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/ToolPolicyConfig"
      },
      "type": "object"
    },
    "Record<string,WhatsAppAccountConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/WhatsAppAccountConfig"
      },
      "type": "object"
    },
    "Record<string,WhatsAppGroupConfig>": {
      "additionalProperties": {
        "$ref": "#/definitions/WhatsAppGroupConfig"
      },
      "type": "object"
    },
    "Record<string,number>": {
      "additionalProperties": {
        "type": "number"
      },
      "type": "object"
    },
    "Record<string,string>": {
      "additionalProperties": {
        "type": "string"
      },
      "type": "object"
    },
    "Record<string,string[]>": {
      "additionalProperties": {
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "type": "object"
    },
    "Record<string,structure--1718-1983--1703-1984--1690-1985--244-2090--213-2090--182-2091--0-2363>": {
      "additionalProperties": {
        "additionalProperties": false,
        "properties": {
          "allowFrom": {
            "items": {
              "type": [
                "string",
                "number"
              ]
            },
            "type": "array"
          },
          "enabled": {
            "type": "boolean"
          },
          "requireMention": {
            "type": "boolean"
          },
          "skills": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "systemPrompt": {
            "type": "string"
          },
          "tools": {
            "$ref": "#/definitions/GroupToolPolicyConfig"
          },
          "toolsBySender": {
            "$ref": "#/definitions/GroupToolPolicyBySenderConfig"
          }
        },
        "type": "object"
      },
      "type": "object"
    },
    "Record<string,structure-d.ts-3185-3321-d.ts-3170-3322-d.ts-3157-3323-d.ts-367-3534-d.ts-331-3535-d.ts-0-3827>": {
      "additionalProperties": {
        "additionalProperties": false,
        "properties": {
          "requireMention": {
            "type": "boolean"
          },
          "tools": {
            "$ref": "#/definitions/GroupToolPolicyConfig"
          },
          "toolsBySender": {
            "$ref": "#/definitions/GroupToolPolicyBySenderConfig"
          }
        },
        "type": "object"
      },
      "type": "object"
    },
    "Record<string,unknown>": {
      "additionalProperties": {},
      "type": "object"
    },
    "ReplyToMode": {
      "enum": [
        "off",
        "first",
        "all"
      ],
      "type": "string"
    },
    "SafeBinProfileFixture": {
      "additionalProperties": false,
      "properties": {
        "allowedValueFlags": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "deniedFlags": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxPositional": {
          "type": "number"
        },
        "minPositional": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "SandboxBrowserSettings": {
      "additionalProperties": false,
      "properties": {
        "allowHostControl": {
          "description": "Allow sandboxed sessions to target the host browser control server. Default: false.",
          "type": "boolean"
        },
        "autoStart": {
          "description": "When true (default), sandboxed browser control will try to start/reattach to the sandbox browser container when a tool call needs it.",
          "type": "boolean"
        },
        "autoStartTimeoutMs": {
          "description": "Max time to wait for CDP to become reachable after auto-start (ms).",
          "type": "number"
        },
        "binds": {
          "description": "Additional bind mounts for the browser container only. When set, replaces docker.binds for the browser container.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cdpPort": {
          "type": "number"
        },
        "cdpSourceRange": {
          "description": "Optional CIDR allowlist for CDP ingress at the container edge (for example: 172.21.0.1/32).",
          "type": "string"
        },
        "containerPrefix": {
          "type": "string"
        },
        "enableNoVnc": {
          "type": "boolean"
        },
        "enabled": {
          "type": "boolean"
        },
        "headless": {
          "type": "boolean"
        },
        "image": {
          "type": "string"
        },
        "network": {
          "description": "Docker network for sandbox browser containers (default: openclaw-sandbox-browser).",
          "type": "string"
        },
        "noVncPort": {
          "type": "number"
        },
        "vncPort": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "SandboxDockerSettings": {
      "additionalProperties": false,
      "properties": {
        "apparmorProfile": {
          "description": "AppArmor profile name.",
          "type": "string"
        },
        "binds": {
          "description": "Additional bind mounts (host:container:mode format, e.g. [\"/host/path:/container/path:rw\"]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "capDrop": {
          "description": "Drop Linux capabilities.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "containerPrefix": {
          "description": "Prefix for sandbox container names.",
          "type": "string"
        },
        "cpus": {
          "description": "Limit container CPU shares (e.g. 0.5, 1, 2).",
          "type": "number"
        },
        "dangerouslyAllowContainerNamespaceJoin": {
          "description": "Dangerous override: allow Docker `network: \"container:<id>\"` namespace joins. Default behavior blocks container namespace joins to preserve sandbox isolation.",
          "type": "boolean"
        },
        "dangerouslyAllowExternalBindSources": {
          "description": "Dangerous override: allow bind mount sources outside runtime allowlisted roots (workspace + agent workspace roots).",
          "type": "boolean"
        },
        "dangerouslyAllowReservedContainerTargets": {
          "description": "Dangerous override: allow bind mounts that target reserved container paths like /workspace or /agent.",
          "type": "boolean"
        },
        "dns": {
          "description": "DNS servers (e.g. [\"1.1.1.1\", \"8.8.8.8\"]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "env": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E",
          "description": "Extra environment variables for sandbox exec."
        },
        "extraHosts": {
          "description": "Extra host mappings (e.g. [\"api.local:10.0.0.2\"]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "image": {
          "description": "Docker image to use for sandbox containers.",
          "type": "string"
        },
        "memory": {
          "description": "Limit container memory (e.g. 512m, 2g, or bytes as number).",
          "type": [
            "string",
            "number"
          ]
        },
        "memorySwap": {
          "description": "Limit container memory swap (same format as memory).",
          "type": [
            "string",
            "number"
          ]
        },
        "network": {
          "description": "Container network mode (bridge|none|custom).",
          "type": "string"
        },
        "pidsLimit": {
          "description": "Limit container PIDs (0 = Docker default).",
          "type": "number"
        },
        "readOnlyRoot": {
          "description": "Run container rootfs read-only.",
          "type": "boolean"
        },
        "seccompProfile": {
          "description": "Seccomp profile (path or profile name).",
          "type": "string"
        },
        "setupCommand": {
          "description": "Optional setup command run once after container creation (array entries are joined by newline).",
          "type": "string"
        },
        "tmpfs": {
          "description": "Extra tmpfs mounts for read-only containers.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ulimits": {
          "$ref": "#/definitions/Record%3Cstring%2C(string%7Cnumber%7Cstructure-.ts-1372-1426-.ts-1354-1426-.ts-1339-1427-.ts-1194-1428-.ts-35-2491-.ts-0-2492-.ts-0-3771)%3E",
          "description": "Set ulimit values by name (e.g. nofile, nproc). Use \"soft:hard\" string, a number, or { soft, hard }."
        },
        "user": {
          "description": "Container user (uid:gid).",
          "type": "string"
        },
        "workdir": {
          "description": "Container workdir mount path (default: /workspace).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "SandboxPruneSettings": {
      "additionalProperties": false,
      "properties": {
        "idleHours": {
          "description": "Prune if idle for more than N hours (0 disables).",
          "type": "number"
        },
        "maxAgeDays": {
          "description": "Prune if older than N days (0 disables).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "SecretInput": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "#/definitions/SecretRef"
        }
      ]
    },
    "SecretProviderConfig": {
      "anyOf": [
        {
          "$ref": "#/definitions/EnvSecretProviderConfig"
        },
        {
          "$ref": "#/definitions/FileSecretProviderConfig"
        },
        {
          "$ref": "#/definitions/ExecSecretProviderConfig"
        }
      ]
    },
    "SecretRef": {
      "additionalProperties": false,
      "description": "Stable identifier for a secret in a configured source. Examples:\n- env source: provider \"default\", id \"OPENAI_API_KEY\"\n- file source: provider \"mounted-json\", id \"/providers/openai/apiKey\"\n- exec source: provider \"vault\", id \"openai/api-key\"",
      "properties": {
        "id": {
          "type": "string"
        },
        "provider": {
          "type": "string"
        },
        "source": {
          "$ref": "#/definitions/SecretRefSource"
        }
      },
      "required": [
        "source",
        "provider",
        "id"
      ],
      "type": "object"
    },
    "SecretRefSource": {
      "enum": [
        "env",
        "file",
        "exec"
      ],
      "type": "string"
    },
    "SecretsConfig": {
      "additionalProperties": false,
      "properties": {
        "defaults": {
          "additionalProperties": false,
          "properties": {
            "env": {
              "type": "string"
            },
            "exec": {
              "type": "string"
            },
            "file": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "providers": {
          "$ref": "#/definitions/Record%3Cstring%2CSecretProviderConfig%3E"
        },
        "resolution": {
          "additionalProperties": false,
          "properties": {
            "maxBatchBytes": {
              "type": "number"
            },
            "maxProviderConcurrency": {
              "type": "number"
            },
            "maxRefsPerProvider": {
              "type": "number"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "SessionConfig": {
      "additionalProperties": false,
      "properties": {
        "agentToAgent": {
          "additionalProperties": false,
          "properties": {
            "maxPingPongTurns": {
              "description": "Max ping-pong turns between requester/target (0–5). Default: 5.",
              "type": "number"
            }
          },
          "type": "object"
        },
        "dmScope": {
          "$ref": "#/definitions/DmScope",
          "description": "DM session scoping (default: \"main\")."
        },
        "identityLinks": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%5B%5D%3E",
          "description": "Map platform-prefixed identities (e.g. \"telegram:123\") to canonical DM peers."
        },
        "idleMinutes": {
          "type": "number"
        },
        "mainKey": {
          "type": "string"
        },
        "maintenance": {
          "$ref": "#/definitions/SessionMaintenanceConfig",
          "description": "Automatic session store maintenance (pruning, capping, file rotation)."
        },
        "parentForkMaxTokens": {
          "description": "Max parent transcript token count allowed for thread/session forking. If parent totalTokens is above this value, OpenClaw skips parent fork and starts a fresh thread session instead. Set to 0 to disable this guard.",
          "type": "number"
        },
        "reset": {
          "$ref": "#/definitions/SessionResetConfig"
        },
        "resetByChannel": {
          "$ref": "#/definitions/Record%3Cstring%2CSessionResetConfig%3E",
          "description": "Channel-specific reset overrides (e.g. { discord: { mode: \"idle\", idleMinutes: 10080 } })."
        },
        "resetByType": {
          "$ref": "#/definitions/SessionResetByTypeConfig"
        },
        "resetTriggers": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "scope": {
          "$ref": "#/definitions/SessionScope"
        },
        "sendPolicy": {
          "$ref": "#/definitions/SessionSendPolicyConfig"
        },
        "store": {
          "type": "string"
        },
        "threadBindings": {
          "$ref": "#/definitions/SessionThreadBindingsConfig",
          "description": "Shared defaults for thread-bound session routing across channels/providers."
        },
        "typingIntervalSeconds": {
          "type": "number"
        },
        "typingMode": {
          "$ref": "#/definitions/TypingMode"
        }
      },
      "type": "object"
    },
    "SessionMaintenanceConfig": {
      "additionalProperties": false,
      "properties": {
        "highWaterBytes": {
          "description": "Target size after disk-budget cleanup (high-water mark), e.g. \"400mb\". Default: 80% of maxDiskBytes.",
          "type": [
            "number",
            "string"
          ]
        },
        "maxDiskBytes": {
          "description": "Optional per-agent sessions-directory disk budget (e.g. \"500mb\"). When exceeded, warn (mode=warn) or enforce oldest-first cleanup (mode=enforce).",
          "type": [
            "number",
            "string"
          ]
        },
        "maxEntries": {
          "description": "Maximum number of session entries to keep. Default: 500.",
          "type": "number"
        },
        "mode": {
          "$ref": "#/definitions/SessionMaintenanceMode",
          "description": "Whether to enforce maintenance or warn only. Default: \"warn\"."
        },
        "pruneAfter": {
          "description": "Remove session entries older than this duration (e.g. \"30d\", \"12h\"). Default: \"30d\".",
          "type": [
            "string",
            "number"
          ]
        },
        "pruneDays": {
          "description": "Deprecated. Use pruneAfter instead.",
          "type": "number"
        },
        "resetArchiveRetention": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            },
            {
              "const": false,
              "type": "boolean"
            }
          ],
          "description": "Retention for archived reset transcripts (`*.reset.<timestamp>`). Set `false` to disable reset-archive cleanup. Default: same as `pruneAfter` (30d)."
        },
        "rotateBytes": {
          "description": "Rotate sessions.json when it exceeds this size (e.g. \"10mb\"). Default: 10mb.",
          "type": [
            "number",
            "string"
          ]
        }
      },
      "type": "object"
    },
    "SessionMaintenanceMode": {
      "enum": [
        "enforce",
        "warn"
      ],
      "type": "string"
    },
    "SessionResetByTypeConfig": {
      "additionalProperties": false,
      "properties": {
        "direct": {
          "$ref": "#/definitions/SessionResetConfig"
        },
        "dm": {
          "$ref": "#/definitions/SessionResetConfig",
          "deprecated": "Use `direct` instead. Kept for backward compatibility."
        },
        "group": {
          "$ref": "#/definitions/SessionResetConfig"
        },
        "thread": {
          "$ref": "#/definitions/SessionResetConfig"
        }
      },
      "type": "object"
    },
    "SessionResetConfig": {
      "additionalProperties": false,
      "properties": {
        "atHour": {
          "description": "Local hour (0-23) for the daily reset boundary.",
          "type": "number"
        },
        "idleMinutes": {
          "description": "Sliding idle window (minutes). When set with daily mode, whichever expires first wins.",
          "type": "number"
        },
        "mode": {
          "$ref": "#/definitions/SessionResetMode"
        }
      },
      "type": "object"
    },
    "SessionResetMode": {
      "enum": [
        "daily",
        "idle"
      ],
      "type": "string"
    },
    "SessionScope": {
      "enum": [
        "per-sender",
        "global"
      ],
      "type": "string"
    },
    "SessionSendPolicyAction": {
      "enum": [
        "allow",
        "deny"
      ],
      "type": "string"
    },
    "SessionSendPolicyConfig": {
      "additionalProperties": false,
      "properties": {
        "default": {
          "$ref": "#/definitions/SessionSendPolicyAction"
        },
        "rules": {
          "items": {
            "$ref": "#/definitions/SessionSendPolicyRule"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "SessionSendPolicyMatch": {
      "additionalProperties": false,
      "properties": {
        "channel": {
          "type": "string"
        },
        "chatType": {
          "$ref": "#/definitions/ChatType"
        },
        "keyPrefix": {
          "description": "Session key prefix match. Note: some consumers match against a normalized key (for example, stripping `agent:<id>:`).",
          "type": "string"
        },
        "rawKeyPrefix": {
          "description": "Optional raw session-key prefix match for consumers that normalize session keys.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "SessionSendPolicyRule": {
      "additionalProperties": false,
      "properties": {
        "action": {
          "$ref": "#/definitions/SessionSendPolicyAction"
        },
        "match": {
          "$ref": "#/definitions/SessionSendPolicyMatch"
        }
      },
      "required": [
        "action"
      ],
      "type": "object"
    },
    "SessionThreadBindingsConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Master switch for thread-bound session routing features. Channel/provider keys can override this default.",
          "type": "boolean"
        },
        "idleHours": {
          "description": "Inactivity window for thread-bound sessions (hours). Session auto-unfocuses after this amount of idle time. Set to 0 to disable. Default: 24.",
          "type": "number"
        },
        "maxAgeHours": {
          "description": "Optional hard max age for thread-bound sessions (hours). Session auto-unfocuses once this age is reached even if active. Set to 0 to disable. Default: 0.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "SessionsToolsVisibility": {
      "enum": [
        "self",
        "tree",
        "agent",
        "all"
      ],
      "type": "string"
    },
    "SignalAccountConfig": {
      "additionalProperties": false,
      "properties": {
        "account": {
          "description": "Optional explicit E.164 account for signal-cli.",
          "type": "string"
        },
        "accountUuid": {
          "description": "Optional account UUID for signal-cli (used for loop protection).",
          "type": "string"
        },
        "actions": {
          "additionalProperties": false,
          "description": "Action toggles for message tool capabilities.",
          "properties": {
            "reactions": {
              "description": "Enable/disable sending reactions via message tool (default: true).",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "allowFrom": {
          "description": "Optional allowlist for inbound DM senders.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "autoStart": {
          "description": "Auto-start signal-cli daemon (default: true if httpUrl not set).",
          "type": "boolean"
        },
        "blockStreaming": {
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "cliPath": {
          "description": "signal-cli binary path (default: signal-cli).",
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by sender ID."
        },
        "enabled": {
          "description": "If false, do not start this account. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for group/channel senders.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Group/channel message handling policy."
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max group/channel messages to keep as history context (0 disables).",
          "type": "number"
        },
        "httpHost": {
          "description": "HTTP host for signal-cli daemon (default 127.0.0.1).",
          "type": "string"
        },
        "httpPort": {
          "description": "HTTP port for signal-cli daemon (default 8080).",
          "type": "number"
        },
        "httpUrl": {
          "description": "Optional full base URL for signal-cli HTTP daemon.",
          "type": "string"
        },
        "ignoreAttachments": {
          "type": "boolean"
        },
        "ignoreStories": {
          "type": "boolean"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "description": "Max outbound media size in MB.",
          "type": "number"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "reactionAllowlist": {
          "description": "Allowlist for reaction notifications when mode is allowlist.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "reactionLevel": {
          "$ref": "#/definitions/SignalReactionLevel",
          "description": "Controls agent reaction behavior:\n- \"off\": No reactions\n- \"ack\": Only automatic ack reactions (👀 when processing)\n- \"minimal\": Agent can react sparingly (default)\n- \"extensive\": Agent can react liberally"
        },
        "reactionNotifications": {
          "$ref": "#/definitions/SignalReactionNotificationMode",
          "description": "Reaction notification mode (off|own|all|allowlist). Default: own."
        },
        "receiveMode": {
          "enum": [
            "on-start",
            "manual"
          ],
          "type": "string"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "sendReadReceipts": {
          "type": "boolean"
        },
        "startupTimeoutMs": {
          "description": "Max time to wait for signal-cli daemon startup (ms, cap 120000).",
          "type": "number"
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "SignalConfig": {
      "additionalProperties": false,
      "properties": {
        "account": {
          "description": "Optional explicit E.164 account for signal-cli.",
          "type": "string"
        },
        "accountUuid": {
          "description": "Optional account UUID for signal-cli (used for loop protection).",
          "type": "string"
        },
        "accounts": {
          "$ref": "#/definitions/Record%3Cstring%2CSignalAccountConfig%3E",
          "description": "Optional per-account Signal configuration (multi-account)."
        },
        "actions": {
          "additionalProperties": false,
          "description": "Action toggles for message tool capabilities.",
          "properties": {
            "reactions": {
              "description": "Enable/disable sending reactions via message tool (default: true).",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "allowFrom": {
          "description": "Optional allowlist for inbound DM senders.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "autoStart": {
          "description": "Auto-start signal-cli daemon (default: true if httpUrl not set).",
          "type": "boolean"
        },
        "blockStreaming": {
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "cliPath": {
          "description": "signal-cli binary path (default: signal-cli).",
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "defaultAccount": {
          "description": "Optional default account id when multiple accounts are configured.",
          "type": "string"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by sender ID."
        },
        "enabled": {
          "description": "If false, do not start this account. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for group/channel senders.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Group/channel message handling policy."
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max group/channel messages to keep as history context (0 disables).",
          "type": "number"
        },
        "httpHost": {
          "description": "HTTP host for signal-cli daemon (default 127.0.0.1).",
          "type": "string"
        },
        "httpPort": {
          "description": "HTTP port for signal-cli daemon (default 8080).",
          "type": "number"
        },
        "httpUrl": {
          "description": "Optional full base URL for signal-cli HTTP daemon.",
          "type": "string"
        },
        "ignoreAttachments": {
          "type": "boolean"
        },
        "ignoreStories": {
          "type": "boolean"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "description": "Max outbound media size in MB.",
          "type": "number"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "reactionAllowlist": {
          "description": "Allowlist for reaction notifications when mode is allowlist.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "reactionLevel": {
          "$ref": "#/definitions/SignalReactionLevel",
          "description": "Controls agent reaction behavior:\n- \"off\": No reactions\n- \"ack\": Only automatic ack reactions (👀 when processing)\n- \"minimal\": Agent can react sparingly (default)\n- \"extensive\": Agent can react liberally"
        },
        "reactionNotifications": {
          "$ref": "#/definitions/SignalReactionNotificationMode",
          "description": "Reaction notification mode (off|own|all|allowlist). Default: own."
        },
        "receiveMode": {
          "enum": [
            "on-start",
            "manual"
          ],
          "type": "string"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "sendReadReceipts": {
          "type": "boolean"
        },
        "startupTimeoutMs": {
          "description": "Max time to wait for signal-cli daemon startup (ms, cap 120000).",
          "type": "number"
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "SignalReactionLevel": {
      "enum": [
        "off",
        "ack",
        "minimal",
        "extensive"
      ],
      "type": "string"
    },
    "SignalReactionNotificationMode": {
      "enum": [
        "off",
        "own",
        "all",
        "allowlist"
      ],
      "type": "string"
    },
    "SkillConfig": {
      "additionalProperties": false,
      "properties": {
        "apiKey": {
          "$ref": "#/definitions/SecretInput"
        },
        "config": {
          "$ref": "#/definitions/Record%3Cstring%2Cunknown%3E"
        },
        "enabled": {
          "type": "boolean"
        },
        "env": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E"
        }
      },
      "type": "object"
    },
    "SkillsConfig": {
      "additionalProperties": false,
      "properties": {
        "allowBundled": {
          "description": "Optional bundled-skill allowlist (only affects bundled skills).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "entries": {
          "$ref": "#/definitions/Record%3Cstring%2CSkillConfig%3E"
        },
        "install": {
          "$ref": "#/definitions/SkillsInstallConfig"
        },
        "limits": {
          "$ref": "#/definitions/SkillsLimitsConfig"
        },
        "load": {
          "$ref": "#/definitions/SkillsLoadConfig"
        }
      },
      "type": "object"
    },
    "SkillsInstallConfig": {
      "additionalProperties": false,
      "properties": {
        "nodeManager": {
          "enum": [
            "npm",
            "pnpm",
            "yarn",
            "bun"
          ],
          "type": "string"
        },
        "preferBrew": {
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "SkillsLimitsConfig": {
      "additionalProperties": false,
      "properties": {
        "maxCandidatesPerRoot": {
          "description": "Max number of immediate child directories to consider under a skills root before treating it as suspicious.",
          "type": "number"
        },
        "maxSkillFileBytes": {
          "description": "Max size (bytes) allowed for a SKILL.md file to be considered.",
          "type": "number"
        },
        "maxSkillsInPrompt": {
          "description": "Max number of skills to include in the model-facing skills prompt.",
          "type": "number"
        },
        "maxSkillsLoadedPerSource": {
          "description": "Max number of skills to load per skills source (bundled/managed/workspace/extra).",
          "type": "number"
        },
        "maxSkillsPromptChars": {
          "description": "Max characters for the model-facing skills prompt block (approx).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "SkillsLoadConfig": {
      "additionalProperties": false,
      "properties": {
        "extraDirs": {
          "description": "Additional skill folders to scan (lowest precedence). Each directory should contain skill subfolders with `SKILL.md`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "watch": {
          "description": "Watch skill folders for changes and refresh the skills snapshot.",
          "type": "boolean"
        },
        "watchDebounceMs": {
          "description": "Debounce for the skills watcher (ms).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "SlackAccountConfig": {
      "additionalProperties": false,
      "properties": {
        "ackReaction": {
          "description": "Per-channel ack reaction override. Slack uses shortcodes (e.g., \"eyes\") rather than unicode emoji.",
          "type": "string"
        },
        "actions": {
          "$ref": "#/definitions/SlackActionConfig"
        },
        "allowBots": {
          "description": "Allow bot-authored messages to trigger replies (default: false).",
          "type": "boolean"
        },
        "allowFrom": {
          "description": "Alias for dm.allowFrom (prefer this so it inherits cleanly via base->account shallow merge). Legacy key: channels.slack.dm.allowFrom.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "appToken": {
          "type": "string"
        },
        "blockStreaming": {
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "botToken": {
          "type": "string"
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "channels": {
          "$ref": "#/definitions/Record%3Cstring%2CSlackChannelConfig%3E"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "commands": {
          "$ref": "#/definitions/ProviderCommandsConfig",
          "description": "Override native command registration for Slack (bool or \"auto\")."
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "dangerouslyAllowNameMatching": {
          "description": "Break-glass override: allow mutable identity matching (name/slug) in allowlists. Default behavior is ID-only matching.",
          "type": "boolean"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dm": {
          "$ref": "#/definitions/SlackDmConfig"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Alias for dm.policy (prefer this so it inherits cleanly via base->account shallow merge). Legacy key: channels.slack.dm.policy."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "enabled": {
          "description": "If false, do not start this Slack account. Default: true.",
          "type": "boolean"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how channel messages are handled:\n- \"open\": channels bypass allowlists; mention-gating applies\n- \"disabled\": block all channel messages\n- \"allowlist\": only allow channels present in channels.slack.channels"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max channel messages to keep as history context (0 disables).",
          "type": "number"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "type": "number"
        },
        "mode": {
          "description": "Slack connection mode (socket|http). Default: socket.",
          "enum": [
            "socket",
            "http"
          ],
          "type": "string"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "nativeStreaming": {
          "description": "Slack native text streaming toggle (`chat.startStream` / `chat.appendStream` / `chat.stopStream`). Used when `streaming` is `partial`. Default: true.",
          "type": "boolean"
        },
        "reactionAllowlist": {
          "description": "Allowlist for reaction notifications when mode is allowlist.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "reactionNotifications": {
          "$ref": "#/definitions/SlackReactionNotificationMode",
          "description": "Reaction notification mode (off|own|all|allowlist). Default: own."
        },
        "replyToMode": {
          "$ref": "#/definitions/ReplyToMode",
          "description": "Control reply threading when reply tags are present (off|first|all)."
        },
        "replyToModeByChatType": {
          "$ref": "#/definitions/Partial%3CRecord%3C(%22direct%22%7C%22group%22%7C%22channel%22)%2CReplyToMode%3E%3E",
          "description": "Optional per-chat-type reply threading overrides. Example: { direct: \"all\", group: \"first\", channel: \"off\" }."
        },
        "requireMention": {
          "description": "Default mention requirement for channel messages (default: true).",
          "type": "boolean"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "signingSecret": {
          "description": "Slack signing secret (required for HTTP mode).",
          "type": "string"
        },
        "slashCommand": {
          "$ref": "#/definitions/SlackSlashCommandConfig"
        },
        "streamMode": {
          "$ref": "#/definitions/SlackLegacyStreamMode",
          "deprecated": "Legacy preview mode key; migrated automatically to `streaming`."
        },
        "streaming": {
          "anyOf": [
            {
              "$ref": "#/definitions/SlackStreamingMode"
            },
            {
              "type": "boolean"
            }
          ],
          "description": "Stream preview mode:\n- \"off\": disable live preview streaming\n- \"partial\": replace preview text with the latest partial output (default)\n- \"block\": append chunked preview updates\n- \"progress\": show progress status, then send final text\n\nLegacy boolean values are still accepted and auto-migrated."
        },
        "textChunkLimit": {
          "type": "number"
        },
        "thread": {
          "$ref": "#/definitions/SlackThreadConfig",
          "description": "Thread session behavior."
        },
        "typingReaction": {
          "description": "Reaction emoji added while processing a reply (e.g. \"hourglass_flowing_sand\"). Removed when done. Useful as a typing indicator fallback when assistant mode is not enabled.",
          "type": "string"
        },
        "userToken": {
          "type": "string"
        },
        "userTokenReadOnly": {
          "description": "If true, restrict user token to read operations only. Default: true.",
          "type": "boolean"
        },
        "webhookPath": {
          "description": "Slack Events API webhook path (default: /slack/events).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "SlackActionConfig": {
      "additionalProperties": false,
      "properties": {
        "channelInfo": {
          "type": "boolean"
        },
        "emojiList": {
          "type": "boolean"
        },
        "memberInfo": {
          "type": "boolean"
        },
        "messages": {
          "type": "boolean"
        },
        "permissions": {
          "type": "boolean"
        },
        "pins": {
          "type": "boolean"
        },
        "reactions": {
          "type": "boolean"
        },
        "search": {
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "SlackChannelConfig": {
      "additionalProperties": false,
      "properties": {
        "allow": {
          "description": "Legacy channel allow toggle; prefer enabled.",
          "type": "boolean"
        },
        "allowBots": {
          "description": "Allow bot-authored messages to trigger replies (default: false).",
          "type": "boolean"
        },
        "enabled": {
          "description": "If false, disable the bot in this channel. (Alias for allow: false.)",
          "type": "boolean"
        },
        "requireMention": {
          "description": "Require mentioning the bot to trigger replies.",
          "type": "boolean"
        },
        "skills": {
          "description": "Optional skill filter for this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "systemPrompt": {
          "description": "Optional system prompt for this channel.",
          "type": "string"
        },
        "tools": {
          "$ref": "#/definitions/GroupToolPolicyConfig",
          "description": "Optional tool policy overrides for this channel."
        },
        "toolsBySender": {
          "$ref": "#/definitions/GroupToolPolicyBySenderConfig"
        },
        "users": {
          "description": "Allowlist of users that can invoke the bot in this channel.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "SlackConfig": {
      "additionalProperties": false,
      "properties": {
        "accounts": {
          "$ref": "#/definitions/Record%3Cstring%2CSlackAccountConfig%3E",
          "description": "Optional per-account Slack configuration (multi-account)."
        },
        "ackReaction": {
          "description": "Per-channel ack reaction override. Slack uses shortcodes (e.g., \"eyes\") rather than unicode emoji.",
          "type": "string"
        },
        "actions": {
          "$ref": "#/definitions/SlackActionConfig"
        },
        "allowBots": {
          "description": "Allow bot-authored messages to trigger replies (default: false).",
          "type": "boolean"
        },
        "allowFrom": {
          "description": "Alias for dm.allowFrom (prefer this so it inherits cleanly via base->account shallow merge). Legacy key: channels.slack.dm.allowFrom.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "appToken": {
          "type": "string"
        },
        "blockStreaming": {
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "botToken": {
          "type": "string"
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "channels": {
          "$ref": "#/definitions/Record%3Cstring%2CSlackChannelConfig%3E"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "commands": {
          "$ref": "#/definitions/ProviderCommandsConfig",
          "description": "Override native command registration for Slack (bool or \"auto\")."
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "dangerouslyAllowNameMatching": {
          "description": "Break-glass override: allow mutable identity matching (name/slug) in allowlists. Default behavior is ID-only matching.",
          "type": "boolean"
        },
        "defaultAccount": {
          "description": "Optional default account id when multiple accounts are configured.",
          "type": "string"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI --deliver when no explicit --reply-to is provided.",
          "type": "string"
        },
        "dm": {
          "$ref": "#/definitions/SlackDmConfig"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Alias for dm.policy (prefer this so it inherits cleanly via base->account shallow merge). Legacy key: channels.slack.dm.policy."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "enabled": {
          "description": "If false, do not start this Slack account. Default: true.",
          "type": "boolean"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how channel messages are handled:\n- \"open\": channels bypass allowlists; mention-gating applies\n- \"disabled\": block all channel messages\n- \"allowlist\": only allow channels present in channels.slack.channels"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max channel messages to keep as history context (0 disables).",
          "type": "number"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "type": "number"
        },
        "mode": {
          "description": "Slack connection mode (socket|http). Default: socket.",
          "enum": [
            "socket",
            "http"
          ],
          "type": "string"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "nativeStreaming": {
          "description": "Slack native text streaming toggle (`chat.startStream` / `chat.appendStream` / `chat.stopStream`). Used when `streaming` is `partial`. Default: true.",
          "type": "boolean"
        },
        "reactionAllowlist": {
          "description": "Allowlist for reaction notifications when mode is allowlist.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "reactionNotifications": {
          "$ref": "#/definitions/SlackReactionNotificationMode",
          "description": "Reaction notification mode (off|own|all|allowlist). Default: own."
        },
        "replyToMode": {
          "$ref": "#/definitions/ReplyToMode",
          "description": "Control reply threading when reply tags are present (off|first|all)."
        },
        "replyToModeByChatType": {
          "$ref": "#/definitions/Partial%3CRecord%3C(%22direct%22%7C%22group%22%7C%22channel%22)%2CReplyToMode%3E%3E",
          "description": "Optional per-chat-type reply threading overrides. Example: { direct: \"all\", group: \"first\", channel: \"off\" }."
        },
        "requireMention": {
          "description": "Default mention requirement for channel messages (default: true).",
          "type": "boolean"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override for this channel/account.",
          "type": "string"
        },
        "signingSecret": {
          "description": "Slack signing secret (required for HTTP mode).",
          "type": "string"
        },
        "slashCommand": {
          "$ref": "#/definitions/SlackSlashCommandConfig"
        },
        "streamMode": {
          "$ref": "#/definitions/SlackLegacyStreamMode",
          "deprecated": "Legacy preview mode key; migrated automatically to `streaming`."
        },
        "streaming": {
          "anyOf": [
            {
              "$ref": "#/definitions/SlackStreamingMode"
            },
            {
              "type": "boolean"
            }
          ],
          "description": "Stream preview mode:\n- \"off\": disable live preview streaming\n- \"partial\": replace preview text with the latest partial output (default)\n- \"block\": append chunked preview updates\n- \"progress\": show progress status, then send final text\n\nLegacy boolean values are still accepted and auto-migrated."
        },
        "textChunkLimit": {
          "type": "number"
        },
        "thread": {
          "$ref": "#/definitions/SlackThreadConfig",
          "description": "Thread session behavior."
        },
        "typingReaction": {
          "description": "Reaction emoji added while processing a reply (e.g. \"hourglass_flowing_sand\"). Removed when done. Useful as a typing indicator fallback when assistant mode is not enabled.",
          "type": "string"
        },
        "userToken": {
          "type": "string"
        },
        "userTokenReadOnly": {
          "description": "If true, restrict user token to read operations only. Default: true.",
          "type": "boolean"
        },
        "webhookPath": {
          "description": "Slack Events API webhook path (default: /slack/events).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "SlackDmConfig": {
      "additionalProperties": false,
      "properties": {
        "allowFrom": {
          "description": "Allowlist for DM senders (ids).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "enabled": {
          "description": "If false, ignore all incoming Slack DMs. Default: true.",
          "type": "boolean"
        },
        "groupChannels": {
          "description": "Optional allowlist for group DM channels (ids or slugs).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupEnabled": {
          "description": "If true, allow group DMs (default: false).",
          "type": "boolean"
        },
        "policy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        },
        "replyToMode": {
          "$ref": "#/definitions/ReplyToMode",
          "deprecated": "Prefer channels.slack.replyToModeByChatType.direct."
        }
      },
      "type": "object"
    },
    "SlackLegacyStreamMode": {
      "enum": [
        "replace",
        "status_final",
        "append"
      ],
      "type": "string"
    },
    "SlackReactionNotificationMode": {
      "enum": [
        "off",
        "own",
        "all",
        "allowlist"
      ],
      "type": "string"
    },
    "SlackSlashCommandConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Enable handling for the configured slash command (default: false).",
          "type": "boolean"
        },
        "ephemeral": {
          "description": "Reply ephemerally (default: true).",
          "type": "boolean"
        },
        "name": {
          "description": "Slash command name (default: \"openclaw\").",
          "type": "string"
        },
        "sessionPrefix": {
          "description": "Session key prefix for slash commands (default: \"slack:slash\").",
          "type": "string"
        }
      },
      "type": "object"
    },
    "SlackStreamingMode": {
      "enum": [
        "off",
        "partial",
        "block",
        "progress"
      ],
      "type": "string"
    },
    "SlackThreadConfig": {
      "additionalProperties": false,
      "properties": {
        "historyScope": {
          "description": "Scope for thread history context (thread|channel). Default: thread.",
          "enum": [
            "thread",
            "channel"
          ],
          "type": "string"
        },
        "inheritParent": {
          "description": "If true, thread sessions inherit the parent channel transcript. Default: false.",
          "type": "boolean"
        },
        "initialHistoryLimit": {
          "description": "Maximum number of thread messages to fetch as context when starting a new thread session (default: 20). Set to 0 to disable thread history fetching.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "StatusReactionsConfig": {
      "additionalProperties": false,
      "properties": {
        "emojis": {
          "$ref": "#/definitions/StatusReactionsEmojiConfig",
          "description": "Override default emojis."
        },
        "enabled": {
          "description": "Enable lifecycle status reactions (default: false).",
          "type": "boolean"
        },
        "timing": {
          "$ref": "#/definitions/StatusReactionsTimingConfig",
          "description": "Override default timing."
        }
      },
      "type": "object"
    },
    "StatusReactionsEmojiConfig": {
      "additionalProperties": false,
      "properties": {
        "coding": {
          "type": "string"
        },
        "done": {
          "type": "string"
        },
        "error": {
          "type": "string"
        },
        "stallHard": {
          "type": "string"
        },
        "stallSoft": {
          "type": "string"
        },
        "thinking": {
          "type": "string"
        },
        "tool": {
          "type": "string"
        },
        "web": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "StatusReactionsTimingConfig": {
      "additionalProperties": false,
      "properties": {
        "debounceMs": {
          "description": "Debounce interval for intermediate states (ms). Default: 700.",
          "type": "number"
        },
        "doneHoldMs": {
          "description": "How long to hold done emoji before cleanup (ms). Default: 1500.",
          "type": "number"
        },
        "errorHoldMs": {
          "description": "How long to hold error emoji before cleanup (ms). Default: 2500.",
          "type": "number"
        },
        "stallHardMs": {
          "description": "Hard stall warning timeout (ms). Default: 60000.",
          "type": "number"
        },
        "stallSoftMs": {
          "description": "Soft stall warning timeout (ms). Default: 25000.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "TalkConfig": {
      "additionalProperties": false,
      "properties": {
        "apiKey": {
          "$ref": "#/definitions/SecretInput"
        },
        "interruptOnSpeech": {
          "description": "Stop speaking when user starts talking (default: true).",
          "type": "boolean"
        },
        "modelId": {
          "type": "string"
        },
        "outputFormat": {
          "type": "string"
        },
        "provider": {
          "description": "Active Talk TTS provider (for example \"elevenlabs\").",
          "type": "string"
        },
        "providers": {
          "$ref": "#/definitions/Record%3Cstring%2CTalkProviderConfig%3E",
          "description": "Provider-specific Talk config keyed by provider id."
        },
        "silenceTimeoutMs": {
          "description": "Milliseconds of user silence before Talk mode sends the transcript after a pause.",
          "type": "number"
        },
        "voiceAliases": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E"
        },
        "voiceId": {
          "description": "Legacy ElevenLabs compatibility fields. Kept during rollout while older clients migrate to provider/providers.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "TalkProviderConfig": {
      "additionalProperties": {
        "description": "Provider-specific extensions."
      },
      "properties": {
        "apiKey": {
          "$ref": "#/definitions/SecretInput",
          "description": "Provider API key (optional; provider-specific env fallback may apply)."
        },
        "modelId": {
          "description": "Default provider model ID for Talk mode.",
          "type": "string"
        },
        "outputFormat": {
          "description": "Default provider output format (for example pcm_44100).",
          "type": "string"
        },
        "voiceAliases": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E",
          "description": "Optional voice name -> provider voice ID map."
        },
        "voiceId": {
          "description": "Default voice ID for the provider's Talk mode implementation.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "TelegramAccountConfig": {
      "additionalProperties": false,
      "properties": {
        "ackReaction": {
          "description": "Per-channel ack reaction override. Telegram expects unicode emoji (e.g., \"👀\") rather than shortcodes.",
          "type": "string"
        },
        "actions": {
          "$ref": "#/definitions/TelegramActionConfig",
          "description": "Per-action tool gating (default: true for all)."
        },
        "allowFrom": {
          "description": "DM allowlist (numeric Telegram user IDs). Onboarding can resolve",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "blockStreaming": {
          "description": "Disable block streaming for this account.",
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "botToken": {
          "type": "string"
        },
        "capabilities": {
          "$ref": "#/definitions/TelegramCapabilitiesConfig",
          "description": "Optional provider capability tags used for agent/runtime guidance."
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "commands": {
          "$ref": "#/definitions/ProviderCommandsConfig",
          "description": "Override native command registration for Telegram (bool or \"auto\")."
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "customCommands": {
          "description": "Custom commands to register in Telegram's command menu (merged with native).",
          "items": {
            "$ref": "#/definitions/TelegramCustomCommand"
          },
          "type": "array"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI `--deliver` when no explicit `--reply-to` is provided.",
          "type": [
            "string",
            "number"
          ]
        },
        "direct": {
          "$ref": "#/definitions/Record%3Cstring%2CTelegramDirectConfig%3E",
          "description": "Per-DM configuration for Telegram DM topics (key is chat ID)."
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Controls how Telegram direct chats (DMs) are handled:\n- \"pairing\" (default): unknown senders get a pairing code; owner must approve\n- \"allowlist\": only allow senders in allowFrom (or paired allow store)\n- \"open\": allow all inbound DMs (requires allowFrom to include \"*\")\n- \"disabled\": ignore all inbound DMs"
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "draftChunk": {
          "$ref": "#/definitions/BlockStreamingChunkConfig",
          "deprecated": "Legacy chunking config from `streamMode: \"block\"`; ignored after migration."
        },
        "enabled": {
          "description": "If false, do not start this Telegram account. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for Telegram group senders (numeric Telegram user IDs).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how group messages are handled:\n- \"open\": groups bypass allowFrom, only mention-gating applies\n- \"disabled\": block all group messages entirely\n- \"allowlist\": only allow group messages from senders in groupAllowFrom/allowFrom"
        },
        "groups": {
          "$ref": "#/definitions/Record%3Cstring%2CTelegramGroupConfig%3E"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max group messages to keep as history context (0 disables).",
          "type": "number"
        },
        "linkPreview": {
          "description": "Controls whether link previews are shown in outbound messages. Default: true.",
          "type": "boolean"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "type": "number"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "network": {
          "$ref": "#/definitions/TelegramNetworkConfig",
          "description": "Network transport overrides for Telegram."
        },
        "proxy": {
          "type": "string"
        },
        "reactionLevel": {
          "description": "Controls agent's reaction capability:\n- \"off\": agent cannot react\n- \"ack\" (default): bot sends acknowledgment reactions (👀 while processing)\n- \"minimal\": agent can react sparingly (guideline: 1 per 5-10 exchanges)\n- \"extensive\": agent can react liberally when appropriate",
          "enum": [
            "off",
            "ack",
            "minimal",
            "extensive"
          ],
          "type": "string"
        },
        "reactionNotifications": {
          "description": "Controls which user reactions trigger notifications:\n- \"off\" (default): ignore all reactions\n- \"own\": notify when users react to bot messages\n- \"all\": notify agent of all reactions",
          "enum": [
            "off",
            "own",
            "all"
          ],
          "type": "string"
        },
        "replyToMode": {
          "$ref": "#/definitions/ReplyToMode",
          "description": "Control reply threading when reply tags are present (off|first|all)."
        },
        "responsePrefix": {
          "description": "Per-channel outbound response prefix override.\n\nWhen set, this takes precedence over the global `messages.responsePrefix`. Use `\"\"` to explicitly disable a global prefix for this channel. Use `\"auto\"` to derive `[{identity.name}]` from the routed agent.",
          "type": "string"
        },
        "retry": {
          "$ref": "#/definitions/OutboundRetryConfig",
          "description": "Retry policy for outbound Telegram API calls."
        },
        "streamMode": {
          "deprecated": "Legacy key; migrated automatically to `streaming`.",
          "enum": [
            "off",
            "partial",
            "block"
          ],
          "type": "string"
        },
        "streaming": {
          "anyOf": [
            {
              "$ref": "#/definitions/TelegramStreamingMode"
            },
            {
              "type": "boolean"
            }
          ],
          "description": "Stream preview mode:\n- \"off\": disable preview updates\n- \"partial\": edit a single preview message\n- \"block\": stream in larger chunked updates\n- \"progress\": alias that maps to \"partial\" on Telegram\n\nLegacy boolean values are still accepted and auto-migrated."
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 4000.",
          "type": "number"
        },
        "threadBindings": {
          "$ref": "#/definitions/SessionThreadBindingsConfig",
          "description": "Telegram thread/conversation binding overrides."
        },
        "timeoutSeconds": {
          "description": "Telegram API client timeout in seconds (grammY ApiClientOptions).",
          "type": "number"
        },
        "tokenFile": {
          "description": "Path to file containing bot token (for secret managers like agenix).",
          "type": "string"
        },
        "webhookCertPath": {
          "description": "Path to the self-signed certificate (PEM) to upload to Telegram during webhook registration.",
          "type": "string"
        },
        "webhookHost": {
          "description": "Local webhook listener bind host (default: 127.0.0.1).",
          "type": "string"
        },
        "webhookPath": {
          "type": "string"
        },
        "webhookPort": {
          "description": "Local webhook listener bind port (default: 8787).",
          "type": "number"
        },
        "webhookSecret": {
          "type": "string"
        },
        "webhookUrl": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "TelegramActionConfig": {
      "additionalProperties": false,
      "properties": {
        "createForumTopic": {
          "description": "Enable forum topic creation.",
          "type": "boolean"
        },
        "deleteMessage": {
          "type": "boolean"
        },
        "editMessage": {
          "type": "boolean"
        },
        "poll": {
          "description": "Enable poll creation. Requires sendMessage to also be enabled.",
          "type": "boolean"
        },
        "reactions": {
          "type": "boolean"
        },
        "sendMessage": {
          "type": "boolean"
        },
        "sticker": {
          "description": "Enable sticker actions (send and search).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "TelegramCapabilitiesConfig": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "additionalProperties": false,
          "properties": {
            "inlineButtons": {
              "$ref": "#/definitions/TelegramInlineButtonsScope"
            }
          },
          "type": "object"
        }
      ]
    },
    "TelegramConfig": {
      "additionalProperties": false,
      "properties": {
        "accounts": {
          "$ref": "#/definitions/Record%3Cstring%2CTelegramAccountConfig%3E",
          "description": "Optional per-account Telegram configuration (multi-account)."
        },
        "ackReaction": {
          "description": "Per-channel ack reaction override. Telegram expects unicode emoji (e.g., \"👀\") rather than shortcodes.",
          "type": "string"
        },
        "actions": {
          "$ref": "#/definitions/TelegramActionConfig",
          "description": "Per-action tool gating (default: true for all)."
        },
        "allowFrom": {
          "description": "DM allowlist (numeric Telegram user IDs). Onboarding can resolve",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "blockStreaming": {
          "description": "Disable block streaming for this account.",
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "botToken": {
          "type": "string"
        },
        "capabilities": {
          "$ref": "#/definitions/TelegramCapabilitiesConfig",
          "description": "Optional provider capability tags used for agent/runtime guidance."
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "commands": {
          "$ref": "#/definitions/ProviderCommandsConfig",
          "description": "Override native command registration for Telegram (bool or \"auto\")."
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "customCommands": {
          "description": "Custom commands to register in Telegram's command menu (merged with native).",
          "items": {
            "$ref": "#/definitions/TelegramCustomCommand"
          },
          "type": "array"
        },
        "defaultAccount": {
          "description": "Optional default account id when multiple accounts are configured.",
          "type": "string"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI `--deliver` when no explicit `--reply-to` is provided.",
          "type": [
            "string",
            "number"
          ]
        },
        "direct": {
          "$ref": "#/definitions/Record%3Cstring%2CTelegramDirectConfig%3E",
          "description": "Per-DM configuration for Telegram DM topics (key is chat ID)."
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Controls how Telegram direct chats (DMs) are handled:\n- \"pairing\" (default): unknown senders get a pairing code; owner must approve\n- \"allowlist\": only allow senders in allowFrom (or paired allow store)\n- \"open\": allow all inbound DMs (requires allowFrom to include \"*\")\n- \"disabled\": ignore all inbound DMs"
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "draftChunk": {
          "$ref": "#/definitions/BlockStreamingChunkConfig",
          "deprecated": "Legacy chunking config from `streamMode: \"block\"`; ignored after migration."
        },
        "enabled": {
          "description": "If false, do not start this Telegram account. Default: true.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for Telegram group senders (numeric Telegram user IDs).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how group messages are handled:\n- \"open\": groups bypass allowFrom, only mention-gating applies\n- \"disabled\": block all group messages entirely\n- \"allowlist\": only allow group messages from senders in groupAllowFrom/allowFrom"
        },
        "groups": {
          "$ref": "#/definitions/Record%3Cstring%2CTelegramGroupConfig%3E"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings for this channel."
        },
        "historyLimit": {
          "description": "Max group messages to keep as history context (0 disables).",
          "type": "number"
        },
        "linkPreview": {
          "description": "Controls whether link previews are shown in outbound messages. Default: true.",
          "type": "boolean"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "type": "number"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "network": {
          "$ref": "#/definitions/TelegramNetworkConfig",
          "description": "Network transport overrides for Telegram."
        },
        "proxy": {
          "type": "string"
        },
        "reactionLevel": {
          "description": "Controls agent's reaction capability:\n- \"off\": agent cannot react\n- \"ack\" (default): bot sends acknowledgment reactions (👀 while processing)\n- \"minimal\": agent can react sparingly (guideline: 1 per 5-10 exchanges)\n- \"extensive\": agent can react liberally when appropriate",
          "enum": [
            "off",
            "ack",
            "minimal",
            "extensive"
          ],
          "type": "string"
        },
        "reactionNotifications": {
          "description": "Controls which user reactions trigger notifications:\n- \"off\" (default): ignore all reactions\n- \"own\": notify when users react to bot messages\n- \"all\": notify agent of all reactions",
          "enum": [
            "off",
            "own",
            "all"
          ],
          "type": "string"
        },
        "replyToMode": {
          "$ref": "#/definitions/ReplyToMode",
          "description": "Control reply threading when reply tags are present (off|first|all)."
        },
        "responsePrefix": {
          "description": "Per-channel outbound response prefix override.\n\nWhen set, this takes precedence over the global `messages.responsePrefix`. Use `\"\"` to explicitly disable a global prefix for this channel. Use `\"auto\"` to derive `[{identity.name}]` from the routed agent.",
          "type": "string"
        },
        "retry": {
          "$ref": "#/definitions/OutboundRetryConfig",
          "description": "Retry policy for outbound Telegram API calls."
        },
        "streamMode": {
          "deprecated": "Legacy key; migrated automatically to `streaming`.",
          "enum": [
            "off",
            "partial",
            "block"
          ],
          "type": "string"
        },
        "streaming": {
          "anyOf": [
            {
              "$ref": "#/definitions/TelegramStreamingMode"
            },
            {
              "type": "boolean"
            }
          ],
          "description": "Stream preview mode:\n- \"off\": disable preview updates\n- \"partial\": edit a single preview message\n- \"block\": stream in larger chunked updates\n- \"progress\": alias that maps to \"partial\" on Telegram\n\nLegacy boolean values are still accepted and auto-migrated."
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 4000.",
          "type": "number"
        },
        "threadBindings": {
          "$ref": "#/definitions/SessionThreadBindingsConfig",
          "description": "Telegram thread/conversation binding overrides."
        },
        "timeoutSeconds": {
          "description": "Telegram API client timeout in seconds (grammY ApiClientOptions).",
          "type": "number"
        },
        "tokenFile": {
          "description": "Path to file containing bot token (for secret managers like agenix).",
          "type": "string"
        },
        "webhookCertPath": {
          "description": "Path to the self-signed certificate (PEM) to upload to Telegram during webhook registration.",
          "type": "string"
        },
        "webhookHost": {
          "description": "Local webhook listener bind host (default: 127.0.0.1).",
          "type": "string"
        },
        "webhookPath": {
          "type": "string"
        },
        "webhookPort": {
          "description": "Local webhook listener bind port (default: 8787).",
          "type": "number"
        },
        "webhookSecret": {
          "type": "string"
        },
        "webhookUrl": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "TelegramCustomCommand": {
      "additionalProperties": false,
      "description": "Custom command definition for Telegram bot menu.",
      "properties": {
        "command": {
          "description": "Command name (without leading /).",
          "type": "string"
        },
        "description": {
          "description": "Description shown in Telegram command menu.",
          "type": "string"
        }
      },
      "required": [
        "command",
        "description"
      ],
      "type": "object"
    },
    "TelegramDirectConfig": {
      "additionalProperties": false,
      "properties": {
        "allowFrom": {
          "description": "Optional allowlist for DM senders (numeric Telegram user IDs).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Per-DM override for DM message policy (open|disabled|allowlist)."
        },
        "enabled": {
          "description": "If false, disable the bot for this DM (and its topics).",
          "type": "boolean"
        },
        "requireTopic": {
          "description": "If true, require messages to be from a topic when topics are enabled.",
          "type": "boolean"
        },
        "skills": {
          "description": "If specified, only load these skills for this DM (when no topic). Omit = all skills; empty = no skills.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "systemPrompt": {
          "description": "Optional system prompt snippet for this DM.",
          "type": "string"
        },
        "tools": {
          "$ref": "#/definitions/GroupToolPolicyConfig",
          "description": "Optional tool policy overrides for this DM."
        },
        "toolsBySender": {
          "$ref": "#/definitions/GroupToolPolicyBySenderConfig"
        },
        "topics": {
          "$ref": "#/definitions/Record%3Cstring%2CTelegramTopicConfig%3E",
          "description": "Per-topic configuration for DM topics (key is message_thread_id as string)"
        }
      },
      "type": "object"
    },
    "TelegramGroupConfig": {
      "additionalProperties": false,
      "properties": {
        "allowFrom": {
          "description": "Optional allowlist for group senders (numeric Telegram user IDs).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "disableAudioPreflight": {
          "description": "If true, skip automatic voice-note transcription for mention detection in this group.",
          "type": "boolean"
        },
        "enabled": {
          "description": "If false, disable the bot for this group (and its topics).",
          "type": "boolean"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Per-group override for group message policy (open|disabled|allowlist)."
        },
        "requireMention": {
          "type": "boolean"
        },
        "skills": {
          "description": "If specified, only load these skills for this group (when no topic). Omit = all skills; empty = no skills.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "systemPrompt": {
          "description": "Optional system prompt snippet for this group.",
          "type": "string"
        },
        "tools": {
          "$ref": "#/definitions/GroupToolPolicyConfig",
          "description": "Optional tool policy overrides for this group."
        },
        "toolsBySender": {
          "$ref": "#/definitions/GroupToolPolicyBySenderConfig"
        },
        "topics": {
          "$ref": "#/definitions/Record%3Cstring%2CTelegramTopicConfig%3E",
          "description": "Per-topic configuration (key is message_thread_id as string)"
        }
      },
      "type": "object"
    },
    "TelegramInlineButtonsScope": {
      "enum": [
        "off",
        "dm",
        "group",
        "all",
        "allowlist"
      ],
      "type": "string"
    },
    "TelegramNetworkConfig": {
      "additionalProperties": false,
      "properties": {
        "autoSelectFamily": {
          "description": "Override Node's autoSelectFamily behavior (true = enable, false = disable).",
          "type": "boolean"
        },
        "dnsResultOrder": {
          "description": "DNS result order for network requests (\"ipv4first\" | \"verbatim\"). Set to \"ipv4first\" to prioritize IPv4 addresses and work around IPv6 issues. Default: \"ipv4first\" on Node 22+ to avoid common fetch failures.",
          "enum": [
            "ipv4first",
            "verbatim"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "TelegramStreamingMode": {
      "enum": [
        "off",
        "partial",
        "block",
        "progress"
      ],
      "type": "string"
    },
    "TelegramTopicConfig": {
      "additionalProperties": false,
      "properties": {
        "agentId": {
          "description": "Route this topic to a specific agent (overrides group-level and binding routing).",
          "type": "string"
        },
        "allowFrom": {
          "description": "Optional allowlist for topic senders (numeric Telegram user IDs).",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        },
        "disableAudioPreflight": {
          "description": "If true, skip automatic voice-note transcription for mention detection in this topic.",
          "type": "boolean"
        },
        "enabled": {
          "description": "If false, disable the bot for this topic.",
          "type": "boolean"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Per-topic override for group message policy (open|disabled|allowlist)."
        },
        "requireMention": {
          "type": "boolean"
        },
        "skills": {
          "description": "If specified, only load these skills for this topic. Omit = all skills; empty = no skills.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "systemPrompt": {
          "description": "Optional system prompt snippet for this topic.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ToolLoopDetectionConfig": {
      "additionalProperties": false,
      "properties": {
        "criticalThreshold": {
          "description": "Critical threshold for blocking repetitive loops (default: 20).",
          "type": "number"
        },
        "detectors": {
          "$ref": "#/definitions/ToolLoopDetectionDetectorConfig",
          "description": "Detector toggles."
        },
        "enabled": {
          "description": "Enable tool-loop protection (default: false).",
          "type": "boolean"
        },
        "globalCircuitBreakerThreshold": {
          "description": "Global no-progress breaker threshold (default: 30).",
          "type": "number"
        },
        "historySize": {
          "description": "Maximum tool call history entries retained for loop detection (default: 30).",
          "type": "number"
        },
        "warningThreshold": {
          "description": "Warning threshold before a warning-only loop classification (default: 10).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "ToolLoopDetectionDetectorConfig": {
      "additionalProperties": false,
      "properties": {
        "genericRepeat": {
          "description": "Enable warning/blocking for repeated identical calls to the same tool/params.",
          "type": "boolean"
        },
        "knownPollNoProgress": {
          "description": "Enable warning/blocking for known no-progress polling loops.",
          "type": "boolean"
        },
        "pingPong": {
          "description": "Enable warning/blocking for no-progress ping-pong alternating patterns.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ToolPolicyConfig": {
      "additionalProperties": false,
      "properties": {
        "allow": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alsoAllow": {
          "description": "Additional allowlist entries merged into the effective allowlist.\n\nIntended for additive configuration (e.g., \"also allow lobster\") without forcing users to replace/duplicate an existing allowlist or profile.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "deny": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "profile": {
          "$ref": "#/definitions/ToolProfileId"
        }
      },
      "type": "object"
    },
    "ToolProfileId": {
      "enum": [
        "minimal",
        "coding",
        "messaging",
        "full"
      ],
      "type": "string"
    },
    "ToolsConfig": {
      "additionalProperties": false,
      "properties": {
        "agentToAgent": {
          "additionalProperties": false,
          "properties": {
            "allow": {
              "description": "Allowlist of agent ids or patterns (implementation-defined).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "enabled": {
              "description": "Enable agent-to-agent messaging tools. Default: false.",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "allow": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alsoAllow": {
          "description": "Additional allowlist entries merged into allow and/or profile allowlist.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "byProvider": {
          "$ref": "#/definitions/Record%3Cstring%2CToolPolicyConfig%3E",
          "description": "Optional tool policy overrides keyed by provider id or \"provider/model\"."
        },
        "deny": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "elevated": {
          "additionalProperties": false,
          "description": "Elevated exec permissions for the host machine.",
          "properties": {
            "allowFrom": {
              "$ref": "#/definitions/AgentElevatedAllowFromConfig",
              "description": "Approved senders for /elevated (per-provider allowlists)."
            },
            "enabled": {
              "description": "Enable or disable elevated mode (default: true).",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "exec": {
          "$ref": "#/definitions/ExecToolConfig",
          "description": "Exec tool defaults."
        },
        "fs": {
          "$ref": "#/definitions/FsToolsConfig",
          "description": "Filesystem tool path guards."
        },
        "links": {
          "$ref": "#/definitions/LinkToolsConfig"
        },
        "loopDetection": {
          "$ref": "#/definitions/ToolLoopDetectionConfig",
          "description": "Runtime loop detection for repetitive/ stuck tool-call patterns."
        },
        "media": {
          "$ref": "#/definitions/MediaToolsConfig"
        },
        "message": {
          "additionalProperties": false,
          "description": "Message tool configuration.",
          "properties": {
            "allowCrossContextSend": {
              "deprecated": "Use tools.message.crossContext settings.\nAllows cross-context sends across providers.",
              "type": "boolean"
            },
            "broadcast": {
              "additionalProperties": false,
              "properties": {
                "enabled": {
                  "description": "Enable broadcast action (default: true).",
                  "type": "boolean"
                }
              },
              "type": "object"
            },
            "crossContext": {
              "additionalProperties": false,
              "properties": {
                "allowAcrossProviders": {
                  "description": "Allow sends across different providers (default: false).",
                  "type": "boolean"
                },
                "allowWithinProvider": {
                  "description": "Allow sends to other channels within the same provider (default: true).",
                  "type": "boolean"
                },
                "marker": {
                  "additionalProperties": false,
                  "description": "Cross-context marker configuration.",
                  "properties": {
                    "enabled": {
                      "description": "Enable origin markers for cross-context sends (default: true).",
                      "type": "boolean"
                    },
                    "prefix": {
                      "description": "Text prefix template, supports {channel}.",
                      "type": "string"
                    },
                    "suffix": {
                      "description": "Text suffix template, supports {channel}.",
                      "type": "string"
                    }
                  },
                  "type": "object"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        },
        "profile": {
          "$ref": "#/definitions/ToolProfileId",
          "description": "Base tool profile applied before allow/deny lists."
        },
        "sandbox": {
          "additionalProperties": false,
          "description": "Sandbox tool policy defaults (deny wins).",
          "properties": {
            "tools": {
              "additionalProperties": false,
              "properties": {
                "allow": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "deny": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        },
        "sessions": {
          "additionalProperties": false,
          "description": "Session tool visibility controls which sessions can be targeted by session tools (sessions_list, sessions_history, sessions_send).\n\nDefault: \"tree\" (current session + spawned subagent sessions).",
          "properties": {
            "visibility": {
              "$ref": "#/definitions/SessionsToolsVisibility",
              "description": "- \"self\": only the current session\n- \"tree\": current session + sessions spawned by this session (default)\n- \"agent\": any session belonging to the current agent id (can include other users)\n- \"all\": any session (cross-agent still requires tools.agentToAgent)"
            }
          },
          "type": "object"
        },
        "subagents": {
          "additionalProperties": false,
          "description": "Sub-agent tool policy defaults (deny wins).",
          "properties": {
            "model": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "additionalProperties": false,
                  "properties": {
                    "fallbacks": {
                      "items": {
                        "type": "string"
                      },
                      "type": "array"
                    },
                    "primary": {
                      "type": "string"
                    }
                  },
                  "type": "object"
                }
              ],
              "description": "Default model selection for spawned sub-agents (string or {primary,fallbacks})."
            },
            "tools": {
              "additionalProperties": false,
              "properties": {
                "allow": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "alsoAllow": {
                  "description": "Additional allowlist entries merged into allow and/or default sub-agent denylist.",
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "deny": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        },
        "web": {
          "additionalProperties": false,
          "properties": {
            "fetch": {
              "additionalProperties": false,
              "properties": {
                "cacheTtlMinutes": {
                  "description": "Cache TTL in minutes for fetched content.",
                  "type": "number"
                },
                "enabled": {
                  "description": "Enable web fetch tool (default: true).",
                  "type": "boolean"
                },
                "firecrawl": {
                  "additionalProperties": false,
                  "properties": {
                    "apiKey": {
                      "description": "Firecrawl API key (optional; defaults to FIRECRAWL_API_KEY env var).",
                      "type": "string"
                    },
                    "baseUrl": {
                      "description": "Firecrawl base URL (default: https://api.firecrawl.dev).",
                      "type": "string"
                    },
                    "enabled": {
                      "description": "Enable Firecrawl fallback (default: true when apiKey is set).",
                      "type": "boolean"
                    },
                    "maxAgeMs": {
                      "description": "Max age (ms) for cached Firecrawl content.",
                      "type": "number"
                    },
                    "onlyMainContent": {
                      "description": "Whether to keep only main content (default: true).",
                      "type": "boolean"
                    },
                    "timeoutSeconds": {
                      "description": "Timeout in seconds for Firecrawl requests.",
                      "type": "number"
                    }
                  },
                  "type": "object"
                },
                "maxChars": {
                  "description": "Max characters to return from fetched content.",
                  "type": "number"
                },
                "maxCharsCap": {
                  "description": "Hard cap for maxChars (tool or config), defaults to 50000.",
                  "type": "number"
                },
                "maxRedirects": {
                  "description": "Maximum number of redirects to follow (default: 3).",
                  "type": "number"
                },
                "readability": {
                  "description": "Use Readability to extract main content (default: true).",
                  "type": "boolean"
                },
                "timeoutSeconds": {
                  "description": "Timeout in seconds for fetch requests.",
                  "type": "number"
                },
                "userAgent": {
                  "description": "Override User-Agent header for fetch requests.",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "search": {
              "additionalProperties": false,
              "properties": {
                "apiKey": {
                  "$ref": "#/definitions/SecretInput",
                  "description": "Brave Search API key (optional; defaults to BRAVE_API_KEY env var)."
                },
                "brave": {
                  "additionalProperties": false,
                  "description": "Brave-specific configuration (used when provider=\"brave\").",
                  "properties": {
                    "mode": {
                      "description": "Brave Search mode: \"web\" (standard results) or \"llm-context\" (pre-extracted page content). Default: \"web\".",
                      "enum": [
                        "web",
                        "llm-context"
                      ],
                      "type": "string"
                    }
                  },
                  "type": "object"
                },
                "cacheTtlMinutes": {
                  "description": "Cache TTL in minutes for search results.",
                  "type": "number"
                },
                "enabled": {
                  "description": "Enable web search tool (default: true when API key is present).",
                  "type": "boolean"
                },
                "gemini": {
                  "additionalProperties": false,
                  "description": "Gemini-specific configuration (used when provider=\"gemini\").",
                  "properties": {
                    "apiKey": {
                      "$ref": "#/definitions/SecretInput",
                      "description": "Gemini API key (defaults to GEMINI_API_KEY env var)."
                    },
                    "model": {
                      "description": "Model to use for grounded search (defaults to \"gemini-2.5-flash\").",
                      "type": "string"
                    }
                  },
                  "type": "object"
                },
                "grok": {
                  "additionalProperties": false,
                  "description": "Grok-specific configuration (used when provider=\"grok\").",
                  "properties": {
                    "apiKey": {
                      "$ref": "#/definitions/SecretInput",
                      "description": "API key for xAI (defaults to XAI_API_KEY env var)."
                    },
                    "inlineCitations": {
                      "description": "Include inline citations in response text as markdown links (default: false).",
                      "type": "boolean"
                    },
                    "model": {
                      "description": "Model to use (defaults to \"grok-4-1-fast\").",
                      "type": "string"
                    }
                  },
                  "type": "object"
                },
                "kimi": {
                  "additionalProperties": false,
                  "description": "Kimi-specific configuration (used when provider=\"kimi\").",
                  "properties": {
                    "apiKey": {
                      "$ref": "#/definitions/SecretInput",
                      "description": "Moonshot/Kimi API key (defaults to KIMI_API_KEY or MOONSHOT_API_KEY env var)."
                    },
                    "baseUrl": {
                      "description": "Base URL for API requests (defaults to \"https://api.moonshot.ai/v1\").",
                      "type": "string"
                    },
                    "model": {
                      "description": "Model to use (defaults to \"moonshot-v1-128k\").",
                      "type": "string"
                    }
                  },
                  "type": "object"
                },
                "maxResults": {
                  "description": "Default search results count (1-10).",
                  "type": "number"
                },
                "perplexity": {
                  "additionalProperties": false,
                  "description": "Perplexity-specific configuration (used when provider=\"perplexity\").",
                  "properties": {
                    "apiKey": {
                      "$ref": "#/definitions/SecretInput",
                      "description": "API key for Perplexity (defaults to PERPLEXITY_API_KEY env var)."
                    },
                    "baseUrl": {
                      "deprecated": "Legacy Sonar/OpenRouter field. Ignored by Search API.",
                      "type": "string"
                    },
                    "model": {
                      "deprecated": "Legacy Sonar/OpenRouter field. Ignored by Search API.",
                      "type": "string"
                    }
                  },
                  "type": "object"
                },
                "provider": {
                  "description": "Search provider (\"brave\", \"gemini\", \"grok\", \"kimi\", or \"perplexity\").",
                  "enum": [
                    "brave",
                    "gemini",
                    "grok",
                    "kimi",
                    "perplexity"
                  ],
                  "type": "string"
                },
                "timeoutSeconds": {
                  "description": "Timeout in seconds for search requests.",
                  "type": "number"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "TtsAutoMode": {
      "enum": [
        "off",
        "always",
        "inbound",
        "tagged"
      ],
      "type": "string"
    },
    "TtsConfig": {
      "additionalProperties": false,
      "properties": {
        "auto": {
          "$ref": "#/definitions/TtsAutoMode",
          "description": "Auto-TTS mode (preferred)."
        },
        "edge": {
          "additionalProperties": false,
          "description": "Microsoft Edge (node-edge-tts) configuration.",
          "properties": {
            "enabled": {
              "description": "Explicitly allow Edge TTS usage (no API key required).",
              "type": "boolean"
            },
            "lang": {
              "type": "string"
            },
            "outputFormat": {
              "type": "string"
            },
            "pitch": {
              "type": "string"
            },
            "proxy": {
              "type": "string"
            },
            "rate": {
              "type": "string"
            },
            "saveSubtitles": {
              "type": "boolean"
            },
            "timeoutMs": {
              "type": "number"
            },
            "voice": {
              "type": "string"
            },
            "volume": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "elevenlabs": {
          "additionalProperties": false,
          "description": "ElevenLabs configuration.",
          "properties": {
            "apiKey": {
              "$ref": "#/definitions/SecretInput"
            },
            "applyTextNormalization": {
              "enum": [
                "auto",
                "on",
                "off"
              ],
              "type": "string"
            },
            "baseUrl": {
              "type": "string"
            },
            "languageCode": {
              "type": "string"
            },
            "modelId": {
              "type": "string"
            },
            "seed": {
              "type": "number"
            },
            "voiceId": {
              "type": "string"
            },
            "voiceSettings": {
              "additionalProperties": false,
              "properties": {
                "similarityBoost": {
                  "type": "number"
                },
                "speed": {
                  "type": "number"
                },
                "stability": {
                  "type": "number"
                },
                "style": {
                  "type": "number"
                },
                "useSpeakerBoost": {
                  "type": "boolean"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        },
        "enabled": {
          "description": "Legacy: enable auto-TTS when `auto` is not set.",
          "type": "boolean"
        },
        "maxTextLength": {
          "description": "Hard cap for text sent to TTS (chars).",
          "type": "number"
        },
        "mode": {
          "$ref": "#/definitions/TtsMode",
          "description": "Apply TTS to final replies only or to all replies (tool/block/final)."
        },
        "modelOverrides": {
          "$ref": "#/definitions/TtsModelOverrideConfig",
          "description": "Allow the model to override TTS parameters."
        },
        "openai": {
          "additionalProperties": false,
          "description": "OpenAI configuration.",
          "properties": {
            "apiKey": {
              "$ref": "#/definitions/SecretInput"
            },
            "baseUrl": {
              "type": "string"
            },
            "model": {
              "type": "string"
            },
            "voice": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "prefsPath": {
          "description": "Optional path for local TTS user preferences JSON.",
          "type": "string"
        },
        "provider": {
          "$ref": "#/definitions/TtsProvider",
          "description": "Primary TTS provider (fallbacks are automatic)."
        },
        "summaryModel": {
          "description": "Optional model override for TTS auto-summary (provider/model or alias).",
          "type": "string"
        },
        "timeoutMs": {
          "description": "API request timeout (ms).",
          "type": "number"
        }
      },
      "type": "object"
    },
    "TtsMode": {
      "enum": [
        "final",
        "all"
      ],
      "type": "string"
    },
    "TtsModelOverrideConfig": {
      "additionalProperties": false,
      "properties": {
        "allowModelId": {
          "description": "Allow model-provided modelId override.",
          "type": "boolean"
        },
        "allowNormalization": {
          "description": "Allow model-provided normalization or language overrides.",
          "type": "boolean"
        },
        "allowProvider": {
          "description": "Allow model-provided provider override (default: false).",
          "type": "boolean"
        },
        "allowSeed": {
          "description": "Allow model-provided seed override.",
          "type": "boolean"
        },
        "allowText": {
          "description": "Allow model-provided TTS text blocks.",
          "type": "boolean"
        },
        "allowVoice": {
          "description": "Allow model-provided voice/voiceId override.",
          "type": "boolean"
        },
        "allowVoiceSettings": {
          "description": "Allow model-provided voice settings override.",
          "type": "boolean"
        },
        "enabled": {
          "description": "Enable model-provided overrides for TTS.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "TtsProvider": {
      "enum": [
        "elevenlabs",
        "openai",
        "edge"
      ],
      "type": "string"
    },
    "TypingMode": {
      "enum": [
        "never",
        "instant",
        "thinking",
        "message"
      ],
      "type": "string"
    },
    "WebConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "If false, do not start the WhatsApp web provider. Default: true.",
          "type": "boolean"
        },
        "heartbeatSeconds": {
          "type": "number"
        },
        "reconnect": {
          "$ref": "#/definitions/WebReconnectConfig"
        }
      },
      "type": "object"
    },
    "WebReconnectConfig": {
      "additionalProperties": false,
      "properties": {
        "factor": {
          "type": "number"
        },
        "initialMs": {
          "type": "number"
        },
        "jitter": {
          "type": "number"
        },
        "maxAttempts": {
          "type": "number"
        },
        "maxMs": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "WhatsAppAccountConfig": {
      "additionalProperties": false,
      "properties": {
        "ackReaction": {
          "$ref": "#/definitions/WhatsAppAckReactionConfig",
          "description": "Acknowledgment reaction sent immediately upon message receipt."
        },
        "allowFrom": {
          "description": "Optional allowlist for WhatsApp direct chats (E.164).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "authDir": {
          "description": "Override auth directory (Baileys multi-file auth state).",
          "type": "string"
        },
        "blockStreaming": {
          "description": "Disable block streaming for this account.",
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "debounceMs": {
          "description": "Debounce window (ms) for batching rapid consecutive messages from the same sender (0 to disable).",
          "type": "number"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI `--deliver` when no explicit `--reply-to` is provided (E.164 or group JID).",
          "type": "string"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "enabled": {
          "description": "Whether the WhatsApp channel is enabled.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for WhatsApp group senders (E.164).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how group messages are handled:\n- \"open\": groups bypass allowFrom, only mention-gating applies\n- \"disabled\": block all group messages entirely\n- \"allowlist\": only allow group messages from senders in groupAllowFrom/allowFrom"
        },
        "groups": {
          "$ref": "#/definitions/Record%3Cstring%2CWhatsAppGroupConfig%3E"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings."
        },
        "historyLimit": {
          "description": "Max group messages to keep as history context (0 disables).",
          "type": "number"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "description": "Maximum media file size in MB. Default: 50.",
          "type": "number"
        },
        "messagePrefix": {
          "description": "Inbound message prefix override (WhatsApp only).",
          "type": "string"
        },
        "name": {
          "description": "Optional display name for this account (used in CLI/UI lists).",
          "type": "string"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override.",
          "type": "string"
        },
        "selfChatMode": {
          "description": "Same-phone setup (bot uses your personal WhatsApp number).",
          "type": "boolean"
        },
        "sendReadReceipts": {
          "description": "Send read receipts for incoming messages (default true).",
          "type": "boolean"
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 4000.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "WhatsAppAckReactionConfig": {
      "additionalProperties": false,
      "properties": {
        "direct": {
          "description": "Send reactions in direct chats. Default: true.",
          "type": "boolean"
        },
        "emoji": {
          "description": "Emoji to use for acknowledgment (e.g., \"👀\"). Empty = disabled.",
          "type": "string"
        },
        "group": {
          "description": "Send reactions in group chats:\n- \"always\": react to all group messages\n- \"mentions\": react only when bot is mentioned\n- \"never\": never react in groups Default: \"mentions\"",
          "enum": [
            "always",
            "mentions",
            "never"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "WhatsAppActionConfig": {
      "additionalProperties": false,
      "properties": {
        "polls": {
          "type": "boolean"
        },
        "reactions": {
          "type": "boolean"
        },
        "sendMessage": {
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "WhatsAppConfig": {
      "additionalProperties": false,
      "properties": {
        "accounts": {
          "$ref": "#/definitions/Record%3Cstring%2CWhatsAppAccountConfig%3E",
          "description": "Optional per-account WhatsApp configuration (multi-account)."
        },
        "ackReaction": {
          "$ref": "#/definitions/WhatsAppAckReactionConfig",
          "description": "Acknowledgment reaction sent immediately upon message receipt."
        },
        "actions": {
          "$ref": "#/definitions/WhatsAppActionConfig",
          "description": "Per-action tool gating (default: true for all)."
        },
        "allowFrom": {
          "description": "Optional allowlist for WhatsApp direct chats (E.164).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blockStreaming": {
          "description": "Disable block streaming for this account.",
          "type": "boolean"
        },
        "blockStreamingCoalesce": {
          "$ref": "#/definitions/BlockStreamingCoalesceConfig",
          "description": "Merge streamed block replies before sending."
        },
        "capabilities": {
          "description": "Optional provider capability tags used for agent/runtime guidance.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chunkMode": {
          "description": "Chunking mode: \"length\" (default) splits by size; \"newline\" splits on every newline.",
          "enum": [
            "length",
            "newline"
          ],
          "type": "string"
        },
        "configWrites": {
          "description": "Allow channel-initiated config writes (default: true).",
          "type": "boolean"
        },
        "debounceMs": {
          "description": "Debounce window (ms) for batching rapid consecutive messages from the same sender (0 to disable).",
          "type": "number"
        },
        "defaultAccount": {
          "description": "Optional default account id when multiple accounts are configured.",
          "type": "string"
        },
        "defaultTo": {
          "description": "Default delivery target for CLI `--deliver` when no explicit `--reply-to` is provided (E.164 or group JID).",
          "type": "string"
        },
        "dmHistoryLimit": {
          "description": "Max DM turns to keep as history context.",
          "type": "number"
        },
        "dmPolicy": {
          "$ref": "#/definitions/DmPolicy",
          "description": "Direct message access policy (default: pairing)."
        },
        "dms": {
          "$ref": "#/definitions/Record%3Cstring%2CDmConfig%3E",
          "description": "Per-DM config overrides keyed by user ID."
        },
        "enabled": {
          "description": "Whether the WhatsApp channel is enabled.",
          "type": "boolean"
        },
        "groupAllowFrom": {
          "description": "Optional allowlist for WhatsApp group senders (E.164).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "groupPolicy": {
          "$ref": "#/definitions/GroupPolicy",
          "description": "Controls how group messages are handled:\n- \"open\": groups bypass allowFrom, only mention-gating applies\n- \"disabled\": block all group messages entirely\n- \"allowlist\": only allow group messages from senders in groupAllowFrom/allowFrom"
        },
        "groups": {
          "$ref": "#/definitions/Record%3Cstring%2CWhatsAppGroupConfig%3E"
        },
        "heartbeat": {
          "$ref": "#/definitions/ChannelHeartbeatVisibilityConfig",
          "description": "Heartbeat visibility settings."
        },
        "historyLimit": {
          "description": "Max group messages to keep as history context (0 disables).",
          "type": "number"
        },
        "markdown": {
          "$ref": "#/definitions/MarkdownConfig",
          "description": "Markdown formatting overrides (tables)."
        },
        "mediaMaxMb": {
          "description": "Maximum media file size in MB. Default: 50.",
          "type": "number"
        },
        "messagePrefix": {
          "description": "Inbound message prefix override (WhatsApp only).",
          "type": "string"
        },
        "responsePrefix": {
          "description": "Outbound response prefix override.",
          "type": "string"
        },
        "selfChatMode": {
          "description": "Same-phone setup (bot uses your personal WhatsApp number).",
          "type": "boolean"
        },
        "sendReadReceipts": {
          "description": "Send read receipts for incoming messages (default true).",
          "type": "boolean"
        },
        "textChunkLimit": {
          "description": "Outbound text chunk size (chars). Default: 4000.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "WhatsAppGroupConfig": {
      "additionalProperties": false,
      "properties": {
        "requireMention": {
          "type": "boolean"
        },
        "tools": {
          "$ref": "#/definitions/GroupToolPolicyConfig"
        },
        "toolsBySender": {
          "$ref": "#/definitions/GroupToolPolicyBySenderConfig"
        }
      },
      "type": "object"
    },
    "WideAreaDiscoveryConfig": {
      "additionalProperties": false,
      "properties": {
        "domain": {
          "description": "Optional unicast DNS-SD domain (e.g. \"openclaw.internal\").",
          "type": "string"
        },
        "enabled": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  "properties": {
    "acp": {
      "$ref": "#/definitions/AcpConfig"
    },
    "agents": {
      "$ref": "#/definitions/AgentsConfig"
    },
    "approvals": {
      "$ref": "#/definitions/ApprovalsConfig"
    },
    "audio": {
      "$ref": "#/definitions/AudioConfig"
    },
    "auth": {
      "$ref": "#/definitions/AuthConfig"
    },
    "bindings": {
      "items": {
        "$ref": "#/definitions/AgentBinding"
      },
      "type": "array"
    },
    "broadcast": {
      "$ref": "#/definitions/BroadcastConfig"
    },
    "browser": {
      "$ref": "#/definitions/BrowserConfig"
    },
    "canvasHost": {
      "$ref": "#/definitions/CanvasHostConfig"
    },
    "channels": {
      "$ref": "#/definitions/ChannelsConfig"
    },
    "cli": {
      "$ref": "#/definitions/CliConfig"
    },
    "commands": {
      "$ref": "#/definitions/CommandsConfig"
    },
    "cron": {
      "$ref": "#/definitions/CronConfig"
    },
    "diagnostics": {
      "$ref": "#/definitions/DiagnosticsConfig"
    },
    "discovery": {
      "$ref": "#/definitions/DiscoveryConfig"
    },
    "env": {
      "additionalProperties": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "$ref": "#/definitions/Record%3Cstring%2Cstring%3E"
          },
          {
            "additionalProperties": false,
            "properties": {
              "enabled": {
                "type": "boolean"
              },
              "timeoutMs": {
                "type": "number"
              }
            },
            "type": "object"
          },
          {
            "not": {}
          }
        ],
        "description": "Sugar: allow env vars directly under env (string values only)."
      },
      "properties": {
        "shellEnv": {
          "additionalProperties": false,
          "description": "Opt-in: import missing secrets from a login shell environment (exec `$SHELL -l -c 'env -0'`).",
          "properties": {
            "enabled": {
              "type": "boolean"
            },
            "timeoutMs": {
              "description": "Timeout for the login shell exec (ms). Default: 15000.",
              "type": "number"
            }
          },
          "type": "object"
        },
        "vars": {
          "$ref": "#/definitions/Record%3Cstring%2Cstring%3E",
          "description": "Inline env vars to apply when not already present in the process env."
        }
      },
      "type": "object"
    },
    "gateway": {
      "$ref": "#/definitions/GatewayConfig"
    },
    "hooks": {
      "$ref": "#/definitions/HooksConfig"
    },
    "logging": {
      "$ref": "#/definitions/LoggingConfig"
    },
    "media": {
      "additionalProperties": false,
      "properties": {
        "preserveFilenames": {
          "description": "Preserve original uploaded filenames when storing inbound media.",
          "type": "boolean"
        },
        "ttlHours": {
          "description": "Optional retention window for persisted inbound media cleanup.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "memory": {
      "$ref": "#/definitions/MemoryConfig"
    },
    "messages": {
      "$ref": "#/definitions/MessagesConfig"
    },
    "meta": {
      "additionalProperties": false,
      "properties": {
        "lastTouchedAt": {
          "description": "ISO timestamp when this config was last written.",
          "type": "string"
        },
        "lastTouchedVersion": {
          "description": "Last OpenClaw version that wrote this config.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "models": {
      "$ref": "#/definitions/ModelsConfig"
    },
    "nodeHost": {
      "$ref": "#/definitions/NodeHostConfig"
    },
    "plugins": {
      "$ref": "#/definitions/PluginsConfig"
    },
    "secrets": {
      "$ref": "#/definitions/SecretsConfig"
    },
    "session": {
      "$ref": "#/definitions/SessionConfig"
    },
    "skills": {
      "$ref": "#/definitions/SkillsConfig"
    },
    "talk": {
      "$ref": "#/definitions/TalkConfig"
    },
    "tools": {
      "$ref": "#/definitions/ToolsConfig"
    },
    "ui": {
      "additionalProperties": false,
      "properties": {
        "assistant": {
          "additionalProperties": false,
          "properties": {
            "avatar": {
              "description": "Assistant avatar (emoji, short text, or image URL/data URI).",
              "type": "string"
            },
            "name": {
              "description": "Assistant display name for UI surfaces.",
              "type": "string"
            }
          },
          "type": "object"
        },
        "seamColor": {
          "description": "Accent color for OpenClaw UI chrome (hex).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "update": {
      "additionalProperties": false,
      "properties": {
        "auto": {
          "additionalProperties": false,
          "description": "Core auto-update policy for package installs.",
          "properties": {
            "betaCheckIntervalHours": {
              "description": "Beta channel check cadence. Default: 1 hour.",
              "type": "number"
            },
            "enabled": {
              "description": "Enable background auto-update checks and apply logic. Default: false.",
              "type": "boolean"
            },
            "stableDelayHours": {
              "description": "Stable channel minimum delay before auto-apply. Default: 6.",
              "type": "number"
            },
            "stableJitterHours": {
              "description": "Additional stable-channel jitter window. Default: 12.",
              "type": "number"
            }
          },
          "type": "object"
        },
        "channel": {
          "description": "Update channel for git + npm installs (\"stable\", \"beta\", or \"dev\").",
          "enum": [
            "stable",
            "beta",
            "dev"
          ],
          "type": "string"
        },
        "checkOnStart": {
          "description": "Check for updates on gateway start (npm installs only).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "web": {
      "$ref": "#/definitions/WebConfig"
    },
    "wizard": {
      "additionalProperties": false,
      "properties": {
        "lastRunAt": {
          "type": "string"
        },
        "lastRunCommand": {
          "type": "string"
        },
        "lastRunCommit": {
          "type": "string"
        },
        "lastRunMode": {
          "enum": [
            "local",
            "remote"
          ],
          "type": "string"
        },
        "lastRunVersion": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "$schema": {
      "type": "string",
      "description": "JSON Schema identifier (optional). VS Code uses this to provide IntelliSense."
    }
  },
  "type": "object",
  "$id": "https://openclaw.ai/schemas/openclaw.schema.json",
  "title": "OpenClaw Configuration",
  "description": "Configuration schema for openclaw.json — the personal AI assistant gateway. See https://docs.openclaw.ai/gateway/configuration for documentation.",
  "x-openclaw-version": "2026.3.8"
}
